---
phase: 05-cp-sat-differential-gate-had
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/alpha2/solvers/cpsat.py
  - tests/test_cpsat_backend.py
  - tests/test_cpsat_status_honesty.py
autonomous: true
requirements: [EXACT-03]
must_haves:
  truths:
    - "H=C5 solves optimize end-to-end to PROVED_OPTIMAL had_2=3 via get_backend('cpsat'), and the extracted family passes verify_certificate — the full second-engine slice: shared Had2Problem -> CP-SAT -> two-condition status gate -> trust root"
    - "A CP-SAT FEASIBLE (incumbent) optimize result maps to INCUMBENT_ONLY, never PROVED_OPTIMAL; exact_value() raises NotProvedOptimal on it — the CP-SAT twin of the incumbent-as-optimum hole is unrepresentable"
    - "PROVED_OPTIMAL is assigned ONLY when status == cp_model.OPTIMAL AND round(objective_value) == round(best_objective_bound); INFEASIBLE in optimize mode maps to ERROR (empty family is always feasible), never PROVED_INFEASIBLE"
    - "Recorded/impossibility solves run deterministically: num_workers==1 and a pinned random_seed are set on the solver; every ExactOutcome carries backend='cpsat' and backend_version containing 'ortools==9.15.6755'"
    - "ortools is imported ONLY by solvers/cpsat.py; the module registers 'cpsat' via register_backend and reuses the frozen build_had2_problem (no re-enumeration)"
  artifacts:
    - path: "src/alpha2/solvers/cpsat.py"
      provides: "CPSATBackend adapter: map_status, guarded extraction, num_workers=1+random_seed determinism, optional symmetry_level pass-through, ortools version stamp, register_backend('cpsat')"
      contains: "register_backend"
      min_lines: 120
    - path: "tests/test_cpsat_backend.py"
      provides: "E2E panel: C5 optimize->3 through verify_certificate, empty-H->n, decision both legs, bool target_k rejected, ortools stamp"
      contains: "verify_certificate"
    - path: "tests/test_cpsat_status_honesty.py"
      provides: "FEASIBLE!=OPTIMAL (INCUMBENT_ONLY under a tight budget), obj==bound gate, exact_value raises off the gate, map_status pure-function table"
      contains: "INCUMBENT_ONLY"
  key_links:
    - from: "src/alpha2/solvers/cpsat.py"
      to: "alpha2.solvers.problems.had2.build_had2_problem"
      via: "shared checksum-gated Had2Problem translation"
      pattern: "build_had2_problem"
    - from: "src/alpha2/solvers/cpsat.py"
      to: "alpha2.solvers.backend.register_backend"
      via: "module-bottom lazy registry hook"
      pattern: "register_backend\\(\"cpsat\""
---

<objective>
Deliver the MVP vertical slice of Phase 5: a second independent exact backend
(`solvers/cpsat.py`) that translates the SAME frozen `Had2Problem` into a CP-SAT
model and returns a status-honest `ExactOutcome`, proven end-to-end by solving
H=C5 to PROVED_OPTIMAL had_2=3 with the family routed through the frozen trust
root. This is the thinnest end-to-end second engine; every later plan (differential
gate, had_3, symmetry, seed-137 panel) composes on top of it.

Purpose: EXACT-03. CP-SAT exists so it can DISAGREE with CBC when one has a bug;
its whole value is epistemic, so the status honesty (FEASIBLE != OPTIMAL,
obj==bound, deterministic recorded mode) is the load-bearing work, not the API.
Output: `solvers/cpsat.py` (the ONLY ortools importer) + two test files green.

Open-Q resolution (RESEARCH Open-Q4, recorded here for audit): recorded/impossibility
mode default is `num_workers=1` + pinned `random_seed` (simplest clearly-deterministic
mode); `interleave_search` is NOT the default and is reserved for later exploration/
survivor scaling.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-cp-sat-differential-gate-had/05-RESEARCH.md
@.planning/phases/05-cp-sat-differential-gate-had/05-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs; extracted from the frozen Phase-4 code. Use directly — no exploration. -->

From src/alpha2/solvers/result.py (FROZEN — do not edit):
  class Status(Enum): MODEL_FOUND, PROVED_OPTIMAL, PROVED_INFEASIBLE, INCUMBENT_ONLY, UNKNOWN, ERROR
  class NotProvedOptimal(Exception)
  @dataclass(frozen=True) class SolveParams: time_limit_s: float|None=None; threads: int=1
  @dataclass(frozen=True) class ExactOutcome:
     problem, mode, status, value:int|None, bound:int|float|None, bound_source:str,
     family:tuple|None, backend:str, backend_version:str, params:SolveParams, wall_time_s:float
     # __post_init__ enforces: VALUED statuses (PROVED_OPTIMAL/INCUMBENT_ONLY/MODEL_FOUND)
     #   require a genuine int value (bool/float/None rejected); others require value=None.
     #   PROVED_OPTIMAL requires value==bound AND family is not None.
     #   bound_source in ("definition","cbc_log","trivial_n","none").
     def exact_value(self)->int  # raises NotProvedOptimal unless PROVED_OPTIMAL

From src/alpha2/solvers/backend.py (FROZEN):
  def register_backend(name:str, factory)->None
  def get_backend(name:str)->ExactBackend  # lazily imports alpha2.solvers.{name}
  Protocol ExactBackend: name:str; backend_version()->str;
     solve_had2(adj:list[set[int]], n:int, *, mode:str, target_k:int|None=None, params:SolveParams|None=None)->ExactOutcome

From src/alpha2/solvers/problems/had2.py (FROZEN — reuse, do NOT re-enumerate):
  @dataclass(frozen=True) Had2Problem: n:int; Gedges:list[(u,v)]; ss:set{(u,v)}; sp:set{(v,(a,b))}; pp:set{frozenset{(e1),(e2)}}
  def build_had2_problem(adj, n)->Had2Problem   # checksum-gated on EVERY build; raises ValueError/ChecksumError

From src/alpha2/corpus/schema.py (FROZEN): make_backends already routes "cpsat"/"cp-sat"/"cp_sat" -> ortools stamp.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — the CP-SAT backend panel (failing end-to-end happy path first)</name>
  <read_first>
    - tests/test_cbc_backend.py (the exact analog — copy `_c5_adj`/`_empty_adj`/`_h_edges`/`_assert_stamp` shape, Tests 6/7/8/8b; swap the stamp to ortools)
    - src/alpha2/solvers/backend.py (get_backend semantics; a missing module raises ValueError until Task 2)
    - src/alpha2/corpus/verifier.py (verify_certificate is the ONLY truth-conferring call)
    - tests/test_had2_problem.py (the C5/empty literals + brute-force differential idiom, for the n<=8 sanity legs)
  </read_first>
  <behavior>
    - test_c5_optimize_proved_optimal_through_trust_root: get_backend("cpsat").solve_had2(_c5_adj(),5,mode="optimize") -> status PROVED_OPTIMAL, exact_value()==3, bound==3, len(family)==3; build_record(...) + verify_certificate(rec) returns 3.
    - test_empty_h_k5: empty H at n=5 -> PROVED_OPTIMAL had_2=5, all singletons, verify_certificate==5.
    - test_decision_both_legs: decision target_k=3 -> MODEL_FOUND (exact_value() raises NotProvedOptimal); target_k=4 -> PROVED_INFEASIBLE (value None, family None).
    - test_decision_rejects_bool_target_k: True/False/0/-1/None/3.0/"3" each raise ValueError.
    - test_stamp: backend=="cpsat" and "ortools==9.15.6755" in backend_version, wall_time_s>0.
    - test_brute_force_differential_n_le_8: reuse the had2 brute-force reference; cpsat optimize exact_value()==brute on the tiny panel (independent of cbc, second-engine cross-check).
  </behavior>
  <action>
    Write tests/test_cpsat_backend.py mirroring tests/test_cbc_backend.py structure but driving get_backend("cpsat"). Reuse the embedded-literal discipline (in-file _c5_adj/_empty_adj/_h_edges; no cross-test imports). Build records via schema.build_record + provenance_params exactly as the cbc analog (lines 132-143, 163-173). Trust-root calls made OUTSIDE any assert truth-expression (call, bind k, then compare). At RED the import of solvers.cpsat has no backend registered so get_backend("cpsat") raises ValueError — that is the intended failing state. Do NOT stub cpsat.py in this task.
  </action>
  <verify>
    <automated>uv run --frozen --extra dev pytest tests/test_cpsat_backend.py -q 2>&1 | tail -5  # expect collection/failure (RED): no cpsat backend yet</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_cpsat_backend.py exists and imports verify_certificate + get_backend
    - Running it fails RED (ValueError "unknown exact backend 'cpsat'" or ModuleNotFound), NOT an accidental green
    - No file under src/ is modified by this task
  </acceptance_criteria>
  <done>The failing happy-path panel is committed; it pins the exact EXACT-03 contract before any implementation exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — solvers/cpsat.py adapter (thinnest slice that passes the panel)</name>
  <read_first>
    - src/alpha2/solvers/cbc.py (the near-line-for-line analog: module docstring discipline, _EXTRACTABLE, map_status two-field gate, _guarded_extract recompute/disjointness, solve_had2 arg guards lines 179-205, sorted() conflict iteration lines 226-231, ExactOutcome assembly lines 278-290, register_backend line 293)
    - src/alpha2/solvers/problems/had2.py (build_had2_problem + Had2Problem fields to translate)
    - src/alpha2/solvers/result.py (ExactOutcome/__post_init__ invariants the return must satisfy)
    - .planning/phases/05-cp-sat-differential-gate-had/05-RESEARCH.md (Pattern 1 status-mapping table + verified CP-SAT snake_case API + determinism knobs)
    - .planning/phases/05-cp-sat-differential-gate-had/05-PATTERNS.md (cpsat.py section: exact code shapes for model translation, map_status, extraction)
  </read_first>
  <action>
    Create src/alpha2/solvers/cpsat.py — the ONLY module importing `from ortools.sat.python import cp_model`. Copy cbc.py's structure wholesale, swapping pulp->cp_model:
    - map_status(status, solver, mode): OPTIMAL & round(objective_value)==round(best_objective_bound) -> MODEL_FOUND (decision) / PROVED_OPTIMAL (optimize); OPTIMAL & obj!=bound -> ERROR; FEASIBLE -> MODEL_FOUND (decision) / INCUMBENT_ONLY (optimize); INFEASIBLE -> PROVED_INFEASIBLE (decision) / ERROR (optimize); UNKNOWN -> UNKNOWN; MODEL_INVALID/other -> ERROR.
    - solve_had2(self, adj, n, *, mode, target_k=None, params=None, symmetry_level=None): copy the cbc mode/target_k/bool guards verbatim (lines 179-199); REPLACE the CBC `p.threads!=1` guard — CP-SAT determinism is num_workers=1 + pinned random_seed, not threads.
    - Model: problem = build_had2_problem(adj, n); mv = new_bool_var per problem.Gedges; sv = new_bool_var per vertex; per-vertex add_at_most_one([mv for e if v in e]+[sv[v]]); add_at_most_one over sorted(problem.ss)/sorted(problem.sp)/sorted(pp) exactly as cbc.py sorts. optimize: m.maximize(sum). decision: m.add(sum >= target_k) (no objective).
    - Solver: solver.parameters.num_workers=1; solver.parameters.random_seed = (pinned; use params/SolveParams seed convention or a fixed module constant e.g. 137); if params.time_limit_s: max_time_in_seconds; if mode=="decision": stop_after_first_solution=True; if symmetry_level is not None: solver.parameters.symmetry_level=symmetry_level; log_search_progress=True. DO NOT set search_branching with a bare int (enum; raises TypeError).
    - Guarded extraction (extract ONLY inside _EXTRACTABLE): fam = G-edge tuples with boolean_value True + (v,) singletons True; drop the CBC fractional-integrality loop (CP-SAT booleans are exact) but KEEP the recompute guard round(objective_value)==len(fam) (optimize) / len(fam)>=target_k (decision) and the disjointness `used`-set guard -> return None on any trip -> caller maps to Status.ERROR.
    - backend_version(self): f"ortools=={importlib.metadata.version('ortools')}". name="cpsat".
    - bound: PROVED_OPTIMAL -> bound=value, bound_source="definition"; INCUMBENT_ONLY/UNKNOWN optimize -> bound = round(best_objective_bound) if available else n, bound_source "trivial_n" (there is no cbc_log analog — do NOT invent one; use "trivial_n").
    - register_backend("cpsat", CPSATBackend) at module bottom.
  </action>
  <verify>
    <automated>uv run --frozen --extra dev pytest tests/test_cpsat_backend.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "import" src/alpha2/solvers/cpsat.py` shows ortools imported; `grep -rl "ortools" src/alpha2 | grep -v cpsat.py` returns nothing (confinement)
    - src/alpha2/solvers/cpsat.py contains `register_backend("cpsat"` and `build_had2_problem`
    - tests/test_cpsat_backend.py passes (C5->3, empty->5, decision both legs, bool rejection, ortools stamp, n<=8 brute differential)
    - No edit to cbc.py, result.py, backend.py, problems/had2.py
  </acceptance_criteria>
  <done>get_backend("cpsat") solves C5 to PROVED_OPTIMAL=3 end-to-end through verify_certificate; the second engine is real.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Status-honesty hardening (FEASIBLE != OPTIMAL; obj==bound; map_status table)</name>
  <read_first>
    - tests/test_cbc_status_honesty.py (the analog: timed-out incumbent -> non-exact status; exact_value raises)
    - src/alpha2/solvers/cpsat.py (the map_status just written)
    - .planning/phases/05-cp-sat-differential-gate-had/05-RESEARCH.md (Pitfall 1 + Pattern 1 table)
  </read_first>
  <behavior>
    - test_map_status_table (pure, no solve): every (cp_model status, mode) row of the Pattern-1 table maps to the exact Status; OPTIMAL-with-obj!=bound and INFEASIBLE-optimize both -> ERROR.
    - test_feasible_optimize_is_incumbent_only: force a stop with an incumbent (tiny max_time_in_seconds on a non-trivial instance, or drive map_status with cp_model.FEASIBLE) -> INCUMBENT_ONLY; exact_value() raises NotProvedOptimal.
    - test_proved_optimal_requires_obj_equals_bound: a PROVED_OPTIMAL outcome has bound==value; map_status returns ERROR when round(obj)!=round(bound).
    - test_determinism_params_set: after an optimize solve the recorded params reflect single-worker recorded mode (num_workers==1 was applied; assert via a small introspection helper or by constructing the solver path).
  </behavior>
  <action>
    Write tests/test_cpsat_status_honesty.py. Import map_status from alpha2.solvers.cpsat and cp_model for the enum constants. Drive the pure map_status table for all statuses x {optimize,decision}. For the FEASIBLE leg prefer driving map_status directly with cp_model.FEASIBLE (deterministic, ms) plus one integration leg with a tight max_time budget asserting status in {INCUMBENT_ONLY, UNKNOWN} and exact_value() raising. Keep all trust-root/exact reads outside assert truth-expressions.
  </action>
  <verify>
    <automated>uv run --frozen --extra dev pytest tests/test_cpsat_status_honesty.py -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_cpsat_status_honesty.py contains INCUMBENT_ONLY and NotProvedOptimal
    - map_status(cp_model.FEASIBLE, solver, "optimize") is Status.INCUMBENT_ONLY and (..., "decision") is Status.MODEL_FOUND
    - map_status for INFEASIBLE optimize is Status.ERROR (never PROVED_INFEASIBLE)
    - pytest tests/test_cpsat_status_honesty.py exits 0
  </acceptance_criteria>
  <done>The CP-SAT incumbent-as-optimum hole is proven closed: no FEASIBLE optimize result can read as an exact had_2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CP-SAT solver -> ExactOutcome | Untrusted solver output crosses into the status-honest result type; a mis-mapped status is a false claim |
| ExactOutcome.family -> verify_certificate | Solver-proposed family is UNTRUSTED until the frozen trust root arbitrates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering (false optimum) | cpsat.map_status / extraction | mitigate | PROVED_OPTIMAL gated on OPTIMAL AND round(objective_value)==round(best_objective_bound); FEASIBLE->INCUMBENT_ONLY; extraction only inside _EXTRACTABLE + recompute guard; proven by test_cpsat_status_honesty.py |
| T-05-02 | Information disclosure (garbage value surfaces) | ExactOutcome | mitigate | exact_value() raises off PROVED_OPTIMAL (frozen result.py invariant); tests assert the raise |
| T-05-03 | Tampering (nondeterministic recorded claim) | cpsat solve params | mitigate | num_workers=1 + pinned random_seed for recorded mode; never default multi-worker; recorded verbatim in params |
| T-05-04 | Elevation (encoding admits disconnected branch set) | model translation | mitigate | pair vars indexed over problem.Gedges ONLY (shared checksum-gated Had2Problem); verifier is the independent arbiter |
| T-05-SC | Tampering (supply chain) | ortools install | accept | No new package installed this phase; ortools==9.15.6755 is the Phase-1-audited pinned artifact; do not auto-download outside the frozen lock |
</threat_model>

<verification>
- `uv run --frozen --extra dev pytest tests/test_cpsat_backend.py tests/test_cpsat_status_honesty.py -q` green.
- Confinement: ortools imported only in cpsat.py (`grep -rl ortools src/alpha2 | grep -v cpsat`).
- Existing suite untouched: `uv run --frozen --extra dev pytest -q -m "not slow"` still green.
</verification>

<success_criteria>
CP-SAT is a real, status-honest second backend: C5 optimize -> PROVED_OPTIMAL had_2=3 through the trust root; FEASIBLE can never read as exact; recorded mode is deterministic (num_workers=1 + seed); ortools confined to one module and version-stamped.
</success_criteria>

<output>
Create `.planning/phases/05-cp-sat-differential-gate-had/05-01-SUMMARY.md` when done.
</output>
