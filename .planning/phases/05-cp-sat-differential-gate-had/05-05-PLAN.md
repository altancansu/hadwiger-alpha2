---
phase: 05-cp-sat-differential-gate-had
plan: 05
type: execute
wave: 2
depends_on: ["05-01", "05-02", "05-03"]
files_modified:
  - src/alpha2/solvers/cbc.py
  - src/alpha2/solvers/cpsat.py
  - tests/test_had3_backends.py
autonomous: true
requirements: [EXACT-05, EXACT-03]
must_haves:
  truths:
    - "Both backends expose solve_had3(adj, n, *, mode, target_k=None, params=None) behind a flag, translating the SAME frozen Had3Problem (seagull tier: triples with <=1 H-edge); the shared instance is what makes CBC-vs-CP-SAT had_3 agreement meaningful"
    - "On a synthetic size-3-forced instance (had_2 < chi, size-3 family reaches chi) both backends solve had_3 to PROVED_OPTIMAL == chi, and the extracted size-3 family passes the widened verify_certificate (size-3 gate)"
    - "had_3 fires ONLY on a certified had_2 < chi: solve_had3 is gated on / documented against a prior had_2 PROVED_OPTIMAL value < chi (the escalation trigger); it is never run speculatively on a had_2 == chi instance in the recorded path"
    - "A lost size-3 constraint is caught three ways: the had3 checksum (Plan 02), the widened verifier (Plan 03), and the CBC-vs-CP-SAT had_3 differential (one backend > the other on the synthetic instance)"
  artifacts:
    - path: "src/alpha2/solvers/cbc.py"
      provides: "solve_had3 additive method translating Had3Problem into the pulp/CBC model (triple vars + size-3 conflict rows); same two-field status gate + guarded extraction"
      contains: "solve_had3"
    - path: "src/alpha2/solvers/cpsat.py"
      provides: "solve_had3 additive method translating Had3Problem into CP-SAT (triple bool vars + size-3 conflicts); same OPTIMAL+obj==bound gate + determinism"
      contains: "solve_had3"
    - path: "tests/test_had3_backends.py"
      provides: "Synthetic size-3-forced instance: had_2 < chi on both backends, then had_3 == chi on both, families through the widened trust root; had_3 differential agreement"
      contains: "verify_certificate"
  key_links:
    - from: "src/alpha2/solvers/cpsat.py"
      to: "alpha2.solvers.problems.had3.build_had3_problem"
      via: "shared checksum-gated Had3Problem translation"
      pattern: "build_had3_problem"
    - from: "tests/test_had3_backends.py"
      to: "alpha2.corpus.verifier.verify_certificate"
      via: "size-3 family routed through the widened trust root"
      pattern: "verify_certificate"
---

<objective>
Wire the had_3 escalation tier into BOTH exact backends behind a flag: add
`solve_had3` to `cbc.py` and `cpsat.py`, each translating the SAME frozen
`Had3Problem` (Plan 02) into its own model, and prove on a synthetic size-3-forced
instance that had_2 < chi but had_3 == chi on both engines — with the size-3 family
arbitrated by the widened trust root (Plan 03) and the two engines agreeing.

Purpose: EXACT-05 (escalation half) + EXACT-03 (CP-SAT "for scale" now covers had_3).
The runbook step-5 escalation becomes REAL and proven on synthetics before any real
had_2 < chi event needs it. No real corpus instance reaches this (296/296 killed at
had_2), so the proof is synthetic by necessity.
Output: solve_had3 on both backends + a dual-backend size-3-forced test.

Trigger discipline (RESEARCH Pattern 3, recorded for audit): had_3 fires ONLY on a
certified had_2 < chi. The test constructs the trigger explicitly (assert had_2 < chi
first, THEN escalate); solve_had3 is behind a flag/method and never on the default had_2 path.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-cp-sat-differential-gate-had/05-RESEARCH.md
@.planning/phases/05-cp-sat-differential-gate-had/05-PATTERNS.md
@src/alpha2/solvers/problems/had3.py

<interfaces>
From src/alpha2/solvers/problems/had3.py (Plan 02): build_had3_problem(adj, n) -> Had3Problem(n, triples, conflicts); ChecksumError.
From src/alpha2/solvers/cbc.py (Plan 01 baseline unchanged): CBCBackend.solve_had2 pattern — LpProblem, Binary vars, disjointness rows, sorted conflict rows, map_status two-field gate, _guarded_extract, ExactOutcome assembly.
From src/alpha2/solvers/cpsat.py (Plan 01): CPSATBackend.solve_had2 pattern — cp_model, new_bool_var, add_at_most_one, maximize / add(>=target_k), num_workers=1+random_seed, map_status, guarded extraction.
From src/alpha2/corpus/verifier.py (Plan 03, widened): verify_model_record/verify_certificate accept size in {1,2,3} with the >=2-G-edges size-3 connectivity check.
From src/alpha2/solvers/result.py: ExactOutcome(problem=...) — set problem="had3" for these outcomes.

NOTE: model_branch_sets for a had_3 family mixes sizes {1,2,3}; the family a backend returns is an UNTRUSTED proposal until verify_certificate passes.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — synthetic size-3-forced dual-backend had_3 test</name>
  <read_first>
    - tests/test_cbc_backend.py + tests/test_had2_problem.py (E2E dual-backend + synthetic small-instance idioms; brute-force reference for had_2)
    - src/alpha2/solvers/problems/had3.py (Had3Problem triples for the chosen instance)
    - src/alpha2/corpus/verifier.py (widened size-3 gate the family must pass)
    - .planning/phases/05-cp-sat-differential-gate-had/05-RESEARCH.md (Pattern 3: fire only on had_2<chi; seagull tier)
  </read_first>
  <behavior>
    - Choose/construct a small triangle-free H (hand-verified) where had_2(G) < chi(G) but a size-3 family of size chi exists (a size-3-forced instance). Pin its invariants (n, chi, had_2, had_3) as in-file literals.
    - test_trigger_had2_below_chi: both backends solve had_2 optimize -> PROVED_OPTIMAL with equal value == the pinned had_2, and had_2 < chi (the escalation trigger is real, and CBC==CP-SAT).
    - test_had3_reaches_chi_both_backends: both backends solve had_3 -> PROVED_OPTIMAL == chi; the size-3 family routes through verify_certificate and returns chi.
    - test_had3_differential_agreement: differential_verdict-style equality — CBC had_3 == CP-SAT had_3 on the instance (a lost size-3 constraint would break this).
    - test_had3_family_is_size_le3: every extracted branch set has size in {1,2,3}, and at least one has size 3 (the escalation genuinely used a triple).
  </behavior>
  <action>
    Write tests/test_had3_backends.py. Embed the synthetic size-3-forced H and its pinned invariants in-file. Assert the had_2<chi trigger FIRST (both backends), then escalate to had_3 on both. Build the had_3 record via build_record with problem-appropriate provenance and route model_branch_sets (the size-<=3 family) through verify_certificate (bind k, then compare — outside assert truth-expressions). At RED, solve_had3 does not exist on either backend -> AttributeError; intended.
  </action>
  <verify>
    <automated>uv run --frozen --extra dev pytest tests/test_had3_backends.py -q 2>&1 | tail -6  # RED: solve_had3 absent</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_had3_backends.py exists with an embedded size-3-forced instance and pinned had_2<chi / had_3==chi literals
    - It asserts the had_2<chi trigger before escalating, and routes the size-3 family through verify_certificate
    - Running it fails RED (AttributeError: solve_had3)
  </acceptance_criteria>
  <done>The dual-backend had_3 escalation contract (trigger on had_2<chi, reach chi on both, verify size-3) is pinned before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — add solve_had3 to cbc.py and cpsat.py (translate the shared Had3Problem)</name>
  <read_first>
    - src/alpha2/solvers/cbc.py (solve_had2 — copy its model-build/gate/extraction structure for solve_had3)
    - src/alpha2/solvers/cpsat.py (solve_had2 — copy its structure)
    - src/alpha2/solvers/problems/had3.py (Had3Problem.triples + conflicts representation to translate into vars/rows)
    - tests/test_had3_backends.py (the contract just written)
    - .planning/phases/05-cp-sat-differential-gate-had/05-RESEARCH.md (Pattern 3 model shape; both backends translate the same Had3Problem)
  </read_first>
  <action>
    Add solve_had3(self, adj, n, *, mode, target_k=None, params=None) to BOTH CBCBackend and CPSATBackend, mirroring each backend's own solve_had2:
    - Reuse the had_2 variables (Gedges + singletons) AND add triple variables from build_had3_problem(adj, n).triples (seagull tier: <=1 H-edge triples). Objective = maximize total family size (singletons + pairs + triples). This is the tier-1 seagull model (objective unchanged, model grows by conflict-sparse triple vars) per RESEARCH Pattern 3 / Blueprint 4 Tier 1.
    - Add per-vertex disjointness INCLUDING triple vars that cover a vertex; add the size-3 conflict rows from Had3Problem.conflicts (a triple conflicts with a set iff all cross pairs are H-edges — pruned by common H-neighborhood). Keep sorted() iteration for determinism.
    - CBC: LpProblem/Binary vars/`<=1` rows/two-field gate/_guarded_extract (extend extraction to include triple vars; recompute guard round(objective)==len(fam) still holds since fam now includes triples). CP-SAT: new_bool_var/add_at_most_one/maximize or add(>=target_k)/num_workers=1+random_seed/OPTIMAL+obj==bound gate/guarded extraction including triples.
    - Return ExactOutcome(problem="had3", ...); family = list of size-1/2/3 tuples. Do NOT alter solve_had2. Keep guards raise-based (no assert). Keep ortools confined to cpsat.py, pulp to cbc.py.
  </action>
  <verify>
    <automated>uv run --frozen --extra dev pytest tests/test_had3_backends.py tests/test_cbc_backend.py tests/test_cpsat_backend.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Both src/alpha2/solvers/cbc.py and src/alpha2/solvers/cpsat.py contain solve_had3 and build_had3_problem
    - solve_had2 tests (test_cbc_backend, test_cpsat_backend) still pass unchanged (additive change)
    - tests/test_had3_backends.py exits 0: both backends had_2<chi then had_3==chi, size-3 family verified, CBC==CP-SAT on had_3
    - Confinement intact: `grep -rl ortools src/alpha2 | grep -v cpsat.py` empty; `grep -rl "import pulp" src/alpha2 | grep -v cbc.py` empty
  </acceptance_criteria>
  <done>had_3 fires behind a flag on both engines, reaches chi on the synthetic instance, and its size-3 family is arbitrated by the widened trust root — the escalation tier is real and cross-checked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Had3Problem -> backend model | Size-3 conflicts are NOT a clean local substructure of H; a lost constraint inflates had_3 plausibly |
| had_3 family -> widened verifier | The size-3 family is an UNTRUSTED proposal; only the widened trust root confers truth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-14 | Tampering (inflated had_3 from a lost size-3 constraint) | solve_had3 model build | mitigate | Three independent guards: had3 checksum (Plan 02), widened verifier (Plan 03), CBC-vs-CP-SAT had_3 differential (this plan) — one backend > the other flags a lost row |
| T-05-15 | Tampering (speculative escalation) | had_3 trigger | mitigate | had_3 fires only on certified had_2<chi; the test asserts the trigger before escalating; solve_had3 is a separate flagged method, not on the had_2 path |
| T-05-16 | Tampering (false had_3 optimum) | status gate | mitigate | Same two-field/two-condition PROVED_OPTIMAL gate + guarded extraction as solve_had2, extended over triple vars |
| T-05-SC | Tampering (supply chain) | — | accept | No package installed; pulp confined to cbc.py, ortools to cpsat.py |
</threat_model>

<verification>
- `uv run --frozen --extra dev pytest tests/test_had3_backends.py tests/test_cbc_backend.py tests/test_cpsat_backend.py -q` green.
- Confinement preserved (grep checks above).
- Full fast suite: `uv run --frozen --extra dev pytest -q -m "not slow"` green.
</verification>

<success_criteria>
had_3 works behind a flag on BOTH backends: on a synthetic size-3-forced instance had_2 < chi and had_3 == chi on CBC and CP-SAT (agreeing), the size-3 family passes the widened trust root, and the escalation fires only on a certified had_2 < chi.
</success_criteria>

<output>
Create `.planning/phases/05-cp-sat-differential-gate-had/05-05-SUMMARY.md` when done.
</output>
