---
phase: 05-cp-sat-differential-gate-had
plan: 06
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/alpha2/solvers/symmetry.py
  - tests/test_symmetry_assume_verify.py
autonomous: true
requirements: [EXACT-06]
must_haves:
  truths:
    - "The H=C5 'WLOG vertex 0 unused' disaster is a passing regression: the invalid hand constraint yields had_2=2 < 3=chi (a FABRICATED sub-chi counterexample), and routing it through assume_and_verify reruns WITHOUT symmetry breaking and returns the correct had_2=3"
    - "assume_and_verify NEVER lets a symmetry-broken run surface an impossibility-flavored (< chi) outcome without a no-SB rerun: attempting to do so raises SBContaminationError"
    - "On/off differential holds on a small vertex-transitive battery (C5 + at least one other cycle/small instance): had_2(sound SB via CP-SAT symmetry_level) == had_2(no SB)"
    - "symmetry.py is stdlib-only control-flow discipline (imports the frozen Status enum + consumes ExactOutcomes/a callable); it imports NO solver library and pulls in NO pynauty"
  artifacts:
    - path: "src/alpha2/solvers/symmetry.py"
      provides: "SBContaminationError + assume_and_verify(sb_outcome, rerun_no_sb, *, chi) enforcing the rerun-without-SB rule for any < chi conclusion; sound-path helper using CP-SAT symmetry_level"
      contains: "SBContaminationError"
      min_lines: 30
    - path: "tests/test_symmetry_assume_verify.py"
      provides: "C5 vertex-unused disaster regression (SB->2, assume_and_verify->3); SBContaminationError on unguarded < chi; on/off differential on a small vertex-transitive battery"
      contains: "assume_and_verify"
  key_links:
    - from: "tests/test_symmetry_assume_verify.py"
      to: "get_backend('cpsat').solve_had2(..., symmetry_level=...)"
      via: "sound-path SB via CP-SAT symmetry_level (no new dependency)"
      pattern: "symmetry_level"
    - from: "src/alpha2/solvers/symmetry.py"
      to: "alpha2.solvers.result.Status"
      via: "impossibility-flavored (< chi) detection over ExactOutcomes"
      pattern: "Status"
---

<objective>
Encode the assume-and-verify symmetry-breaking discipline (`solvers/symmetry.py`)
and prove it on the H=C5 "WLOG vertex 0 unused" disaster: symmetry breaking may
accelerate the EXISTENCE branch, but any impossibility-flavored (< chi) conclusion
MUST rerun without SB before it is recorded or enters the differential gate. The
C5 disaster (had_2=3=chi, "vertex 0 unused" fabricates had_2=2<3) is a passing
regression.

Purpose: EXACT-06. SB is standard but treacherous: on C5 (and on trivial-Aut TFP
complements) a "WLOG" constraint can be satisfied by no optimum, fabricating a
sub-chi SHC counterexample. The discipline makes that structurally impossible for
recorded/impossibility claims.
Output: symmetry.py (stdlib-only control-flow discipline) + the disaster regression + on/off differential.

Open-Q resolution (RESEARCH Open-Q2, recorded for audit): pynauty is DEFERRED (not
installed; needs a C compiler). EXACT-06's MVP is fully satisfiable without it — the
C5 disaster uses a deliberately-invalid HAND constraint (built inline in the test)
and the sound path uses CP-SAT's internal symmetry_level (objective-preserving,
sound by construction). Orbit/lex-leader SB from a computed Aut(H) pays off only
when vertex-transitive pools run (Phases 8-9); it is deferred there, not descoped —
no EXACT-06 success criterion requires it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-cp-sat-differential-gate-had/05-RESEARCH.md
@.planning/phases/05-cp-sat-differential-gate-had/05-PATTERNS.md

<interfaces>
From src/alpha2/solvers/result.py (FROZEN): Status.PROVED_OPTIMAL; ExactOutcome.status, .value, .exact_value().
From src/alpha2/solvers/backend.py: get_backend("cpsat").solve_had2(adj, n, *, mode="optimize", symmetry_level=<int>)  # symmetry_level kwarg added in Plan 01.
From src/alpha2/solvers/cbc.py (test literal): _c5_adj() = [{1,4},{0,2},{1,3},{2,4},{3,0}] — had_2=3=chi.

RESEARCH-verified inline CP-SAT model (for the disaster demo, built in the TEST):
  cp_model over C5 with mv (G-edge bool) + sv (vertex bool) + AtMostOne disjointness/conflicts,
  PLUS the invalid hand constraint forcing vertex 0 unused (sv[0]==0 and no mv touching 0)
  -> maximize -> OPTIMAL objective 2 (a fabricated had_2=2 < 3). The sound solve (no hand constraint) -> 3.

assume_and_verify contract: given an SB-on ExactOutcome and a zero-arg rerun_no_sb callable,
  if the SB-on outcome is impossibility-flavored (PROVED_OPTIMAL with value < chi), it MUST call
  rerun_no_sb and RETURN that no-SB outcome; if rerun_no_sb is None it raises SBContaminationError;
  if the SB-on outcome verifies >= chi it is returned as-is (existence branch: certificate self-justifies).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — C5 disaster regression + SBContaminationError + on/off differential</name>
  <read_first>
    - tests/test_cbc_backend.py (the C5 literal + E2E idiom)
    - .planning/phases/05-cp-sat-differential-gate-had/05-RESEARCH.md (Pattern 5: the hand-verified C5 disaster; the code example CP-SAT model shape)
    - src/alpha2/solvers/cpsat.py (the symmetry_level kwarg on solve_had2 to drive the sound path)
    - src/alpha2/solvers/result.py (Status/exact_value for < chi detection)
  </read_first>
  <behavior>
    - test_c5_sb_fabricates_sub_chi: build the C5 CP-SAT model INLINE (cp_model) with the invalid "vertex 0 unused" hand constraint; solve -> OPTIMAL objective 2; construct the corresponding ExactOutcome (had_2=2, which is < chi=3).
    - test_assume_verify_restores_had2_3: assume_and_verify(sb_outcome=<the fabricated 2>, rerun_no_sb=lambda: get_backend("cpsat").solve_had2(c5,5,mode="optimize"), chi=3) returns an outcome with exact_value()==3 (the no-SB rerun).
    - test_unguarded_sub_chi_raises: assume_and_verify(sb_outcome=<fabricated 2>, rerun_no_sb=None, chi=3) raises SBContaminationError.
    - test_existence_branch_passthrough: an SB-on outcome that verifies >= chi is returned unchanged (no rerun forced).
    - test_on_off_differential: on C5 (and >=1 other small vertex-transitive instance, e.g. C7 or empty-H=K_n), had_2 via get_backend("cpsat").solve_had2(..., symmetry_level=<sound level>) == had_2 with no symmetry_level.
    - test_no_pynauty_import: importing alpha2.solvers.symmetry does not import pynauty/ortools/pulp.
  </behavior>
  <action>
    Write tests/test_symmetry_assume_verify.py. Build the invalid-hand-constraint C5 model inline with cp_model (per RESEARCH code example) to produce the fabricated had_2=2; wrap the fabricated value in an ExactOutcome (problem="had2", status PROVED_OPTIMAL, value=2, bound=2, family=<the SB-on 2-set family>). Drive assume_and_verify with a real no-SB rerun callable. For the on/off differential use the Plan-01 symmetry_level kwarg. Keep exact_value() reads outside assert truth-expressions. At RED, alpha2.solvers.symmetry is absent -> ModuleNotFoundError.
  </action>
  <verify>
    <automated>uv run --frozen --extra dev pytest tests/test_symmetry_assume_verify.py -q 2>&1 | tail -6  # RED: symmetry module absent</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_symmetry_assume_verify.py exists with the C5 fabricated-2 construction, the assume_and_verify->3 restore, the SBContaminationError leg, and the on/off differential
    - Running it fails RED with ModuleNotFoundError for alpha2.solvers.symmetry
    - The on/off differential uses solve_had2(..., symmetry_level=...)
  </acceptance_criteria>
  <done>The disaster and the discipline are pinned as tests before the wrapper exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — solvers/symmetry.py assume-and-verify discipline</name>
  <read_first>
    - src/alpha2/solvers/result.py (Status/exact_value; the raises-only, trust-boundary docstring stance to mirror)
    - src/alpha2/solvers/backend.py (stdlib-only, raises-only discipline)
    - tests/test_symmetry_assume_verify.py (the contract just written)
    - .planning/phases/05-cp-sat-differential-gate-had/05-PATTERNS.md (symmetry.py section: assume-and-verify rerun-without-SB is new discipline; wrapper/guard structure borrows from cbc.solve_had2)
  </read_first>
  <action>
    Create src/alpha2/solvers/symmetry.py, stdlib-only (import ONLY `from alpha2.solvers.result import Status`; NO pulp/ortools/pynauty):
    - class SBContaminationError(Exception) — an SB-on run produced a < chi outcome that was not re-verified without SB.
    - def is_impossibility_flavored(outcome, chi) -> bool: True iff outcome.status is PROVED_OPTIMAL and outcome.exact_value() < chi (the < chi conclusion that SB may never own). Keep the exact_value() call guarded (only when PROVED_OPTIMAL).
    - def assume_and_verify(sb_outcome, rerun_no_sb, *, chi): if is_impossibility_flavored(sb_outcome, chi): if rerun_no_sb is None: raise SBContaminationError(...); return rerun_no_sb(); else return sb_outcome. Docstring: SB accelerates existence only; any < chi conclusion is rerun without SB before recording/differential.
    - (Optional convenience) def solve_had2_sound_sb(backend, adj, n, *, mode, chi, symmetry_level, params=None): call backend.solve_had2(..., symmetry_level=symmetry_level) then pass through assume_and_verify with rerun_no_sb=lambda: backend.solve_had2(..., <no symmetry_level>). Document that symmetry_level is the sound path (objective-preserving); hand orbit SB (pynauty) is deferred.
    Every guard raise-based (no assert).
  </action>
  <verify>
    <automated>uv run --frozen --extra dev pytest tests/test_symmetry_assume_verify.py -q</automated>
  </verify>
  <acceptance_criteria>
    - src/alpha2/solvers/symmetry.py contains SBContaminationError and assume_and_verify; `grep -E "import (pulp|ortools|pynauty)" src/alpha2/solvers/symmetry.py` returns nothing
    - `grep -c "assert " src/alpha2/solvers/symmetry.py` == 0
    - pytest tests/test_symmetry_assume_verify.py exits 0: C5 disaster restored to 3, SBContaminationError on unguarded < chi, on/off differential equal
  </acceptance_criteria>
  <done>Symmetry breaking can accelerate existence but can NEVER own an impossibility conclusion; the C5 disaster is a passing regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SB-on solve -> recorded/differential outcome | A symmetry constraint satisfied by no optimum fabricates a sub-chi counterexample (the C5 disaster); SB may never own a < chi conclusion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-17 | Tampering (fabricated impossibility via SB) | assume_and_verify | mitigate | Any < chi SB-on outcome is rerun WITHOUT SB before it is returned/recorded; unguarded surfacing raises SBContaminationError; C5 disaster is a passing regression |
| T-05-18 | Tampering (unsound hand SB on trivial-Aut H) | sound-path helper | mitigate | Sound path uses CP-SAT symmetry_level (objective-preserving) only; hand orbit SB from a computed Aut deferred with pynauty; on/off differential asserts had_2(SB)==had_2(no-SB) |
| T-05-SC | Tampering (supply chain) | pynauty | accept | pynauty NOT installed (deferred); symmetry.py imports no solver/nauty library; sound path needs no new dependency |
</threat_model>

<verification>
- `uv run --frozen --extra dev pytest tests/test_symmetry_assume_verify.py -q` green.
- stdlib-only: `grep -E "pulp|ortools|pynauty" src/alpha2/solvers/symmetry.py` empty.
- Full fast suite green: `uv run --frozen --extra dev pytest -q -m "not slow"`.
</verification>

<success_criteria>
Assume-and-verify is enforced: the C5 "WLOG vertex unused" disaster is a passing regression (SB->2, discipline->3), an SB-on < chi outcome can never surface without a no-SB rerun (SBContaminationError), and had_2(sound SB) == had_2(no SB) on a small vertex-transitive battery — with pynauty deferred and no EXACT-06 criterion descoped.
</success_criteria>

<output>
Create `.planning/phases/05-cp-sat-differential-gate-had/05-06-SUMMARY.md` when done.
</output>
