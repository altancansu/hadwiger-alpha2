---
phase: 05-cp-sat-differential-gate-had
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/alpha2/corpus/verifier.py
  - tests/test_verifier_size3_mutants.py
  - tests/test_verifier_dash_O.py
autonomous: true
requirements: [EXACT-05]
must_haves:
  truths:
    - "verify_model_record accepts a branch set of size 3 ONLY when >=2 of its 3 internal pairs are G-edges (connected in G); a disconnected triple (0 or 1 G-edges) raises VerificationError"
    - "The size gate is exactly {1,2,3}: a size-4 set still raises; size-1 and size-2 paths are byte-unchanged (size-2 pair must be a G-edge; C(k,2) cross-adjacency unchanged and size-agnostic)"
    - "Every existing size-<=2 corpus record still verifies unchanged (full R1 corpus + Phase-2 mutant suite stay green after the widening)"
    - "The widened trust root stays stdlib-only and raises-only: a known-bad size-3 record is still rejected under `python -O` (the -O canary is extended over the size-3 path)"
  artifacts:
    - path: "src/alpha2/corpus/verifier.py"
      provides: "verify_model_record size gate {1,2,3} + explicit size-3 >=2-G-edges connectivity check; size-<=2 logic and _is_conflict unchanged"
      contains: "not in (1, 2, 3)"
    - path: "tests/test_verifier_size3_mutants.py"
      provides: "Accepts a connected triple; rejects disconnected triple / missing cross-adjacency / size-4 / aliased vertex; all size-<=2 records still verify"
      contains: "VerificationError"
    - path: "tests/test_verifier_dash_O.py"
      provides: "Existing verifier -O canary EXTENDED with a bad size-3 record that must still raise under -O"
      contains: "model_branch_sets"
  key_links:
    - from: "tests/test_verifier_size3_mutants.py"
      to: "alpha2.corpus.verifier.verify_model_record"
      via: "adversarial size-3 mutant records"
      pattern: "verify_model_record"
---

<objective>
Widen the program's TRUST ROOT (`corpus/verifier.verify_model_record`) from the
size-{1,2} gate to {1,2,3} with an explicit size-3 connectivity check (>=2 of the
3 internal pairs are G-edges), so the trust root can arbitrate the had_3 escalation
models Plan 05 produces. This is the single deliberate, adversarially-guarded change
to the frozen verifier this phase.

Purpose: EXACT-05 (trust-root half). The verifier is the sole conferrer of truth;
widening it is delicate (RESEARCH Pitfall 6). The discipline: keep every size-<=2
path byte-identical, add ONLY the size-3 branch, re-prove fail-closed with new
size-3 mutants + the full existing mutant/R1 suites + an extended -O canary.
Output: extended verifier.py + a size-3 mutant suite + an extended -O canary.

Wave-1 slice, independent of the CP-SAT backend: it needs only the frozen verifier.
It is guard 2 of the 3 size-3 guards (enumeration checksum=Plan 02, differential=Plan 05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-cp-sat-differential-gate-had/05-RESEARCH.md
@.planning/phases/05-cp-sat-differential-gate-had/05-PATTERNS.md

<interfaces>
From src/alpha2/corpus/verifier.py (the file being extended — CURRENT size gate at lines 102-115):
  _is_conflict(A, B, adj)  # size-AGNOSTIC: true iff every cross pair is an H-edge (non-adjacent in G). NO change needed.
  _build_adj(H_edges, n)   # rebuilds adjacency from STORED H_edges; range/canonical/dup guarded.
  verify_model_record(rec) # size gate `if len(S) not in (1,2): raise`; size-2 pair-is-G-edge check `if b in adj[a]: raise`;
                           # disjointness `used`-set; C(k,2) cross-adjacency `for i<j: if _is_conflict(...): raise`; returns k.
  verify_certificate(rec)  # verify_model_record + verify_chi_witness; the single trust entry point.

From tests/test_verifier_mutants.py: good_record() builds a size-<=2 KNOWN-GOOD record (n=31 seed 1, k=16); the
  Phase-2 mutant suite (overlapping sets, H-edge pair, missing cross-adjacency, truncated family, wrong chi) must stay green.

Graph fact (exact for 3 vertices): connected-in-G triple <=> >=2 internal G-edges <=> <=1 internal H-edge.
</interfaces>

Open-Q resolution (RESEARCH Open-Q1 / A6, recorded for audit): NO schema-version bump and NO new `had_3` invariant
field is added this phase. schema.build_record's invariants dict is closed and derives `had_2 = len(model_branch_sets)`;
in Phase 5 the size-3 escalation is proven at the TRUST-ROOT level (verify_model_record accepts size-3 families) and on
SYNTHETIC instances that are never corpus-stored (296/296 are killed at had_2; no real had_2<chi event occurs). A distinct
`had_3`/`had_le3` corpus fact is deferred to the first real consumer that stores an escalation certificate (Phase 6/11) —
this defers only an unused storage-field naming, not any EXACT-05 success-criterion behavior.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — size-3 mutant suite + accept-a-connected-triple + -O canary extension</name>
  <read_first>
    - tests/test_verifier_mutants.py (good_record() builder + the five-mutant pattern to mirror; embedded-record discipline, no schema import)
    - src/alpha2/corpus/verifier.py (current size gate + size-2 connectivity + cross-adjacency loop)
    - tests/test_verifier_dash_O.py (the subprocess -O canary shape: real `if __debug__: sys.exit(3)` branch, embedded bad record, exit codes 0/2/3)
    - .planning/phases/05-cp-sat-differential-gate-had/05-PATTERNS.md (verifier.py extension section: exact size-3 check `g_edges = (b not in adj[a])+(c not in adj[a])+(c not in adj[b]); if g_edges < 2: raise`)
  </read_first>
  <behavior>
    - test_accepts_connected_triple: a KNOWN-GOOD size-3 record (small H where a triple is connected in G, plus the C(k,2) cross-adjacency satisfied and chi<=k, with a valid H_edges_sha256 + Tutte-Berge fields if routed through verify_certificate; use verify_model_record for the pure size-3 leg) verifies and returns k.
    - test_rejects_disconnected_triple: a triple with only 1 G-edge (2 internal H-edges) raises VerificationError.
    - test_rejects_triple_zero_g_edges: a fully-H triple (a path in H) raises.
    - test_rejects_missing_cross_adjacency: a size-3 set non-adjacent in G to another set raises (via _is_conflict, unchanged).
    - test_rejects_size4_set: a size-4 branch set still raises (gate is exactly {1,2,3}).
    - test_rejects_aliased_vertex: a triple reusing a vertex from another set raises (disjointness).
    - test_all_existing_size2_records_unchanged: good_record() (k=16) still returns 16; import + re-run of the Phase-2 mutant assertions passes (reference them or re-embed).
    - test_dash_O extension: the subprocess script also builds a bad size-3 record (disconnected triple) and asserts verify_model_record still raises under -O (exit 0), never rubber-stamps (exit 2).
  </behavior>
  <action>
    Write tests/test_verifier_size3_mutants.py mirroring test_verifier_mutants.py's good_record/mutant idiom, but with size-3 records (embed a small hand-built H with a valid canonical sha256 computed in-file, exactly as test_verifier_dash_O embeds `[[0,1]]`). Then EXTEND tests/test_verifier_dash_O.py: add a second embedded bad-record subprocess check (or a parametrized SCRIPT) whose model_branch_sets contains a disconnected triple, asserting the same exit-code contract (0=raised, 2=rubber-stamped, 3=not under -O). At RED the new size-3 acceptance test fails because the verifier still rejects size 3.
  </action>
  <verify>
    <automated>uv run --frozen --extra dev pytest tests/test_verifier_size3_mutants.py -q 2>&1 | tail -6  # RED: size-3 not yet accepted</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_verifier_size3_mutants.py exists with a connected-triple acceptance test AND disconnected/size-4/aliased/missing-cross-adjacency rejection tests
    - tests/test_verifier_dash_O.py now embeds a bad size-3 record subprocess check
    - Running the size-3 acceptance test fails RED (current verifier raises "size 3 not in (1,2)")
  </acceptance_criteria>
  <done>The size-3 trust-root contract (accept connected triples, reject everything else, fail closed under -O) is pinned adversarially before the widening.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — widen verify_model_record to {1,2,3} with the size-3 connectivity check</name>
  <read_first>
    - src/alpha2/corpus/verifier.py (lines 71-122 — the exact function; keep size-<=2 and _is_conflict byte-identical)
    - tests/test_verifier_size3_mutants.py + tests/test_verifier_dash_O.py (the contract just written)
    - .planning/phases/05-cp-sat-differential-gate-had/05-PATTERNS.md (verifier.py extension: the minimal, adversarial widening)
  </read_first>
  <action>
    Edit src/alpha2/corpus/verifier.verify_model_record ONLY:
    - Size gate: change `if len(S) not in (1, 2): raise ...` to `if len(S) not in (1, 2, 3): raise VerificationError(f"branch set size {len(S)} not in (1,2,3): {S!r}")`.
    - Keep the size-2 branch (`if len(S) == 2: a,b=S; if b in adj[a]: raise ... H-edge`) UNCHANGED.
    - ADD a size-3 branch alongside it: `if len(S) == 3: a,b,c = S; g_edges = (b not in adj[a]) + (c not in adj[a]) + (c not in adj[b]); if g_edges < 2: raise VerificationError(f"triple {S!r} disconnected in G ({g_edges} G-edges < 2)")`.
    - Do NOT touch disjointness, the C(k,2) cross-adjacency loop, or _is_conflict (already size-agnostic). Stay stdlib-only and raises-only (no assert). Update the function docstring's rule 4/5 to note size in {1,2,3} and the size-3 >=2-G-edges connectivity requirement.
    No change to schema.py (see Open-Q resolution above).
  </action>
  <verify>
    <automated>uv run --frozen --extra dev pytest tests/test_verifier_size3_mutants.py tests/test_verifier_mutants.py tests/test_verifier_dash_O.py tests/test_corpus_r1.py -q && uv run --frozen --extra dev python -O -m pytest tests/test_verifier_dash_O.py -q</automated>
  </verify>
  <acceptance_criteria>
    - src/alpha2/corpus/verifier.py contains `not in (1, 2, 3)` and the `g_edges < 2` size-3 check
    - `grep -c "assert " src/alpha2/corpus/verifier.py` returns 0 (raises-only preserved)
    - pytest over test_verifier_size3_mutants + test_verifier_mutants (Phase-2 suite) + test_corpus_r1 (full corpus R1) + test_verifier_dash_O all exit 0
    - `python -O -m pytest tests/test_verifier_dash_O.py` exits 0 (fail-closed on the size-3 bad record under -O)
  </acceptance_criteria>
  <done>The trust root arbitrates size-3 escalation models, size-<=2 records verify byte-unchanged, and the widened path fails closed under -O.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stored certificate -> verify_model_record | The trust root is the SOLE truth-conferrer; a widening that admits a bad triple silently corrupts every downstream claim |
| assert vs raise under `python -O` | Under -O asserts are stripped; an assert-based check would rubber-stamp |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Elevation (trust-root bypass) | verify_model_record size-3 branch | mitigate | Explicit >=2-G-edges connectivity check; size gate exactly {1,2,3}; size-3 mutant suite (disconnected/size-4/aliased/missing-cross-adj) each raises VerificationError |
| T-05-08 | Tampering (regression of size-<=2) | verify_model_record | mitigate | size-<=2 and _is_conflict kept byte-identical; full Phase-2 mutant suite + R1 full corpus re-run green |
| T-05-09 | Tampering (assert-stripped rubber stamp) | widened path under -O | mitigate | raises-only; -O canary extended with a bad size-3 record, subprocess exit-code contract |
| T-05-SC | Tampering (supply chain) | — | accept | No package installed; verifier stays stdlib-only |
</threat_model>

<verification>
- `uv run --frozen --extra dev pytest tests/test_verifier_size3_mutants.py tests/test_verifier_mutants.py tests/test_verifier_dash_O.py tests/test_corpus_r1.py -q` green.
- `uv run --frozen --extra dev python -O -m pytest tests/test_verifier_dash_O.py -q` green (fail-closed under -O).
- `grep -c "assert " src/alpha2/corpus/verifier.py` == 0.
</verification>

<success_criteria>
The trust root now accepts size-3 branch sets iff connected in G (>=2 internal G-edges), rejects every malformed triple, leaves all size-<=2 records byte-unchanged, and still fails closed under `python -O`.
</success_criteria>

<output>
Create `.planning/phases/05-cp-sat-differential-gate-had/05-03-SUMMARY.md` when done.
</output>
