---
phase: 05-cp-sat-differential-gate-had
plan: 07
type: execute
wave: 3
depends_on: ["05-01", "05-04"]
files_modified:
  - tests/test_differential_panel.py
autonomous: true
requirements: [EXACT-04, EXACT-03]
must_haves:
  truths:
    - "On the shared CI agreement panel (C5 had_2=3, empty-H had_2=n, >=2 tiny TFP instances) both CBC and CP-SAT return PROVED_OPTIMAL with EQUAL had_2, and both extracted families independently pass verify_certificate"
    - "seed-137 (n=31, m=177, chi=16) solves had_2=17 PROVED_OPTIMAL on BOTH backends (deterministic CP-SAT: num_workers=1 + pinned seed); differential_verdict returns AGREED_KILL (17 >= 16) and both 17-set families verify — SC1 satisfied end-to-end"
    - "The panel routes every agreement through differential_verdict (the SOLE licenser) — no backend value is trusted without its pair; the seed-137 leg carries @pytest.mark.slow and joins the existing release-gate -m slow selector"
    - "The CP-SAT seed-137 optimize wall time is measured once and recorded, pinning the slow-tier budget (RESEARCH Assumption A2 / Wave-0 measurement)"
  artifacts:
    - path: "tests/test_differential_panel.py"
      provides: "Fast dual-backend agreement panel (tiny instances) + slow seed-137 had_2=17 dual-backend PROVED_OPTIMAL both-families-verified panel through differential_verdict"
      contains: "differential_verdict"
  key_links:
    - from: "tests/test_differential_panel.py"
      to: "get_backend('cbc') + get_backend('cpsat')"
      via: "same instance solved by both engines (independent encodings)"
      pattern: "get_backend"
    - from: "tests/test_differential_panel.py"
      to: "alpha2.solvers.differential.differential_verdict"
      via: "agreement licensed only through the gate"
      pattern: "differential_verdict"
---

<objective>
Prove Phase-5 Success Criterion 1 end-to-end: the CP-SAT backend AGREES with CBC
on every shared CI-panel instance — both PROVED_OPTIMAL, equal had_2, both families
independently verified — INCLUDING the seed-137 = 17 dual-backend optimality proof
in deterministic mode. This is the capstone panel that closes EXACT-04's agreement
claim and demonstrates EXACT-03's CP-SAT engine at the 296-lineage anchor.

Purpose: EXACT-04 (agreement panel) + EXACT-03 (CP-SAT proves the seed-137 anchor).
Every agreement is licensed through `differential_verdict` — the panel never trusts
a single backend's value. The slow seed-137 leg joins the release gate automatically
via `-m slow`.
Output: `test_differential_panel.py` (fast tier + slow seed-137 dual-backend leg) +
the recorded CP-SAT seed-137 timing.

Open-Q resolution (RESEARCH Open-Q3, recorded for audit): the committed seed-137
corpus record is NOT upgraded to the 17-set family here. The dual-backend proof lives
IN MEMORY (like the Phase-4 seed-137 regression) — the frozen corpus / repro / R1 /
manifest stay byte-untouched. The corpus upgrade remains a deferred, deliberate freeze
amendment (first consumer: the Phase-11 falsification harness); no EXACT-03..06
criterion requires it.

Wave-0 measurement (RESEARCH Assumption A2): the CP-SAT seed-137 optimize wall time
is measured in this plan and recorded in the SUMMARY, pinning the slow-tier budget.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-cp-sat-differential-gate-had/05-RESEARCH.md
@.planning/phases/05-cp-sat-differential-gate-had/05-PATTERNS.md

<interfaces>
From tests/test_seed137_regression.py (the exact slow-tier analog to mirror): @pytest.mark.slow; regenerate (n=31, seed=137)
  via triangle_free_process(31, random.Random(137)); assert m==177, nu==15, chi==16 BEFORE any solve is trusted (R2 discipline);
  CBC solve_had2(..., mode="optimize", params=SolveParams(time_limit_s=600)) -> PROVED_OPTIMAL, exact_value()==17, bound==17,
  len(family)==17; build_record + verify_certificate; metamorphic anchor (17 > stored 16-set family).
From src/alpha2/solvers/backend.py: get_backend("cbc"), get_backend("cpsat").
From src/alpha2/solvers/differential.py (Plan 04): differential_verdict(a, b, chi) -> "AGREED_KILL"/"SHC_CANDIDATE"/"INSUFFICIENT"; raises CriticalDisagreement.
From src/alpha2/solvers/result.py: SolveParams(time_limit_s=...); Status.PROVED_OPTIMAL.
From src/alpha2/corpus/schema.py: build_record, make_backends (routes "cpsat" -> ortools stamp), provenance_seed.
Panel instances: C5 (had_2=3, chi=3 -> AGREED_KILL since 3>=3), empty-H n=5 (had_2=5), tiny TFP n in {6,7,8} seeds 1/2, seed-137 (had_2=17, chi=16).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — dual-backend agreement panel (fast) + seed-137 slow leg through the gate</name>
  <read_first>
    - tests/test_seed137_regression.py (the slow-tier seed-137 optimize proof + R2 regeneration gates + metamorphic anchor to generalize across the pair)
    - tests/test_cbc_backend.py (the tiny-instance E2E + regeneration-gate idioms)
    - src/alpha2/solvers/differential.py (differential_verdict signature + CriticalDisagreement)
    - .planning/phases/05-cp-sat-differential-gate-had/05-RESEARCH.md (Pattern 2 CI agreement panel; Assumption A2 timing)
  </read_first>
  <behavior>
    - test_agreement_panel_fast: for each tiny instance (C5, empty-H n=5, TFP n=6/7/8 s=1/2), solve had_2 optimize on BOTH backends; assert both PROVED_OPTIMAL, equal exact_value(); differential_verdict(cbc, cpsat, chi) returns "AGREED_KILL" (all have had_2 >= chi); route BOTH families through verify_certificate (each returns its k). Regeneration gates on the TFP instances run before any solve (R2 discipline).
    - test_seed137_dual_backend (@pytest.mark.slow): regenerate seed-137, assert m==177/nu==15/chi==16; solve had_2 optimize on BOTH backends (CP-SAT with num_workers=1 + pinned seed; SolveParams time budget sized from the measured wall time with headroom); assert both PROVED_OPTIMAL, both exact_value()==17, both bound==17; differential_verdict(cbc, cpsat, 16) == "AGREED_KILL"; both 17-set families pass verify_certificate; metamorphic anchor: 17 > stored 16-set corpus family (read-only load), no corpus write.
    - test_backends_disagreement_would_halt: a negative control constructing two unequal PROVED_OPTIMAL outcomes for the same panel instance raises CriticalDisagreement (proves the panel would quarantine, not skip).
  </behavior>
  <action>
    Write tests/test_differential_panel.py mirroring test_seed137_regression.py's slow-tier + regeneration-gate discipline, generalized to the CBC+CP-SAT pair and routed through differential_verdict. Embedded-literal discipline (in-file adjacencies + pinned invariants; no cross-test imports). All exact_value()/verify_certificate calls outside assert truth-expressions. Keep the seed-137 leg @pytest.mark.slow. At RED, differential.py / cpsat.py may be present (Plan 01/04 done) so the fast panel should actually PASS once those exist — RED here means: written before this plan's own run confirms green; if any backend disagrees or a family fails to verify, the test fails loudly.
  </action>
  <verify>
    <automated>uv run --frozen --extra dev pytest tests/test_differential_panel.py -q -m "not slow"</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_differential_panel.py exists; the fast tier passes (dual-backend agreement on C5/empty/tiny TFP through differential_verdict, both families verified)
    - The seed-137 leg carries @pytest.mark.slow and calls differential_verdict(..., 16) expecting "AGREED_KILL"
    - A negative-control test asserts CriticalDisagreement on constructed unequal proven optima
  </acceptance_criteria>
  <done>The fast dual-backend agreement panel is green; the seed-137 slow leg is wired through the gate, ready to run.</done>
</task>

<task type="auto">
  <name>Task 2: Run the seed-137 dual-backend proof, record timing, pin the slow budget</name>
  <read_first>
    - tests/test_differential_panel.py (the slow leg just written)
    - tests/test_seed137_regression.py (the ~149 s CBC single-thread reference timing to compare against)
    - .planning/phases/05-cp-sat-differential-gate-had/05-RESEARCH.md (Assumption A2: CP-SAT seed-137 timing NOT yet measured; feeds the slow-tier budget)
  </read_first>
  <action>
    Execute the slow leg once: `uv run --frozen --extra dev pytest tests/test_differential_panel.py -m slow -x`. Measure the CP-SAT seed-137 optimize wall time (read from the ExactOutcome.wall_time_s or pytest --durations). If CP-SAT is materially slower than the ~149 s CBC reference, raise the SolveParams time budget on the CP-SAT leg with headroom and re-run until green. Record the measured CP-SAT wall time and the final pinned budget in the SUMMARY (Assumption A2 resolved). Confirm the frozen corpus is byte-untouched (no data/ write occurred: the record lived in memory).
  </action>
  <verify>
    <automated>uv run --frozen --extra dev pytest tests/test_differential_panel.py -m slow -x --durations=5</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_differential_panel.py -m slow` exits 0: both backends PROVED_OPTIMAL had_2=17, differential_verdict=="AGREED_KILL", both families verify_certificate-clean
    - The measured CP-SAT seed-137 wall time and the pinned slow-tier budget are recorded in 05-07-SUMMARY.md
    - `git status --porcelain data/` shows no modification to the frozen corpus/manifest (in-memory record only)
  </acceptance_criteria>
  <done>SC1 is proven end-to-end: CP-SAT agrees with CBC on seed-137=17 in deterministic mode, both families verified, timing pinned, corpus byte-untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Two backend proofs -> agreement claim | An agreement asserted without routing through the gate would let a single backend's value stand unchecked |
| In-memory dual-backend record -> frozen corpus | The seed-137 proof must not mutate the frozen corpus/manifest |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-19 | Tampering (unverified agreement) | agreement panel | mitigate | Every agreement licensed through differential_verdict; both families independently pass verify_certificate; no backend value trusted alone |
| T-05-20 | Tampering (nondeterministic recorded seed-137 claim) | CP-SAT seed-137 leg | mitigate | CP-SAT run with num_workers=1 + pinned random_seed; value/status/verifiability asserted, never the family bits (Assumption A1) |
| T-05-21 | Tampering (drifted generator self-certifies) | seed-137 regeneration | mitigate | m==177/nu==15/chi==16 gates run BEFORE any solve is trusted (R2 discipline) |
| T-05-22 | Integrity (frozen corpus mutated) | in-memory record | mitigate | Record lives/dies in memory; `git status data/` clean; corpus upgrade explicitly deferred |
| T-05-SC | Tampering (supply chain) | — | accept | No package installed; ortools/pulp pinned artifacts |
</threat_model>

<verification>
- Fast tier: `uv run --frozen --extra dev pytest tests/test_differential_panel.py -q -m "not slow"` green.
- Slow tier: `uv run --frozen --extra dev pytest tests/test_differential_panel.py -m slow -x` green (seed-137 dual-backend).
- Frozen corpus untouched: `git status --porcelain data/` empty.
- Full release-gate replay: `uv run --frozen --extra dev pytest -q` (including -m slow) green.
</verification>

<success_criteria>
CP-SAT agrees with CBC on every shared CI-panel instance (both PROVED_OPTIMAL, equal had_2, both families verified) including seed-137=17 in deterministic mode; every agreement is licensed through the differential gate; the CP-SAT seed-137 timing is measured and the slow budget pinned; the frozen corpus is byte-untouched.
</success_criteria>

<output>
Create `.planning/phases/05-cp-sat-differential-gate-had/05-07-SUMMARY.md` when done (record the measured CP-SAT seed-137 wall time + pinned slow budget).
</output>
