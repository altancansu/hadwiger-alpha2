---
phase: 05-cp-sat-differential-gate-had
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/alpha2/solvers/problems/had3.py
  - tests/test_had3_problem.py
autonomous: true
requirements: [EXACT-05]
must_haves:
  truths:
    - "The triple index contains exactly the vertex triples {a,b,c} that induce a CONNECTED subgraph of G — i.e. at most 1 of the 3 internal pairs is an H-edge (>=2 are G-edges) — enforced BY the enumeration, never proposed otherwise"
    - "Triple-vs-set conflicts are enumerated from the common H-neighborhood (a triple T conflicts with S iff every vertex of S lies in the intersection of N_H over T), not from an all-pairs scan"
    - "build_had3_problem passes a raise-based structural checksum recomputed independently from H's structure and raises on any mismatch (mirrors had2's ChecksumError gate) — size-3 conflicts are NOT a clean local substructure of H, so the independent checksum is the first of three guards"
    - "A synthetic size-3-forced instance (had_2 < chi, but a size-3 family reaches chi) yields a triple index that CONTAINS the forcing triples; the module is backend-neutral and imports no solver library"
  artifacts:
    - path: "src/alpha2/solvers/problems/had3.py"
      provides: "Had3Problem frozen dataclass, enumerate_had3 (triple index <=1 H-edge + common-H-neighborhood conflicts), build_had3_problem with independent checksum gate"
      contains: "build_had3_problem"
      min_lines: 60
    - path: "tests/test_had3_problem.py"
      provides: "Triple-enumeration correctness (connectivity by index), checksum literals on synthetic instances, mutation->raises, backend-neutrality"
      contains: "ChecksumError"
  key_links:
    - from: "src/alpha2/solvers/problems/had3.py"
      to: "alpha2.generators.tfp.is_triangle_free"
      via: "up-front triangle-free re-check (mirrors had2)"
      pattern: "is_triangle_free"
---

<objective>
Build the backend-neutral size-3 branch-set model data (`solvers/problems/had3.py`)
— the ONE genuinely-new piece of combinatorics in Phase 5. It enumerates the
seagull-tier triple index (triples with <=1 internal H-edge = >=2 internal G-edges
= connected in G) and the size-3 conflict sets pruned by empty common H-neighborhood,
gated by an independent structural checksum exactly as `had2.py` guards its
conflict classes.

Purpose: EXACT-05 (model half). RESEARCH Pitfall 3 warns size-3 conflicts are NOT
a clean local substructure of H, so this is where encoding bugs live — the checksum
here + the extended verifier (Plan 03) + the CBC/CP-SAT had_3 differential (Plan 05)
are the three independent guards. This plan delivers the first.
Output: `problems/had3.py` (stdlib-only, no solver import) + its test file green.

This is a Wave-1 slice independent of the CP-SAT backend: the module is pure model
data, consumed later by both backends in Plan 05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-cp-sat-differential-gate-had/05-RESEARCH.md
@.planning/phases/05-cp-sat-differential-gate-had/05-PATTERNS.md

<interfaces>
From src/alpha2/solvers/problems/had2.py (the EXACT structural analog — copy the pattern):
  class ChecksumError(Exception)
  @dataclass(frozen=True) Had2Problem: n, Gedges, ss, sp, pp
  def enumerate_had2(adj, n) -> (Gedges, ss, sp, pp)
  def build_had2_problem(adj, n) -> Had2Problem  # triangle-free re-check + independent count checksum, raises

From src/alpha2/generators/tfp.py: def is_triangle_free(adj, n) -> bool

Graph fact (RESEARCH Assumption A3, exact for 3 vertices): 3 vertices induce a
connected subgraph iff >= 2 of the 3 internal pairs are edges. In G = complement(H):
a triple is connected in G iff >= 2 internal pairs are G-edges iff <= 1 internal
pair is an H-edge.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — triple-enumeration + checksum semantics on synthetic size-3-forced instances</name>
  <read_first>
    - tests/test_had2_problem.py (the analog: set-equality vs naive reference, checksum literals, mutation->ChecksumError, triangle-refusal, brute-force differential idiom)
    - src/alpha2/solvers/problems/had2.py (Gedges/ss/sp/pp conventions + build gate to mirror)
    - .planning/phases/05-cp-sat-differential-gate-had/05-RESEARCH.md (Pattern 3: triple index rule, common-H-neighborhood conflict pruning)
  </read_first>
  <behavior>
    - test_triple_index_is_connectivity_by_index: a test-local naive reference enumerates all C(n,3) triples and keeps those with >=2 internal G-edges; enumerate_had3's triple index is SET-equal to it on >=3 synthetic small H (including a path/cherry-rich H and a size-3-forced H).
    - test_triples_have_at_most_one_H_edge: every triple in the index has <=1 internal H-edge (assert directly against adj).
    - test_conflict_pruning_common_neighborhood: for a hand-built H where a specific triple has a non-empty common H-neighborhood, the enumerated conflicts include exactly the expected (triple, other-set) pairs and exclude triples whose common H-neighborhood is empty.
    - test_checksum_literals: on >=2 fixed synthetic instances the (len(triples), len(conflicts)) tuple equals in-file pinned literals.
    - test_mutation_raises_checksum: dropping one enumerated element makes build_had3_problem raise ChecksumError (monkeypatch enumerate_had3 seam, mirroring had2's test).
    - test_triangle_containing_h_refused / backend-neutrality: build raises on a triangle H; `import sys; assert no ortools/pulp module pulled in by importing had3`.
    - test_size3_forced_instance_shape: define a small H where alpha/had_2<chi is plausible and a size-3 family of size chi exists; assert the forcing triples ARE present in the index (the model can represent the escalation).
  </behavior>
  <action>
    Write tests/test_had3_problem.py. Embed a small test-local naive reference (all-triples filtered by >=2 G-edges, and a direct common-H-neighborhood conflict scan) — INDEPENDENT of the module under test, exactly like test_had2_problem's _naive_* loops. Include >=1 explicit synthetic size-3-forced instance (small H, hand-checked). Pin checksum literals in-file. At RED the import of alpha2.solvers.problems.had3 fails (module absent) — intended. No src/ edit here.
  </action>
  <verify>
    <automated>uv run --frozen --extra dev pytest tests/test_had3_problem.py -q 2>&1 | tail -5  # RED: had3 module absent</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_had3_problem.py exists with a test-local naive triple reference and pinned checksum literals
    - Running it fails RED with ModuleNotFoundError for alpha2.solvers.problems.had3
    - At least one test targets a synthetic size-3-forced instance
  </acceptance_criteria>
  <done>The size-3 model contract (connectivity-by-index, common-neighborhood conflicts, checksum) is pinned before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — solvers/problems/had3.py (triple index + conflicts + checksum gate)</name>
  <read_first>
    - src/alpha2/solvers/problems/had2.py (copy WHOLE pattern: frozen dataclass, enumerate_*, build_* with independent checksum recomputed from H structure; ChecksumError)
    - tests/test_had3_problem.py (the contract just written)
    - .planning/phases/05-cp-sat-differential-gate-had/05-PATTERNS.md (had3.py section: triple comprehension `((b in adj[a])+(c in adj[a])+(c in adj[b])) <= 1`, common-H-neighborhood conflict enumeration)
  </read_first>
  <action>
    Create src/alpha2/solvers/problems/had3.py, stdlib-only, importing NO solver library (reuse ChecksumError from had2 or define a size-3 analog; prefer importing the existing ChecksumError for consistency). Provide:
    - @dataclass(frozen=True) Had3Problem(n:int, triples:list, conflicts:set) with a module docstring matching had2's discipline (backend-neutral; triple index IS the size-3 connectivity constraint).
    - enumerate_had3(adj, n): triples = [(a,b,c) for a<b<c if ((b in adj[a])+(c in adj[a])+(c in adj[b])) <= 1]; conflicts enumerated from common H-neighborhoods: for each triple T, W = intersection of adj over T's vertices; any candidate set S drawn from W (singletons and G-edge pairs within W) whose every vertex is a cross H-neighbor forms a conflict — follow RESEARCH Pattern 3 (enumerate from W, not all pairs). Return (triples, conflicts) with a canonical, sorted, dedup'd representation (mirror had2's frozenset dedup so double-discovery cannot double-count).
    - build_had3_problem(adj, n): raise ValueError if not is_triangle_free(adj, n); enumerate; recompute the counts independently from H's structure (an expression over degrees / common-neighborhood sizes that does NOT reuse the enumeration's own lists) and raise ChecksumError on mismatch; return Had3Problem.
    Keep every guard raise-based (no assert). Sort all iterations for determinism (had2 precedent).
  </action>
  <verify>
    <automated>uv run --frozen --extra dev pytest tests/test_had3_problem.py -q</automated>
  </verify>
  <acceptance_criteria>
    - src/alpha2/solvers/problems/had3.py contains build_had3_problem and a triangle-free re-check
    - `grep -E "import (pulp|ortools)" src/alpha2/solvers/problems/had3.py` returns nothing (backend-neutral)
    - pytest tests/test_had3_problem.py exits 0 (set-equality vs naive reference, checksum literals, mutation->ChecksumError, triangle refusal, size-3-forced shape)
  </acceptance_criteria>
  <done>The size-3 model data is enumerable, connectivity-correct by index, and checksum-guarded — ready for both backends in Plan 05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| H structure -> triple/conflict enumeration | A hand-derived size-3 conflict class is a fresh class of encoding bugs (Pitfall 3); a lost conflict inflates had_3 plausibly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Tampering (silent enumeration bug) | enumerate_had3 conflicts | mitigate | Independent structural checksum recomputed from H (not the enumeration's own output) raises ChecksumError on mismatch; set-equality test vs an independent naive reference; this is guard 1 of 3 (verifier=Plan 03, differential=Plan 05) |
| T-05-06 | Elevation (disconnected triple admitted) | triple index | mitigate | Connectivity enforced BY the index (<=1 internal H-edge); never a widened all-triples index; verifier independently re-checks in Plan 03 |
| T-05-SC | Tampering (supply chain) | — | accept | No package installed; module is stdlib-only |
</threat_model>

<verification>
- `uv run --frozen --extra dev pytest tests/test_had3_problem.py -q` green.
- Backend-neutrality: `grep -E "pulp|ortools" src/alpha2/solvers/problems/had3.py` empty.
- Existing suite unaffected: `uv run --frozen --extra dev pytest -q -m "not slow"` green.
</verification>

<success_criteria>
The seagull-tier size-3 model exists as backend-neutral data: triple index = connected-in-G triples (<=1 H-edge), conflicts pruned by common H-neighborhood, gated by an independent checksum — the enumeration guard for EXACT-05.
</success_criteria>

<output>
Create `.planning/phases/05-cp-sat-differential-gate-had/05-02-SUMMARY.md` when done.
</output>
