---
phase: 06-kill-battery-cli-gate-search-statuses
plan: 03
type: execute
wave: 3
depends_on: [06-02]
files_modified:
  - src/alpha2/search/heuristic.py
  - tests/test_profile_general.py
  - tests/test_heuristic_instrumentation.py
autonomous: true
requirements: [SRCH-01, SRCH-02]
user_setup: []

must_haves:
  truths:
    - "solve() iterates non-spanning profiles (p', s') with p'+s'=chi and 2p'+s'<=n, each with its own restart budget"
    - "The rewritten data structure can HOLD the seed-137 D.3 non-spanning state (9 pairs + 7 singletons = 25 vertices, 6 unused)"
    - "With a generous budget, profile-general solve FINDS a verified K16 model for seed-137 that the spanning profile misses"
    - "solve() never uses `assert` — it does not crash on pool instances with chi < n/2"
    - "Instrumentation (restarts-to-solution, initial-conflict count) is exposed; a miss returns sets=None and is NEVER a RESISTANT result"
    - "The determinism-sensitive `tuple(conf)[rng.randrange(len(conf))]` idiom is preserved byte-for-byte"
  artifacts:
    - path: "src/alpha2/search/heuristic.py"
      provides: "profile-general solve() with preserved local-search body + instrumentation return contract"
      contains: "2 * p"
      min_lines: 150
  key_links:
    - from: "src/alpha2/search/heuristic.py"
      to: "src/alpha2/battery/pipeline.py"
      via: "solve() signature preserved so the runbook keeps calling it unchanged"
      pattern: "def solve\\(adj, n, k, rng"
---

<objective>
Rewrite the profile-selection head of `search/heuristic.py:solve()` from the single hard-coded
SPANNING profile (`p = n-k; s = 2*k-n; assert ...`) to a PROFILE-GENERAL enumeration over
(p', s') with p' + s' = chi and 2p' + s' <= n (SRCH-01) — the exact seed-137 non-spanning fix — and
expose restarts-to-solution + initial-conflict instrumentation for P7 fitness (SRCH-02). The current
spanning-only head empirically MISSES seed-137 (found=False in 45s) because it cannot even hold the
D.3 optimum (9 pairs + 7 singletons, 6 unused).

This preserves the local-search body and the instrumentation return contract byte-for-byte; only the
profile head changes. Heuristic "not found" remains a fact about the searcher (sets=None) — the
pipeline treats it as an unconditional edge to exact, NEVER as RESISTANT.

Purpose: hardening after the SC1 slice — future seed-137-class instances must not be spuriously
un-found by the searcher.
Output: a profile-general `solve()` and two unit tests (hold-the-state + find-the-model + instrumentation).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-kill-battery-cli-gate-search-statuses/06-CONTEXT.md
@.planning/phases/06-kill-battery-cli-gate-search-statuses/06-RESEARCH.md
@.planning/phases/06-kill-battery-cli-gate-search-statuses/06-PATTERNS.md

<interfaces>
The head to REPLACE (src/alpha2/search/heuristic.py:99-101):
  def solve(adj, n, k, rng, time_budget=90.0):
      p = n - k; s = 2 * k - n
      assert p >= 0 and s >= 0 and 2 * p + s == n     # single SPANNING profile; MISSES seed-137

PRESERVE byte-for-byte (module is ruff-excluded in pyproject.toml — do NOT reformat / ruff --fix):
  - the determinism idiom: pr = tuple(conf)[rng.randrange(len(conf))]   (heuristic.py:115)
  - the local-search body: initial_state / full_conflicts / update_conflicts / cand_energy /
    assignments / valid_groups (heuristic.py:49-97, 102-155)
  - the return contract (heuristic.py:156-157): return sets, best_init, moves, restarts, elapsed
    and a miss returns (None, best_init, None, restarts, elapsed)
  - the solve() SIGNATURE: def solve(adj, n, k, rng, time_budget=...) — Plan 02's pipeline imports it

Profile enumeration (RESEARCH Pattern 2 / §4 step 3): p' + s' = chi, 2p' + s' <= n, i.e. s' from
  max(0, 2*chi - n) up to chi; iterate clique-heavy -> pair-heavy; allow unused vertices; per-profile
  restart budget; NEVER an assert (crashes on chi < n/2 pool instances live in P4/P6).

seed-137 regression fact: D.3 optimum = 9 pairs + 7 singletons = 25 vertices of 31, 6 unused.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0 profile-general + instrumentation tests (hold-the-state, find-the-model)</name>
  <read_first>
    - src/alpha2/search/heuristic.py:49-101 (initial_state + the spanning head — understand the profile shape being generalized)
    - tests/test_seed137_regression.py:76-105 (how seed-137 adj is regenerated; nu=15, chi=16, m=177)
    - .planning/phases/06-kill-battery-cli-gate-search-statuses/06-PATTERNS.md:238-263 (heuristic self-analog block; the D.3 hold-the-state regression)
  </read_first>
  <behavior>
    - test_profile_general: profile-general solve() with a generous budget FINDS a K16 model for seed-137; the returned family verifies (all cross pairs G-edges, 16 disjoint branch sets of size <=2)
    - hold-the-state: the data structure can represent 9 pairs + 7 singletons = 25 vertices with 6 unused (initial_state / profile machinery accepts a non-spanning profile)
    - test_heuristic_instrumentation: a solve returns (sets, best_init, moves, restarts, elapsed); best_init and restarts are ints; a forced miss (tiny budget) returns sets=None with restarts>=1
    - a pool-style instance with chi < n/2 does not raise (no assert crash)
  </behavior>
  <action>
    Write tests/test_profile_general.py: regenerate seed-137 (triangle_free_process(31, Random(137))),
    compute chi=16, call solve with a generous time_budget, assert a K16 model is found and that each
    branch set pair is a G-edge (non-H-edge) and sets are disjoint of size <=2 (16 sets). Include an
    explicit hold-the-state assertion that a non-spanning profile (e.g. s'=7, p'=9) can be represented.
    Add a chi<n/2 smoke case (small synthetic adj) asserting solve does not raise. Write
    tests/test_heuristic_instrumentation.py asserting the 5-tuple return contract and that a tiny
    budget yields sets=None (a miss) with restarts>=1 — and document (comment) that the pipeline maps
    None to the exact-solver edge, never RESISTANT (SRCH-02). These tests FAIL against the current
    spanning head (they exercise Task 2). Mark the seed-137 find test @pytest.mark.slow if its budget
    exceeds a few seconds.
  </action>
  <acceptance_criteria>
    - `pytest tests/test_heuristic_instrumentation.py -x -q` collects and runs; the 5-tuple + miss-returns-None assertions are present
    - test_profile_general contains an explicit seed-137 K16-find assertion and a non-spanning hold-the-state assertion
    - the chi<n/2 smoke case asserts solve does not raise
  </acceptance_criteria>
  <verify>
    <automated>./.venv/bin/python -m pytest tests/test_heuristic_instrumentation.py --collect-only -q</automated>
  </verify>
  <done>Two Wave-0 tests scaffolded: the seed-137 profile-general find + non-spanning hold-the-state, and the instrumentation/miss-contract test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite solve() profile head to profile-general (SRCH-01/02)</name>
  <read_first>
    - src/alpha2/search/heuristic.py:99-157 (the full solve() — replace ONLY the head; keep the body + return)
    - src/alpha2/search/heuristic.py:49-67 (initial_state(adj, n, p, s, rng) — reuse per profile)
    - .planning/reference/alpha2-program-source.md:652 (§4 step 3 — iterate pair/singleton profiles, seed clique-rich, allow unused vertices)
    - .planning/phases/06-kill-battery-cli-gate-search-statuses/06-RESEARCH.md:238-241 (Pattern 2 — exact (p',s') bounds; no assert)
    - pyproject.toml (confirm heuristic.py is ruff-excluded — do NOT reformat)
  </read_first>
  <behavior>
    - solve(adj, n, k, rng, time_budget) enumerates profiles (p', s') with p'+s'=k and 2p'+s'<=n (s' from max(0, 2k-n) to k), each restart drawing from a profile, allocating a per-profile share of the budget
    - seed-137 (n=31, k=16) with a generous budget returns a valid 16-set model (found)
    - the return contract is unchanged: (sets, best_init, moves, restarts, elapsed); miss -> (None, best_init, None, restarts, elapsed)
    - no `assert` remains in the module's control path; chi<n/2 does not crash
  </behavior>
  <action>
    Replace the spanning head (`p = n-k; s = 2*k-n; assert ...`) with a bounded profile enumeration:
    compute the profile list [(p', s') for s' in range(max(0, 2*k - n), k + 1) with p' = k - s' and
    2*p' + s' <= n], ordered clique-heavy (small p') to pair-heavy or vice-versa per RESEARCH; loop
    restarts across the profiles, each restart calling the EXISTING initial_state(adj, n, p', s', rng)
    and running the UNCHANGED local-search body. Keep best_init as the first restart's initial conflict
    count; accumulate restarts across all profiles; return the moment a profile clears all conflicts.
    On overall timeout return (None, best_init, None, restarts, elapsed). Remove the assert entirely
    (bounded enumeration replaces it). Do NOT touch initial_state/full_conflicts/update_conflicts/
    cand_energy/assignments/valid_groups, and do NOT reformat the `tuple(conf)[rng.randrange(len(conf))]`
    line or run ruff on the module. Preserve the single-RNG determinism (same rng threaded through).
  </action>
  <acceptance_criteria>
    - `pytest tests/test_profile_general.py tests/test_heuristic_instrumentation.py -x` passes (find seed-137 K16 + hold non-spanning state + instrumentation contract)
    - `grep -vE "^\s*#" src/alpha2/search/heuristic.py | grep -c "assert "` returns 0
    - `grep -n "tuple(conf)\[rng.randrange(len(conf))\]" src/alpha2/search/heuristic.py` still present verbatim (determinism idiom preserved)
    - `./.venv/bin/python -m pytest tests/test_battery_seed137_e2e.py -x -q` STILL passes (SC1 not regressed — the small SC1 heuristic budget still misses; the exact had_2=17 kill is unchanged)
    - `./.venv/bin/python -m pytest -q -m "not slow"` green (no regression to 207-test suite)
  </acceptance_criteria>
  <verify>
    <automated>./.venv/bin/python -m pytest tests/test_profile_general.py tests/test_heuristic_instrumentation.py -x -q</automated>
  </verify>
  <done>solve() is profile-general (non-spanning), finds the seed-137 K16 model under a generous budget, exposes instrumentation, never asserts, preserves the determinism idiom, and does not regress the SC1 e2e slice.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| searcher output -> pipeline | an untrusted proposal (or a miss); the verifier arbitrates the model, the pipeline routes a miss to exact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-10 | Tampering (false RESISTANT) | heuristic miss semantics | mitigate | miss returns sets=None; the pipeline (Plan 02) maps None to the exact-solver edge; RESISTANT only via exact timeout — never a searcher miss |
| T-06-11 | Denial (crash on pool instance) | assert on chi<n/2 | mitigate | remove the assert; bounded profile enumeration; chi<n/2 smoke case asserts no raise |
| T-06-12 | Tampering (nondeterminism) | ruff/reformat of the determinism idiom | mitigate | preserve `tuple(conf)[rng.randrange(len(conf))]` byte-for-byte; module stays ruff-excluded; single-RNG threading unchanged |
| T-06-13 | Spoofing (unverified model) | profile-general family trusted as-is | mitigate | the returned family is UNTRUSTED until verify_certificate arbitrates in the pipeline; the test asserts G-edge/disjointness structurally |
</threat_model>

<verification>
- `./.venv/bin/python -m pytest tests/test_profile_general.py tests/test_heuristic_instrumentation.py -q` green
- `./.venv/bin/python -m pytest tests/test_battery_seed137_e2e.py -q` STILL green (SC1 preserved)
- `./.venv/bin/python -m pytest -q -m "not slow"` green
- grep confirms zero asserts and the determinism idiom intact
</verification>

<success_criteria>
- SRCH-01: profile-general heuristic iterates (p', s') with p'+s'=chi and 2p'+s'<=n; finds the seed-137-class model the spanning profile missed
- SRCH-02: restarts-to-solution + initial-conflict instrumentation exposed; a miss is sets=None, never a RESISTANT result
- SC1 e2e remains green (budget-separated: small SC1 budget misses; generous SRCH budget finds)
</success_criteria>

<output>
Create `.planning/phases/06-kill-battery-cli-gate-search-statuses/06-03-SUMMARY.md` when done.
</output>
