---
phase: 06-kill-battery-cli-gate-search-statuses
plan: 04
type: execute
wave: 3
depends_on: [06-01, 06-02]
files_modified:
  - src/alpha2/status/__init__.py
  - src/alpha2/status/views.py
  - src/alpha2/gate/safe_families.py
  - src/alpha2/gate/runner.py
  - src/alpha2/cli.py
  - tests/test_status_views.py
  - tests/test_safe_family_screen.py
autonomous: true
requirements: [VRF-03, GATE-03]
user_setup: []

must_haves:
  truths:
    - "Instance status (KILLED / SHC-CANDIDATE / RESISTANT) is a DERIVED view over the immutable corpus + append-only results log"
    - "Status derivation only READS stored records; a RESISTANT->KILLED transition appends new facts, never edits a stored record"
    - "RESISTANT is derivable ONLY from a results-log event recording an exact-method timeout (INSUFFICIENT), never a heuristic miss"
    - "Each instance's terminal state carries method + certificate reference + reason + seed/provenance"
    - "The G5/G6 safe-family screen is a maintained family->citation map consulted by the gate; unimplemented families log 'screen not yet active' rather than falsely killing"
  artifacts:
    - path: "src/alpha2/status/views.py"
      provides: "build_status_view(corpus_records, log_events) -> derived KILLED/SHC-CANDIDATE/RESISTANT per instance (read-only)"
      contains: "RESISTANT"
      min_lines: 50
    - path: "src/alpha2/gate/safe_families.py"
      provides: "proven-safe family->citation map (from Appendix E §2 line 644) + membership screen"
      min_lines: 30
  key_links:
    - from: "src/alpha2/status/views.py"
      to: "src/alpha2/corpus/manifest.py"
      via: "read-only load-and-derive pattern (manifest_key / build_manifest analog)"
      pattern: "manifest_key|json.load"
    - from: "src/alpha2/gate/safe_families.py"
      to: "src/alpha2/gate/runner.py"
      via: "G6 screen consulted in the flag chain"
      pattern: "safe_families"
    - from: "src/alpha2/cli.py"
      to: "src/alpha2/status/views.py"
      via: "status subcommand dispatch"
      pattern: "views\\."
---

<objective>
Two hardening deliverables that depend on the SC1 slice but are off its critical path:

1. VRF-03 — derived statuses. KILLED / SHC-CANDIDATE / RESISTANT is a PURE FUNCTION of the immutable
   corpus records + append-only results-log events, computed on read. Transitions never edit a stored
   record (the store is append-only + hash-chained by construction). RESISTANT is derivable ONLY from
   an exact-method timeout event (differential INSUFFICIENT), never a heuristic miss. Expose it via an
   `alpha2 status` subcommand.

2. GATE-03 — the G5/G6 known-safe screen as a maintained settled/open map (family -> citation) drawn
   from the proven-safe family list frozen at Appendix E §2 line 644, consulted by the gate so proven
   terrain is never re-hunted. MVP ships the pinned family list as data + membership screens; families
   not yet implemented log "screen not yet active" rather than falsely killing.

Purpose: complete the status/log discipline (SC5) and seed the safe-family map (SC3).
Output: `src/alpha2/status/views.py`, `src/alpha2/gate/safe_families.py`, an `alpha2 status` subcommand.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-kill-battery-cli-gate-search-statuses/06-CONTEXT.md
@.planning/phases/06-kill-battery-cli-gate-search-statuses/06-RESEARCH.md
@.planning/phases/06-kill-battery-cli-gate-search-statuses/06-PATTERNS.md

<interfaces>
Load-and-derive read-only analog (src/alpha2/corpus/manifest.py:30-99):
  _FAMILY_KEY map; manifest_key(record) -> "tfp:n{n}:s{seed}" / "cayley:p{n}:s{seed}";
  build_manifest(records) derives a keyed view; with open(corpus_path) as fh: records=json.load(fh) (READ-ONLY)

Results-log events (Plan 02 battery/log.py): each event carries terminal_state, method,
  certificate_ref, reason, seed, provenance, budgets. RESISTANT derives from an event whose
  terminal_state records an exact-method timeout (differential INSUFFICIENT) — never a heuristic miss.

Gate chain to consult (Plan 01 gate/runner.py): DEFAULT_CHAIN registers g5/g6 as flag stubs;
  safe_families provides the G6 membership screen the runner calls (flag_only, D-01 Role B).

FROZEN proven-safe family list (Appendix E §2 line 644 — reference/quote, never invent):
  .planning/reference/alpha2-program-source.md:644
  (line graphs; quasi-line; C5-free; inflations of graphs on <=11 vertices; inflations of complements
   of girth->=5 graphs incl. C5/Petersen/Hoffman-Singleton; inflations of complements of triangle-free
   Kneser; Clebsch/Mesner/Gewirtz complements + inflations; Higman-Sims complement; Andrasfai + odd-
   anticycle inflations; Eberhard Cayley complements; fractional-clique-cover < 3 even order; <=87
   vertices connected-matching weakening — each with its cited author)

Paths authority (src/alpha2/paths.py): CORPUS + RESULTS_LOG (Plan 02).
cli.py (Plan 02): add a `status` subparser beside `battery` (this plan modifies cli.py — Wave 3, after Plan 02).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0 status + safe-family tests</name>
  <read_first>
    - src/alpha2/corpus/manifest.py:30-99 (load-and-derive read-only pattern the view mirrors)
    - tests/test_seed137_regression.py:44-70 (read-only corpus load idiom)
    - .planning/reference/alpha2-program-source.md:644 (the proven-safe family list to encode as data)
    - .planning/phases/06-kill-battery-cli-gate-search-statuses/06-PATTERNS.md:195-215, 330 (status view analog; safe_families no-analog note)
  </read_first>
  <behavior>
    - test_status_views: given synthetic corpus records + log events, build_status_view derives KILLED for a verified-model record, SHC-CANDIDATE for a differential SHC value-fact, RESISTANT for an exact-timeout event
    - a RESISTANT->KILLED transition (later kill event) yields KILLED WITHOUT mutating the earlier stored record (input records object unchanged)
    - a heuristic-miss log event does NOT by itself produce RESISTANT
    - test_safe_family_screen: an implemented family (e.g. C5-free or line-graph, whichever is coded) screens as a cited safe member; an unimplemented family returns "screen not yet active" (never a false kill)
  </behavior>
  <action>
    Write tests/test_status_views.py building synthetic in-memory corpus records (verified model;
    SHC value-fact) and results-log events (KILLED, SHC-CANDIDATE, exact-timeout RESISTANT, heuristic
    miss) and asserting build_status_view's derivations, the append-only transition (stored record
    object identity/contents unchanged after a later event), and that a heuristic miss never yields
    RESISTANT. Write tests/test_safe_family_screen.py asserting the family->citation map contains the
    pinned §2 families (by name) each with a citation string, that an implemented membership screen
    returns a cited hit, and that an unimplemented family screens as "screen not yet active". These
    tests exercise Tasks 2-3.
  </action>
  <acceptance_criteria>
    - `./.venv/bin/python -m pytest tests/test_status_views.py tests/test_safe_family_screen.py --collect-only -q` collects (imports resolve once Tasks 2-3 exist)
    - test_status_views includes an explicit assertion that a heuristic-miss event does not produce RESISTANT
    - test_safe_family_screen asserts every mapped family carries a non-empty citation
  </acceptance_criteria>
  <verify>
    <automated>./.venv/bin/python -m pytest tests/test_status_views.py tests/test_safe_family_screen.py --collect-only -q</automated>
  </verify>
  <done>Wave-0 tests scaffolded for derived statuses (incl. the miss!=RESISTANT and append-only-transition invariants) and the safe-family screen (cited families + not-yet-active fallback).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: status/views.py — derived KILLED/SHC-CANDIDATE/RESISTANT + `alpha2 status`</name>
  <read_first>
    - src/alpha2/corpus/manifest.py:50-99 (build_manifest / manifest_key — the read-only derive shape)
    - src/alpha2/battery/log.py (Plan 02 — results-log event fields)
    - src/alpha2/cli.py (Plan 02 — add a `status` subparser beside `battery`)
    - .planning/phases/06-kill-battery-cli-gate-search-statuses/06-CONTEXT.md:53-57 (VRF-03 locked semantics)
  </read_first>
  <behavior>
    - build_status_view(corpus_records, log_events) returns a per-instance keyed view {status, method, certificate_ref, reason, seed, provenance}
    - KILLED derives from a corpus record with a verified model; SHC-CANDIDATE from a differential SHC value-fact; RESISTANT ONLY from a log event recording an exact-method timeout (INSUFFICIENT)
    - the function never mutates a stored record; a later event supersedes an earlier status by appending, not editing
    - `alpha2 status` prints the derived view (reads corpus + results log via paths, read-only)
  </behavior>
  <action>
    Create src/alpha2/status/__init__.py and src/alpha2/status/views.py with build_status_view(
    corpus_records, log_events) that keys instances (reuse the manifest_key scheme / provenance
    identity), derives KILLED from a verified corpus model, SHC-CANDIDATE from a differential SHC
    value-fact, and RESISTANT ONLY when a log event's terminal_state is the exact-method timeout
    (INSUFFICIENT) marker — a heuristic-miss event is ignored for RESISTANT. Precedence: a verified
    KILLED supersedes an earlier RESISTANT by appending a new fact; the view is computed on read and
    NEVER edits an input record (treat inputs as immutable; raise on internal drift/duplicate). Add a
    load_status_view() that json.load's paths.CORPUS (read-only) and reads paths.RESULTS_LOG line by
    line. Add a `status` subcommand to cli.py dispatching to load_status_view and printing a JSON
    summary. Raises-only; no assert.
  </action>
  <acceptance_criteria>
    - `pytest tests/test_status_views.py -x -q` passes: KILLED/SHC-CANDIDATE/RESISTANT derivations correct; RESISTANT only from an exact-timeout event; a heuristic miss never RESISTANT
    - the append-only invariant test passes: input corpus record objects are unchanged after a superseding event
    - `./.venv/bin/python -m alpha2.cli status` runs read-only and prints a JSON view (exits 0)
    - `grep -vE "^\s*#" src/alpha2/status/views.py | grep -c "assert "` returns 0
  </acceptance_criteria>
  <verify>
    <automated>./.venv/bin/python -m pytest tests/test_status_views.py -x -q</automated>
  </verify>
  <done>status/views.py derives the three statuses read-only from immutable corpus + append-only log; RESISTANT gated on exact-timeout; transitions never edit records; `alpha2 status` exposes the view.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: gate/safe_families.py — G5/G6 settled/open map (GATE-03)</name>
  <read_first>
    - .planning/reference/alpha2-program-source.md:641-644 (G5/G6 + the FROZEN proven-safe family list with citations)
    - src/alpha2/corpus/manifest.py:36-47 (_FAMILY_KEY map — the family->value shape analog)
    - src/alpha2/gate/runner.py (Plan 01 — DEFAULT_CHAIN g5/g6 stub hooks to wire)
    - .planning/phases/06-kill-battery-cli-gate-search-statuses/06-CONTEXT.md:100-115 (B7 + full-map growth are DEFERRED; MVP stubs unimplemented families)
  </read_first>
  <behavior>
    - SAFE_FAMILIES maps each pinned §2 family name to a citation string (line graphs->Reed-Seymour, quasi-line->Chudnovsky-Fradkin, C5-free->PST, inflations<=11->PST, Higman-Sims complement, ... <=87 connected-matching->Furedi-Gyarfas-Simonyi/Chen-Deng)
    - screen_safe_families(adj, n, inv) returns a cited safe-member hit for any implemented membership test, or a "screen not yet active" marker for families whose membership test is deferred
    - the runner consults the screen as a flag_only G6 check (D-01 Role B — never hard-kills a studied pool instance)
    - the B7-free screen is NOT implemented here (deferred, needs author sign-off)
  </behavior>
  <action>
    Create src/alpha2/gate/safe_families.py with SAFE_FAMILIES: an ordered map from each proven-safe
    family name (verbatim from §2 line 644) to its citation. Implement screen_safe_families(adj, n, inv)
    that runs the membership tests that are cheap/decidable now (e.g. C5-free via induced-C5 absence, or
    line-graph/fractional-clique-cover<3-even-order if tractable — implementer discretion on which subset
    to code for MVP) and returns a result carrying either a cited safe hit or a per-family "screen not
    yet active" note for deferred tests. Wire it into runner.DEFAULT_CHAIN as the G6 flag_only check
    (replacing the Plan 01 stub) so a safe-family hit is RECORDED as a flag (Role B) — do NOT hard-kill
    studied pools. Do NOT implement the B7-free screen (deferred to author sign-off, Plan 05). Raises-
    only; no assert. Keep the map as data so it can grow as pools/literature settle classes.
  </action>
  <acceptance_criteria>
    - `pytest tests/test_safe_family_screen.py -x -q` passes: every mapped family has a non-empty citation; an implemented screen returns a cited hit; a deferred family returns "screen not yet active"
    - `grep -c "B" src/alpha2/gate/safe_families.py` — the B7 screen is absent (deferred); a comment marks it deferred to Plan 05
    - the gate seed-137 pass test (Plan 01) STILL passes: the G6 safe-family screen is flag_only and never kills seed-137
    - `grep -vE "^\s*#" src/alpha2/gate/safe_families.py | grep -c "assert "` returns 0
  </acceptance_criteria>
  <verify>
    <automated>./.venv/bin/python -m pytest tests/test_safe_family_screen.py tests/test_gate_seed137_pass.py -x -q</automated>
  </verify>
  <done>safe_families.py encodes the pinned §2 family->citation map + a membership screen consulted by the gate as flag_only (Role B); unimplemented families log "screen not yet active"; B7 deferred; seed-137 still passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored facts -> derived view | the view must READ immutable corpus + append-only log, never write |
| safe-family screen -> gate verdict | a false "safe" hit could wrongly influence a kill; must be flag_only for studied pools |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-14 | Tampering (record edit) | status/views.py transition | mitigate | derivation is read-only; a RESISTANT->KILLED transition appends a new fact; test asserts input records unchanged (VRF-03) |
| T-06-15 | Spoofing (false RESISTANT) | RESISTANT derivation | mitigate | RESISTANT derives ONLY from an exact-timeout (INSUFFICIENT) log event; a heuristic-miss event is ignored — test-locked |
| T-06-16 | Tampering (false kill) | gate/safe_families.py G6 screen | mitigate | G6 screen is flag_only (D-01 Role B) — records a cited hit, never hard-kills a studied pool instance; seed-137 pass test re-run |
| T-06-17 | Spoofing (invented citation) | SAFE_FAMILIES map | mitigate | families + citations quoted verbatim from §2 line 644; unimplemented membership -> "screen not yet active", never a fabricated safe verdict; B7 deferred |
</threat_model>

<verification>
- `./.venv/bin/python -m pytest tests/test_status_views.py tests/test_safe_family_screen.py tests/test_gate_seed137_pass.py -q` green
- `./.venv/bin/python -m pytest -q -m "not slow"` green (no regression)
- `alpha2 status` runs read-only (corpus + log untouched)
- `python -O` canary: status + safe_families paths carry zero `assert`
</verification>

<success_criteria>
- VRF-03: status is a derived read-only view over immutable corpus + append-only log; transitions never edit records; RESISTANT only via exact-method timeout
- GATE-03: G5/G6 known-safe screen is a maintained family->citation map (from §2) consulted by the gate; MVP logs "screen not yet active" for deferred families
</success_criteria>

<output>
Create `.planning/phases/06-kill-battery-cli-gate-search-statuses/06-04-SUMMARY.md` when done.
</output>
