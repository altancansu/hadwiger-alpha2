---
phase: 06-kill-battery-cli-gate-search-statuses
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/alpha2/invariants/cliques.py
  - src/alpha2/gate/__init__.py
  - src/alpha2/gate/checks.py
  - src/alpha2/gate/runner.py
  - tests/test_gate_criticality.py
  - tests/test_gate_outcomes.py
  - tests/test_gate_runner.py
  - tests/test_gate_seed137_pass.py
autonomous: true
requirements: [GATE-01, GATE-02]
user_setup: []

must_haves:
  truths:
    - "The gate runs G1-G6 as a cost-ordered chain and returns PASS / FAIL(witness) / ERROR per candidate"
    - "seed-137 (n=31) PASSES the hard-gate set {G1 criticality, G2, connectivity}"
    - "The criticality predicate accepts even n: n=32 (nu=16) passes exactly as n=31 (nu=15) does"
    - "G3-deep (kappa, delta) and G4 (omega window) FAIL for seed-137 but are recorded as flags, not kills (D-01 Role B)"
    - "omega(G) and kappa(G) are computed via networkx confined to invariants/cliques.py"
  artifacts:
    - path: "src/alpha2/invariants/cliques.py"
      provides: "omega_G, kappa_G, is_connected_G via networkx max_weight_clique/node_connectivity/is_connected"
      contains: "max_weight_clique"
      min_lines: 25
    - path: "src/alpha2/gate/checks.py"
      provides: "G1-G6 pure predicates over (adj, n, inv); raises-only guards"
      contains: "n // 2"
      min_lines: 60
    - path: "src/alpha2/gate/runner.py"
      provides: "ordered chain + PASS/FAIL(witness)/ERROR outcome + GateKill; hard vs flag_only tiers (Role B)"
      min_lines: 50
  key_links:
    - from: "src/alpha2/gate/checks.py"
      to: "src/alpha2/invariants/cliques.py"
      via: "omega_G/kappa_G calls for G3/G4 inputs"
      pattern: "cliques\\.(omega_G|kappa_G|is_connected_G)"
    - from: "src/alpha2/gate/checks.py"
      to: "src/alpha2/generators/tfp.py"
      via: "reuse is_triangle_free / is_edge_maximal_tf for G2"
      pattern: "is_(triangle_free|edge_maximal_tf)"
---

<objective>
Build the G1-G6 gate as a configured, cost-ordered predicate chain (GATE-01) with definitions
frozen verbatim from the author's Appendix E §2 (GATE-02), plus the networkx-confined clique/
connectivity invariants (omega, kappa) the gate consumes.

The gate implements D-01 Role B (LOCKED): the ONLY tests that may KILL are the minimal hard-gate
{G1 even-n criticality `nu == n//2`, G2 triangle-free/diameter-2, connectivity}. G3-deep (kappa,
delta, vertex-criticality), G4 (omega window / clique ratio), G5, G6 run as FLAG-ONLY for studied
pools — they record regime + reason + witness onto the result but do NOT terminate the runbook.
seed-137 provably fails the strict §2 gate at G3/G4 (kappa=11<16, delta=16<17, omega=14>13,
omega/n=0.45) yet MUST reach the had_2 step, so a deep gate killing seed-137 is WRONG.

Purpose: this gate is step 1 of the 7-step runbook. It is the prerequisite subsystem the SC1
battery slice (Plan 02) consumes.
Output: `src/alpha2/gate/` (checks + runner) and `src/alpha2/invariants/cliques.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-kill-battery-cli-gate-search-statuses/06-CONTEXT.md
@.planning/phases/06-kill-battery-cli-gate-search-statuses/06-RESEARCH.md
@.planning/phases/06-kill-battery-cli-gate-search-statuses/06-PATTERNS.md

<interfaces>
FROZEN gate definitions — reference/quote, never invent (Appendix E §2):
  .planning/reference/alpha2-program-source.md:635-644
    G1: n>=31, n odd, n=2chi-1  (criticality)  -> encode as `nu == n//2` (even-n fix, LOCKED)
    G2: H triangle-free, diameter 2 (edge-maximal triangle-free)
    G3: chi>=7, kappa(G)>=chi, delta(G)>=chi+1, G Hamiltonian, G vertex-critical, H-v has PM forall v
    G4: 8 <= omega(G) <= chi-3, and omega/n below ~1/4
    G5: every non-adjacent pair in an induced C5; contains W5, K8, all 33 Carter unavoidables
    G6: outside every proven-safe family (safe-family list at line 644)

Existing G2 primitives — REUSE, do not re-derive (src/alpha2/generators/tfp.py:49-62):
  is_triangle_free(adj, n) -> bool
  is_edge_maximal_tf(adj, n) -> bool   # every non-edge closes a triangle <=> diameter 2

networkx-confinement analog (src/alpha2/invariants/matching.py:11-32):
  build nx.Graph via add_nodes_from(range(n)); edges (u,v) for v in adj[u] if u<v; import nx INSIDE fn
  networkx 3.6.1: graph_clique_number is REMOVED. Use max_weight_clique(G, weight=None) -> (clique,_)

Outcome-dataclass raises-only analog (src/alpha2/solvers/result.py:33-58, differential.py:57-108):
  raise on invalid state in __post_init__; NEVER assert (python -O strips asserts)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0 gate test scaffolds + invariants/cliques.py (omega, kappa, connectivity)</name>
  <read_first>
    - src/alpha2/invariants/matching.py:11-32 (networkx-confined, import-inside-fn analog — copy the shape)
    - .planning/phases/06-kill-battery-cli-gate-search-statuses/06-PATTERNS.md:33-64 (cliques.py exact-analog block, verbatim omega_G/kappa_G snippets)
    - .planning/reference/alpha2-program-source.md:635-644 (frozen G3/G4 definitions consuming kappa/omega)
    - src/alpha2/generators/tfp.py:33-48 (triangle_free_process — to regenerate seed-137 adj in tests)
  </read_first>
  <behavior>
    - omega_G(adj, n) returns 14 for seed-137 (n=31, seed=137); uses max_weight_clique(G, weight=None), NOT graph_clique_number
    - kappa_G(adj, n) returns 11 for seed-137 via node_connectivity(complement)
    - is_connected_G(adj, n) returns True for seed-137
    - a K5 complement (small hand instance) yields omega/kappa consistent with hand computation
  </behavior>
  <action>
    Create the four Wave-0 test files as failing/placeholder scaffolds (they exercise Task 2/3 code):
    tests/test_gate_criticality.py, tests/test_gate_outcomes.py, tests/test_gate_runner.py,
    tests/test_gate_seed137_pass.py. Each regenerates instances with triangle_free_process(n,
    random.Random(seed)) and asserts against the gate API from Tasks 2-3.
    Create src/alpha2/invariants/cliques.py with three networkx-confined functions: omega_G(adj, n)
    building G = nx.complement of the H-graph and returning len(max_weight_clique(G, weight=None)[0]);
    kappa_G(adj, n) returning nx.node_connectivity(complement); is_connected_G(adj, n) returning
    nx.is_connected(complement). Import networkx INSIDE each function (CHI-01-style confinement — this
    module is the sole clique/connectivity path exactly as matching.py is the sole chi path). Build the
    H nx.Graph identically to matching.py (add_nodes_from(range(n)); edges u<v). Provide a private
    _H_graph(adj, n) helper. Raises-only; no assert.
  </action>
  <acceptance_criteria>
    - `./.venv/bin/python -c "import random; from alpha2.generators.tfp import triangle_free_process; from alpha2.invariants.cliques import omega_G,kappa_G,is_connected_G; a,_=triangle_free_process(31,random.Random(137)); print(omega_G(a,31),kappa_G(a,31),is_connected_G(a,31))"` prints `14 11 True`
    - `grep -n "graph_clique_number" src/alpha2/invariants/cliques.py` returns nothing (removed API not used)
    - `grep -vE "^\s*#" src/alpha2/invariants/cliques.py | grep -c "assert "` returns 0
  </acceptance_criteria>
  <verify>
    <automated>./.venv/bin/python -c "import random; from alpha2.generators.tfp import triangle_free_process; from alpha2.invariants.cliques import omega_G,kappa_G,is_connected_G; a,_=triangle_free_process(31,random.Random(137)); assert (omega_G(a,31),kappa_G(a,31),is_connected_G(a,31))==(14,11,True)"</automated>
  </verify>
  <done>cliques.py exposes omega_G/kappa_G/is_connected_G (networkx-confined, raises-only) verified at seed-137 = (14, 11, True); the four gate test files exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: gate/checks.py — G1-G6 predicates frozen from §2 (even-n criticality)</name>
  <read_first>
    - .planning/reference/alpha2-program-source.md:635-644 (the FROZEN G1-G6 table — quote reasons in witnesses, never paraphrase definitions)
    - src/alpha2/generators/tfp.py:49-62 (is_triangle_free / is_edge_maximal_tf — REUSE for G2)
    - src/alpha2/invariants/cliques.py (Task 1 — omega_G/kappa_G/is_connected_G)
    - src/alpha2/corpus/verifier.py:34-70 (raises-only guard discipline; if not cond: raise)
    - .planning/phases/06-kill-battery-cli-gate-search-statuses/06-PATTERNS.md:68-95 (checks.py predicate-shape block, even-n snippet)
  </read_first>
  <behavior>
    - g1_criticality PASSES n=31 (nu=15) and n=32 (nu=16); FAILS n=30 with nu!=n//2; FAILS n<31
    - g2 PASSES seed-137 (triangle-free + edge-maximal/diameter-2)
    - g3 flag predicate reports FAIL for seed-137 (kappa=11<chi=16, delta=16<chi+1=17) with a witness carrying the failing values
    - g4 flag predicate reports FAIL for seed-137 (omega=14 > chi-3=13; omega/n=0.45 > 0.25) with a witness
    - connectivity check PASSES seed-137
  </behavior>
  <action>
    Create src/alpha2/gate/checks.py with pure predicates over (adj, n, inv) where inv is a dict of
    pre-computed invariants (nu_H, chi_G, omega_G, kappa_G). Each returns a gate result (PASS with a
    witness dict, or FAIL with reason string + witness dict) — the outcome types live in runner.py
    (Task 3); import them here. Implement: g1_criticality using `nu == n // 2` (NEVER `n == 2*chi - 1`
    — that silently drops even-n; n=32 is the standing counterexample) plus the n>=31 Carter bound;
    g2_triangle_free_diam2 delegating to tfp.is_triangle_free and tfp.is_edge_maximal_tf; g_connectivity
    via cliques.is_connected_G; g3_deep computing delta(G) directly as min over v of (n-1-len(adj[v]))
    and reading kappa from inv, checking chi>=7, kappa>=chi, delta>=chi+1 (record each sub-condition in
    the witness; the Hamiltonicity / vertex-criticality / H-v-PM sub-conditions may be logged as
    "not yet screened" for MVP); g4_omega_window checking 8<=omega<=chi-3 and omega/n<0.25 from inv.
    Quote the §2 "Source/reason" text into each check's witness/reason (RST for K6; K8 unavoidable;
    seagull theorem) — do NOT invent definitions. Raises-only on malformed input; no assert anywhere.
    Do NOT decide hard-vs-flag here — checks are pure; tier assignment lives in the runner (Task 3).
  </action>
  <acceptance_criteria>
    - `pytest tests/test_gate_criticality.py -x` passes: g1 accepts n=31 AND n=32 via nu==n//2; rejects an nu!=n//2 case
    - `grep -n "2 \* chi - 1\|2\*chi-1\|n == 2" src/alpha2/gate/checks.py` returns nothing (forbidden criticality form absent)
    - `grep -vE "^\s*#" src/alpha2/gate/checks.py | grep -c "assert "` returns 0
    - a computed check panel shows seed-137: g1 PASS, g2 PASS, connectivity PASS, g3_deep FAIL(kappa/delta), g4 FAIL(omega window)
  </acceptance_criteria>
  <verify>
    <automated>./.venv/bin/python -m pytest tests/test_gate_criticality.py -x -q</automated>
  </verify>
  <done>checks.py implements G1-G6 predicates frozen from §2 with even-n criticality; G2 reuses tfp primitives; G3/G4 read omega/kappa from cliques; raises-only; even-n and seed-137 panels verified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: gate/runner.py — cost-ordered chain, PASS/FAIL(witness)/ERROR, Role B tiers</name>
  <read_first>
    - src/alpha2/solvers/result.py:33-58 (Status enum + frozen validated-dataclass __post_init__ raise discipline — mirror the shape)
    - src/alpha2/solvers/differential.py:57-108 (compose-guard-raise verdict shape)
    - .planning/phases/06-kill-battery-cli-gate-search-statuses/06-CONTEXT.md:23-40 (D-01 Role B — LOCKED hard vs flag_only tiers)
    - .planning/phases/06-kill-battery-cli-gate-search-statuses/06-PATTERNS.md:99-121 (runner.py analog block)
    - src/alpha2/gate/checks.py (Task 2)
  </read_first>
  <behavior>
    - run_gate walks an ordered list [(name, tier, check_fn)] in cost order (G1..G6)
    - a HARD FAIL stops immediately and returns a GateKill(reason=name, witness=...); no later checks run
    - a FLAG_ONLY FAIL is appended to a flags list and execution CONTINUES
    - an ERROR quarantines (distinct outcome) and is NEVER a kill
    - hard set = {g1_criticality, g2_triangle_free_diam2, g_connectivity}; g3_deep/g4/g5/g6 = flag_only
    - seed-137 => overall PASS with flags [g3_deep, g4] recorded (proves Role B)
  </behavior>
  <action>
    Create src/alpha2/gate/runner.py defining the outcome vocabulary as raises-only validated
    dataclasses/enum: Pass(witness), Fail(reason, witness), Error(trace), and GateKill(reason, witness).
    Define a GateResult aggregate carrying overall verdict in {PASS, KILLED, ERROR}, the killing check
    name (if any), and the ordered flags list. Provide DEFAULT_CHAIN as the cost-ordered
    [(name, tier, check_fn)] with tier in {"hard","flag_only"} per D-01: hard = g1/g2/connectivity;
    flag_only = g3_deep/g4/g5/g6. Provide run_gate(adj, n, inv, chain=DEFAULT_CHAIN) that iterates the
    chain: on a hard Fail return GateResult(KILLED, killing=name, witness); on a flag_only Fail append
    to flags and continue; on an Error return GateResult(ERROR) (never KILLED). g5/g6 may be registered
    as stub checks returning Pass with a "screen not yet active" witness (GATE-03 hardening lands in
    Plan 04). Every guard raises; no assert. Wire the seed-137 pass test.
  </action>
  <acceptance_criteria>
    - `pytest tests/test_gate_runner.py tests/test_gate_outcomes.py tests/test_gate_seed137_pass.py -x` passes
    - test_gate_seed137_pass asserts run_gate(seed-137) verdict == PASS with g3_deep and g4 in flags (D-01 Role B proven; a deep gate does NOT kill seed-137)
    - test_gate_outcomes asserts ERROR is never KILLED, and a hard FAIL stops the chain (later checks not invoked)
    - `grep -vE "^\s*#" src/alpha2/gate/runner.py | grep -c "assert "` returns 0
  </acceptance_criteria>
  <verify>
    <automated>./.venv/bin/python -m pytest tests/test_gate_runner.py tests/test_gate_outcomes.py tests/test_gate_seed137_pass.py -x -q</automated>
  </verify>
  <done>runner.py runs the cost-ordered chain with Role B hard/flag_only tiers; PASS/FAIL(witness)/ERROR modeled raises-only; seed-137 PASSES the hard set with G3/G4 flags recorded; ERROR is never a kill.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| candidate graph -> gate | untrusted (adj, n, seed) crosses into the necessary-conditions filter |
| gate check -> invariant values | omega/kappa/chi are computed inputs the checks consume |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering (of a verdict) | gate/runner.py deep-gate kill of a studied instance | mitigate | D-01 Role B LOCKED: G3-deep/G4/G5/G6 are flag_only; test_gate_seed137_pass asserts seed-137 PASSES with G3/G4 as flags, never killed |
| T-06-02 | Spoofing (of criticality) | gate/checks.py g1_criticality | mitigate | encode `nu == n//2` (even-n accepted, n=32 passes); forbid `n == 2*chi-1`; grep gate asserts the forbidden form is absent |
| T-06-03 | Elevation (of a bypass) | gate hard-set on malformed input | mitigate | G1 nu==n//2 + G2 triangle-free/diam2 + connectivity are hard; raises-only guards survive `python -O` |
| T-06-SC | Tampering (supply chain) | package installs | accept | Phase 6 installs NO new package (RESEARCH: audit N/A); only stdlib + already-audited networkx 3.6.1 |
</threat_model>

<verification>
- `./.venv/bin/python -m pytest tests/test_gate_criticality.py tests/test_gate_outcomes.py tests/test_gate_runner.py tests/test_gate_seed137_pass.py -q` all green
- `./.venv/bin/python -m pytest -q -m "not slow"` still green (no regression to the 207-test suite)
- `python -O` canary: gate path carries zero `assert` (grep-verified in each file)
</verification>

<success_criteria>
- GATE-01: gate is a configurable cost-ordered chain; hard FAIL kills on first failure with reason + witness; ERROR never kills
- GATE-02: G1-G6 frozen from §2; criticality accepts even n (n=32 passes)
- D-01 Role B honored: seed-137 PASSES the hard set; G3-deep/G4 recorded as flags, not kills
- omega/kappa/connectivity networkx-confined to invariants/cliques.py
</success_criteria>

<output>
Create `.planning/phases/06-kill-battery-cli-gate-search-statuses/06-01-SUMMARY.md` when done.
</output>
