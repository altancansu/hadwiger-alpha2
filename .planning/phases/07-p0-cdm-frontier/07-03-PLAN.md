---
phase: 07-p0-cdm-frontier
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/alpha2/pool/cdm/schema.py
  - src/alpha2/pool/cdm/verifier.py
  - src/alpha2/pool/cdm/store.py
autonomous: true
requirements: [POOL-0]

must_haves:
  truths:
    - "build_cdm_record assembles a JSON-serializable CDM certificate (graph6 provenance + H_edges + H_edges_sha256 + matching_M + invariants{n, complement_connected, cdm} + a top-level generation{geng_version, flags, shard, index} SC1 field), platform-agnostic (no CBC/Rosetta stamp)"
    - "verify_cdm_witness accepts a valid CDM record and raises VerificationError on any non-matching / non-connected / non-dominating / empty-M / not-a-G-edge / sha256-mismatch violation"
    - "verify_cdm_witness is stdlib-only, raises-only, and fails closed under python -O"
    - "append_certificate writes to paths.CDM_CORPUS, verifies-at-append via the CDM leg, enforces append-only prefix-immutability + hash chain, and writes atomically; the frozen 296-corpus and its store are untouched"
  artifacts:
    - path: "src/alpha2/pool/cdm/schema.py"
      provides: "build_cdm_record + CDM reproduction block (platform-agnostic)"
      exports: ["build_cdm_record"]
      min_lines: 40
    - path: "src/alpha2/pool/cdm/verifier.py"
      provides: "verify_cdm_witness trust leg + VerificationError"
      exports: ["verify_cdm_witness", "VerificationError"]
      min_lines: 50
    - path: "src/alpha2/pool/cdm/store.py"
      provides: "append-only CDM corpus writer (default paths.CDM_CORPUS)"
      exports: ["append_certificate"]
      min_lines: 40
  key_links:
    - from: "src/alpha2/pool/cdm/store.py"
      to: "verify_cdm_witness + paths.CDM_CORPUS"
      via: "verify-at-append + default path"
      pattern: "verify_cdm_witness|paths\\.CDM_CORPUS"
    - from: "src/alpha2/pool/cdm/schema.py"
      to: "alpha2.corpus.schema helpers"
      via: "reuse canonical_edges/h_edges_sha256/chain_hash/provenance_graph6"
      pattern: "from alpha2.corpus.schema import"
---

<objective>
Build the CDM certificate trust leg: `build_cdm_record` (schema), the NEW stdlib-only
`verify_cdm_witness` (independent verifier — NOT a reuse of `verify_model_record`), and the
append-only `append_certificate` writing to the dedicated `paths.CDM_CORPUS`. This is the
existence half of the Asymmetry Principle — a tiny hand-checkable matching witness, stored and
independently re-verified, with the frozen 296-corpus and its trust root byte-untouched (Q2 default).

Purpose: CDM-HOLDS = existence = cheap + stored, but only behind an independent stdlib verifier
matching the frozen trust root's discipline. Greens `test_cdm_verifier`, `test_cdm_verifier_dash_O`,
and `test_cdm_store`.
Output: `pool/cdm/schema.py`, `pool/cdm/verifier.py`, `pool/cdm/store.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-p0-cdm-frontier/07-RESEARCH.md
@.planning/phases/07-p0-cdm-frontier/07-PATTERNS.md

<interfaces>
<!-- Reuse these stdlib-only helpers from the frozen schema (import, do not re-implement): -->
From src/alpha2/corpus/schema.py:
- `provenance_graph6(family, n, graph6)`  (schema.py:120-122)
- `canonical_edges(H_edges)`               (schema.py:48-62)
- `h_edges_sha256(H_edges)`                (schema.py:91-98)
- `canonical_record_json`, `chain_hash(prev_chain, record)`, `CHAIN_FIELD`  (schema.py:65-88)
- `_as_int_pairs(edges)`                   (schema.py:229-243 — JSON-native int coercion)
- `validate_provenance(prov)`              (schema.py:125)
DO NOT stamp `CANONICAL_PLATFORM`/`make_reproduction`/`make_backends` (had₂/CBC-shaped) — write a
CDM-specific reproduction block that is `byte_exact` + platform-agnostic (RESEARCH Pitfall 5 / A4).

From src/alpha2/corpus/verifier.py (COPY the discipline, NOT verify_model_record):
- `VerificationError` (verifier.py:30) — mirror as a new class in pool/cdm/verifier.py
- `_build_adj(H_edges, n)` (verifier.py:49-68) — rebuild list[set] from stored edges, raising on malformed/non-canonical/duplicate
- sha256 integrity gate (verifier.py:92-96) — recompute + compare before trusting
- range-check-before-index guard CR-01 (verifier.py:176-179) — `0 <= v < n` before indexing adj

From src/alpha2/corpus/store.py (CLONE the mechanics; swap verifier + default path):
- `append_certificate(rec, path=None)` (store.py:86-144), `_verify_chain(old)` (store.py:53-75),
  atomic write tempfile→flush→os.fsync→os.replace (store.py:132-143). Default path → `paths.CDM_CORPUS`.

CDM record shape (RESEARCH §Architecture / 07-PATTERNS.md §schema): store
`{provenance(graph6, family="mtf_complement"), H_edges, H_edges_sha256, matching_M, invariants:{n, complement_connected: bool, cdm: true}, verified, method, reproduction(byte_exact, platform-agnostic)}`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: build_cdm_record schema (reuse frozen helpers, platform-agnostic)</name>
  <files>src/alpha2/pool/cdm/schema.py</files>
  <behavior>
    - build_cdm_record(...) returns a dict that round-trips through json.dumps/loads with field-equality.
    - The record carries provenance(graph6, family="mtf_complement"), canonicalized H_edges, matching_M as int pairs, and invariants{n, complement_connected, cdm}.
    - No CANONICAL_PLATFORM / cbc_under_rosetta key appears; reproduction.kind == "byte_exact".
    - The record carries a NEW top-level `generation` field {geng_version, flags, shard, index}; these keys survive json.dumps/loads.
  </behavior>
  <read_first>
    - src/alpha2/corpus/schema.py:42,48-98,120-243 (helpers to reuse; the had₂ build_record to mirror but NOT copy wholesale; the CBC/platform block to AVOID)
    - 07-PATTERNS.md §"src/alpha2/pool/cdm/schema.py (schema, transform)" (ENV-05 carve-out A4)
    - 07-RESEARCH.md §"Pitfall 5" (CDM certs are solver-independent → platform-agnostic)
  </read_first>
  <action>
    Create `pool/cdm/schema.py` importing the reusable helpers from `alpha2.corpus.schema`
    (`provenance_graph6, canonical_edges, h_edges_sha256, chain_hash, CHAIN_FIELD, canonical_record_json, validate_provenance, _as_int_pairs`) — stdlib-only boundary preserved (corpus/schema imports no networkx/ortools). Implement `build_cdm_record(*, provenance, H_edges, matching_M, complement_connected, method, generation=None, verified=True)` returning a JSON-serializable dict with canonicalized `H_edges`, `H_edges_sha256`, `matching_M` via `_as_int_pairs`, `invariants={"n": provenance["n"], "complement_connected": bool(complement_connected), "cdm": True}`, and a NEW top-level `generation` field carrying the SC1 per-instance provenance `{geng_version, flags:"geng -ctq | pickg -Z2", shard, index}` (a dedicated-CDM-schema field — do NOT smuggle these keys through the frozen `validate_provenance` graph6 shape). Add a CDM-specific `reproduction` block `{"kind": "byte_exact", "tools": {geng/pickg/nauty version, python version}}` — explicitly NO `canonical_platform`/`cbc_under_rosetta` (RESEARCH Pitfall 5). Coerce every stored int with `int(...)` so no tuple/np-int leaks. Validate provenance via the reused `validate_provenance`.
  </action>
  <verify>
    <automated>uv run python -c "import json,alpha2.pool.cdm.schema as s; r=s.build_cdm_record(provenance={'kind':'graph6','family':'mtf_complement','n':5,'graph6':'D~{'}, H_edges=[[0,1],[1,2]], matching_M=[[0,1]], complement_connected=True, method='cdm_dfs', generation={'geng_version':'nauty2.9.3','flags':'geng -ctq | pickg -Z2','shard':None,'index':0}); assert json.loads(json.dumps(r))==r; assert r['generation']['geng_version']=='nauty2.9.3' and r['generation']['index']==0; assert 'canonical_platform' not in json.dumps(r); assert r['reproduction']['kind']=='byte_exact'; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - The record round-trips through `json.dumps/loads` with equality (no tuple/np-int leak).
    - `grep -n "canonical_platform\|cbc_under_rosetta\|make_reproduction\|make_backends" src/alpha2/pool/cdm/schema.py` returns nothing (platform-agnostic).
    - `grep -n "from alpha2.corpus.schema import" src/alpha2/pool/cdm/schema.py` present (helpers reused, not re-implemented).
    - invariants contains `n`, `complement_connected`, `cdm`.
    - The record has a top-level `generation` {geng_version, flags, shard, index} that survives the json round-trip (SC1 per-instance provenance stored).
  </acceptance_criteria>
  <done>build_cdm_record produces a JSON-native, platform-agnostic CDM certificate reusing the frozen schema helpers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: verify_cdm_witness trust leg (stdlib, raises-only, -O-safe)</name>
  <files>src/alpha2/pool/cdm/verifier.py</files>
  <behavior>
    - verify_cdm_witness on a valid CDM record returns truthy (True or |M|).
    - Raises VerificationError on each mutant: empty M, non-matching, non-connected, non-dominating, size-2-set-that-is-an-H-edge-not-a-G-edge, H_edges_sha256 mismatch.
    - Range-checks 0<=v<n on every witness vertex before indexing (no negative-wrap aliasing).
    - Still raises under python -O on a non-dominating mutant (raises-only, not assert-based).
  </behavior>
  <read_first>
    - src/alpha2/corpus/verifier.py:1-68,92-96,117,169-185 (discipline docstring; _build_adj; sha256 gate; CR-01 range guard; matching/covered-set logic). DO NOT reuse verify_model_record.
    - src/alpha2/invariants/witness.py (emission-side extractor is UNTRUSTED; verifier re-checks from stored data)
    - 07-RESEARCH.md §"The CDM Property" (A1 reading: dominating = ≥1 endpoint) + §Anti-Patterns ("do NOT reuse verify_model_record")
    - 07-PATTERNS.md §"src/alpha2/pool/cdm/verifier.py" (the four CDM verifier legs)
    - tests/pool/cdm/test_cdm_verifier.py, tests/pool/cdm/test_cdm_verifier_dash_O.py (the contract this greens)
  </read_first>
  <action>
    Create `pool/cdm/verifier.py` importing ONLY the stdlib (json/hashlib/collections). Open with the corpus/verifier trust-boundary docstring (stdlib-only; imports nothing from proposers; rebuilds adjacency from stored data; every check `if not cond: raise`; correct under `python -O`). Define a new `VerificationError`. Copy `_build_adj(H_edges, n)` verbatim-style (raise on malformed/non-canonical/duplicate). Implement `verify_cdm_witness(record)` with these legs, each `if not cond: raise VerificationError(...)`: (1) recompute `h_edges_sha256` on stored `H_edges` and compare to `record["H_edges_sha256"]` (integrity gate); (2) rebuild `adj` for G from the stored complement encoding — decide and gate the storage convention so each `(a,b)` in M is a G-edge (i.e. NOT an H-edge), range-checking `0 <= a < b < n` BEFORE indexing (CR-01); (3) M non-empty; (4) M is a matching (no repeated vertex, copy the `covered`-set logic); (5) M connected — every pair `V(e),V(f)` adjacent in G (re-check `vsets_adjacent`); (6) M dominating — every `w ∉ V(M)` adjacent to ≥1 endpoint of each M-edge (A1). Return `True` (or `len(M)`). Use NO `assert` anywhere.
  </action>
  <verify>
    <automated>uv run pytest tests/pool/cdm/test_cdm_verifier.py tests/pool/cdm/test_cdm_verifier_dash_O.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/pool/cdm/test_cdm_verifier.py -x` passes (valid record accepted; ≥6 mutants each raise VerificationError).
    - `pytest tests/pool/cdm/test_cdm_verifier_dash_O.py -x` passes (mutant still raises under `python -O`).
    - `grep -nE "import (networkx|ortools)|verify_model_record" src/alpha2/pool/cdm/verifier.py` returns nothing (no networkx/ortools; verify_model_record NOT reused).
    - `grep -vE '^\s*#' src/alpha2/pool/cdm/verifier.py | grep -c '\bassert\b'` == 0.
  </acceptance_criteria>
  <done>Independent stdlib CDM verifier accepts valid witnesses, rejects all mutants, fails closed under -O.</done>
</task>

<task type="auto">
  <name>Task 3: append-only CDM corpus store (paths.CDM_CORPUS, frozen 296 untouched)</name>
  <files>src/alpha2/pool/cdm/store.py</files>
  <read_first>
    - src/alpha2/corpus/store.py:1-144 (the full clone target: verify-at-append, _verify_chain, atomic tempfile→fsync→os.replace, prefix-immutability)
    - src/alpha2/paths.py (CDM_CORPUS added in 07-01; ensure_parent)
    - 07-PATTERNS.md §"src/alpha2/pool/cdm/store.py" (three guarantees; swap import to the CDM leg; default path)
    - tests/pool/cdm/test_cdm_store.py (the contract this greens)
  </read_first>
  <action>
    Create `pool/cdm/store.py` cloning `corpus/store.py`'s append-only mechanics, importing the NEW `verify_cdm_witness` and `VerificationError` from `alpha2.pool.cdm.verifier` (NOT `corpus.verifier`) and `chain_hash`/`CHAIN_FIELD` from `alpha2.pool.cdm.schema` (or the reused corpus.schema helpers). Implement `append_certificate(rec, path=None)`: default `path = paths.ensure_parent(paths.CDM_CORPUS)`; on append, (1) re-verify every prior record via `verify_cdm_witness` and recompute the per-record hash chain from record 0 (prefix-immutability — raise on any tamper); (2) verify the incoming `rec` via the CDM leg and require `rec["verified"] is True`; (3) write atomically via `tempfile.mkstemp(dir=...)` → `fh.flush()` → `os.fsync(fh.fileno())` → `os.replace(tmp, path)`, unlinking the temp on any failure. Do NOT import, touch, or reference `corpus/store.py`, the frozen 296-corpus, or `paths.CORPUS`.
  </action>
  <verify>
    <automated>uv run pytest tests/pool/cdm/test_cdm_store.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/pool/cdm/test_cdm_store.py -x` passes (valid-append creates file; second-append preserves prefix; unverified/mutant refused with file byte-unchanged; prefix-immutability on tamper; atomic write survives monkeypatch crash).
    - `grep -n "verify_cdm_witness" src/alpha2/pool/cdm/store.py` present; `grep -n "paths.CDM_CORPUS" src/alpha2/pool/cdm/store.py` present.
    - `grep -nE "corpus.store|corpus.verifier|paths.CORPUS\b" src/alpha2/pool/cdm/store.py` returns nothing (frozen corpus untouched).
    - `git status` shows no modification to `data/corpus/hadwiger_alpha2_certificates.json`.
  </acceptance_criteria>
  <done>Dedicated append-only CDM store verifies-at-append via the new leg, atomic + hash-chained, frozen corpus untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored certificate → trust root | verify_cdm_witness rebuilds adjacency from stored primitive data; never trusts a live object or a proposer |
| CDM store → frozen 296-corpus | strict isolation: a separate file (paths.CDM_CORPUS), verifier, and chain; no shared mutable state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-04 | Tampering | mutated CDM certificate accepted as valid | mitigate | sha256 integrity gate + all six verifier legs, each `if not cond: raise`; verify-at-append blocks the write |
| T-7-05 | Tampering | negative-vertex wrap aliasing (`adj[-1]==adj[n-1]`) | mitigate | CR-01 range-check `0 <= v < n` on every witness vertex BEFORE indexing (verifier.py:176-179 pattern) |
| T-7-06 | Tampering (integrity) | prefix mutation of the append-only CDM corpus | mitigate | re-verify every prior record + recompute hash chain from record 0 on each append; atomic tempfile→fsync→os.replace |
| T-7-07 | Repudiation/DoS | `-O` stripping asserts to rubber-stamp a bad witness | mitigate | verifier is raises-only (0 asserts); `test_cdm_verifier_dash_O` canary enforces it |
| T-7-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase |
</threat_model>

## Open decisions (defaulted — override before execute)

- **Q2 (corpus home) — DEFAULT: dedicated CDM corpus.** This plan writes to `paths.CDM_CORPUS`
  with a NEW stdlib `verify_cdm_witness` leg; the frozen 296-corpus, `corpus/store.py`, and
  `corpus/verifier.py` stay byte-untouched. Alternative (additive schema extension) rejected as
  riskier to the frozen trust root. **This is the plan most affected by Q2 — veto here if unified.**
- **Q1 (transfer-lemma carve-out) — DEFAULT: explicit carve-out.** `build_cdm_record` stores
  `invariants.complement_connected` so disconnected-complement instances are catalogued distinctly;
  escalation-suppression is enforced in 07-05.

<verification>
- `pytest tests/pool/cdm/test_cdm_verifier.py tests/pool/cdm/test_cdm_verifier_dash_O.py tests/pool/cdm/test_cdm_store.py -x` all green.
- verifier.py + schema.py: stdlib-only, zero `assert`; schema carries no CBC/platform stamp.
- Frozen corpus + its store/verifier byte-unchanged (`git status`).
</verification>

<success_criteria>
The CDM existence trust leg is complete: JSON-native platform-agnostic certificates, an
independent stdlib verifier rejecting all mutants under `-O`, and an append-only atomic
hash-chained store on the dedicated corpus — frozen 296-corpus untouched.
</success_criteria>

<output>
Create `.planning/phases/07-p0-cdm-frontier/07-03-SUMMARY.md` when done.
</output>
