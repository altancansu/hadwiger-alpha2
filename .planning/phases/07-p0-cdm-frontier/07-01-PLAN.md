---
phase: 07-p0-cdm-frontier
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/alpha2/pool/__init__.py
  - src/alpha2/pool/cdm/__init__.py
  - src/alpha2/paths.py
  - tests/pool/__init__.py
  - tests/pool/cdm/__init__.py
  - tests/pool/cdm/conftest.py
  - tests/pool/cdm/test_cdm_n_le_11.py
  - tests/pool/cdm/test_cdm_verifier.py
  - tests/pool/cdm/test_cdm_verifier_dash_O.py
  - tests/pool/cdm/test_generation_counts.py
  - tests/pool/cdm/test_generation_crosscheck.py
  - tests/pool/cdm/test_dfs_cpsat_agree.py
  - tests/pool/cdm/test_transfer_lemma.py
  - tests/pool/cdm/test_disconnected_complement.py
  - tests/pool/cdm/test_cdm_store.py
autonomous: true
requirements: [POOL-0]

must_haves:
  truths:
    - "Running `pytest tests/pool/cdm -q` collects all 9 CDM test modules (red is expected — modules under test do not yet exist)"
    - "The definition-regression gate test_cdm_n_le_11 exists and encodes the CLWY n≤11 all-CDM claim (A1 reading)"
    - "conftest.py exposes small α=2 fixtures (C₅, K_a⊔K_b, hand CDM witnesses) with no cross-test imports"
    - "paths.CDM_CORPUS resolves to data/corpus/cdm_certificates.json and the frozen paths.CORPUS literal is byte-unchanged"
  artifacts:
    - path: "src/alpha2/paths.py"
      provides: "CDM_CORPUS path constant beside CORPUS"
      contains: "CDM_CORPUS"
    - path: "tests/pool/cdm/conftest.py"
      provides: "shared CDM fixtures"
      min_lines: 30
    - path: "tests/pool/cdm/test_cdm_n_le_11.py"
      provides: "A1 definition regression (highest-priority gate)"
      min_lines: 20
    - path: "tests/pool/cdm/test_cdm_verifier.py"
      provides: "single-perturbation mutant suite for verify_cdm_witness"
      min_lines: 30
  key_links:
    - from: "src/alpha2/pool/cdm/store.py (future)"
      to: "paths.CDM_CORPUS"
      via: "default corpus path"
      pattern: "paths\\.CDM_CORPUS"
    - from: "tests/pool/cdm/*"
      to: "src/alpha2/pool/cdm/*"
      via: "import under test"
      pattern: "from alpha2.pool.cdm"
---

<objective>
Land the Wave-0 failing tests, fixtures, package skeleton, and the new `paths.CDM_CORPUS`
constant for the CDM frontier. These tests are the executable contract every later plan
must satisfy; they are RED now (modules under test do not exist) and go GREEN as Waves 2–3
implement `pool/cdm/`.

Purpose: Nyquist discipline — the definition regression (`test_cdm_n_le_11`, the A1 gate)
and the trust-root mutant suite must exist and fail *before* any n=12–14 new-science claim.
Output: `src/alpha2/pool/` + `tests/pool/cdm/` skeleton, `paths.CDM_CORPUS`, 9 test modules,
one shared `conftest.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-p0-cdm-frontier/07-RESEARCH.md
@.planning/phases/07-p0-cdm-frontier/07-PATTERNS.md
@.planning/phases/07-p0-cdm-frontier/07-VALIDATION.md

<interfaces>
<!-- Contracts the tests assert against. Extracted from repo analogs; executor uses these directly. -->

The modules under test (created in Waves 2–3) will expose:
- `alpha2.pool.cdm.reference.has_cdm(adj, n) -> list[tuple[int,int]] | None`  (witness M or None)
- `alpha2.pool.cdm.cpsat.cdm_cpsat(adj, n) -> bool`  (feasibility; SAT witness untrusted)
- `alpha2.pool.cdm.verifier.verify_cdm_witness(record) -> True`  (raises VerificationError on any violation)
- `alpha2.pool.cdm.verifier.VerificationError`  (Exception subclass — mirror corpus/verifier.py:30)
- `alpha2.pool.cdm.schema.build_cdm_record(...) -> dict`
- `alpha2.pool.cdm.store.append_certificate(rec, path=None) -> None`  (default path=paths.CDM_CORPUS)
- `alpha2.pool.cdm.generate.stream_mtf(n, res=None, mod=None) -> Iterator[(index, graph6, shard)]`

adj convention (LOCKED, RESEARCH §"Complement + α=2 sanity"): `adj = list[set[int]]`,
`adj[u]` = neighbours of u in G (the complement of the graph6-decoded MTF graph H).

paths.py (MODIFY — add beside CORPUS at paths.py:12):
```
CDM_CORPUS = REPO_ROOT / "data" / "corpus" / "cdm_certificates.json"
```

Existing analog test bodies to mirror (copy structure, not literals):
- tests/test_verifier_mutants.py:79-158  (single-perturbation mutant style)
- tests/test_solver_paths_dash_O.py:24-102 (subprocess `-O` canary harness + exit-code contract)
- tests/test_store_append_only.py (tmp_path, prefix-immutability, atomic-crash monkeypatch)
- tests/test_cpsat_backend.py:39-51,225-251 (inline adj fixtures + parametrized differential panel)
- src/alpha2/generators/tfp.py:56-62 (`is_edge_maximal_tf(adj, n)` — the Lemma-2.5 predicate)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Package skeleton, paths.CDM_CORPUS, and CDM fixtures</name>
  <files>src/alpha2/pool/__init__.py, src/alpha2/pool/cdm/__init__.py, src/alpha2/paths.py, tests/pool/__init__.py, tests/pool/cdm/__init__.py, tests/pool/cdm/conftest.py</files>
  <read_first>
    - src/alpha2/paths.py (self — the file being modified; CORPUS at line 12, ensure_parent at 22)
    - src/alpha2/generators/__init__.py and src/alpha2/corpus/__init__.py (empty-package analog, PATTERNS "exact")
    - tests/__init__.py (confirms tests/ is a package; pyproject testpaths=["tests"], pythonpath=["src"])
    - tests/test_cpsat_backend.py:19-51 (embedded-literal fixture discipline: no cross-test imports; `_c5_adj`/`_empty_adj` shape)
    - 07-PATTERNS.md §"src/alpha2/paths.py (config, MODIFY)" and §"tests/pool/cdm/conftest.py"
  </read_first>
  <action>
    Create empty `src/alpha2/pool/__init__.py` and `src/alpha2/pool/cdm/__init__.py` (package markers, mirror generators/__init__.py). Create empty `tests/pool/__init__.py` and `tests/pool/cdm/__init__.py` so pytest resolves the nested package alongside the existing tests/__init__.py.
    In `src/alpha2/paths.py`, add a `CDM_CORPUS = REPO_ROOT / "data" / "corpus" / "cdm_certificates.json"` constant immediately after the `CORPUS` line (Q2 default — dedicated CDM corpus). Do NOT touch the `CORPUS` literal, `RESULTS_LOG`, or `ensure_parent`; `ensure_parent(paths.CDM_CORPUS)` must work via the existing generalized helper.
    In `tests/pool/cdm/conftest.py`, provide pytest fixtures as embedded literals (no imports from other test modules): a C₅ adjacency (`adj = list[set[int]]`, α=2, has CDM), a `K_a⊔K_b` graph as two disjoint cliques (α=2, disconnected, expected CDM-fail), and a hand-built valid CDM record dict + its witness M for the verifier tests. Shape the record after the fields in the Task-referenced build_cdm_record contract (provenance graph6, H_edges, H_edges_sha256, matching_M, invariants{n, complement_connected, cdm}). Add a fixture returning the FULL connected n≤11 MTF-complement set as graph6 strings — every maximal-triangle-free graph at each n≤11 (61 at n=11 plus the smaller n; ≤~100 tiny graphs total). Build it self-contained: either embed the graph6 literals or generate them at fixture-build time by shelling out to `geng -ctq n | pickg -Z2` for n up to 11 (cheap), WITHOUT importing 07-04's generator module. This full set is the ONLY independent oracle that our A1 reading == CLWY's CDM — dual-engine agreement cannot catch a shared wrong A1 reading, and a hand-picked handful can pass while the definition is subtly wrong on an unsampled graph.
  </action>
  <verify>
    <automated>uv run python -c "import alpha2.pool.cdm, alpha2.paths as p; assert str(p.CDM_CORPUS).endswith('data/corpus/cdm_certificates.json'); assert 'hadwiger_alpha2_certificates.json' in str(p.CORPUS)"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import alpha2.pool.cdm"` succeeds (packages importable).
    - `alpha2.paths.CDM_CORPUS` ends with `data/corpus/cdm_certificates.json`; `alpha2.paths.CORPUS` still ends with `hadwiger_alpha2_certificates.json` (frozen literal byte-unchanged).
    - `pytest tests/pool/cdm -q --collect-only` collects conftest fixtures without collection error.
    - conftest.py imports nothing from any other test module (grep: no `from tests` / `import test_`).
  </acceptance_criteria>
  <done>pool/cdm packages exist, paths.CDM_CORPUS added without disturbing CORPUS, conftest fixtures collect.</done>
</task>

<task type="auto">
  <name>Task 2: Definition-regression gate + trust-root verifier tests (RED)</name>
  <files>tests/pool/cdm/test_cdm_n_le_11.py, tests/pool/cdm/test_cdm_verifier.py, tests/pool/cdm/test_cdm_verifier_dash_O.py</files>
  <read_first>
    - tests/test_verifier_mutants.py:20,69-158 (single-perturbation mutant structure + sha256-mismatch mutant)
    - tests/test_solver_paths_dash_O.py:24-102 (subprocess `-O` canary: `if __debug__: sys.exit(3)` guard, exit-code contract 3=not-optimized→fail / 0=raised-correctly / 2=rubber-stamped, `_run_dash_O` with PYTHONPATH=src)
    - tests/test_cpsat_backend.py:225-251 (parametrize/panel style)
    - 07-RESEARCH.md §"The DFS reference decision procedure" (A1 reading: w adjacent to edge ⟺ adjacent to ≥1 endpoint) and §"Pitfall 4"
    - 07-PATTERNS.md §"tests/pool/cdm/test_cdm_verifier.py" and §"test_cdm_verifier_dash_O.py"
  </read_first>
  <action>
    `test_cdm_n_le_11.py` (HIGHEST-priority gate, POOL-0 / A1): iterate the FULL connected n≤11 MTF-complement fixture (ALL of them, not a sample); decode each, filter to CONNECTED complements (disconnected `K_a⊔K_b` are legitimately excluded per the carve-out), and assert `has_cdm(adj, n) is not None` (CDM holds) for EVERY connected instance — this reproduces CLWY's result that every connected α=2 graph at n≤11 has CDM and is the sole independent check that our A1 reading matches theirs. Use a FUNCTION-LOCAL import (`from alpha2.pool.cdm.reference import has_cdm` inside the test body, or `pytest.importorskip`) so `--collect-only` stays clean while the body is RED until 07-02. Include an explicit comment: any connected α=2 graph at n≤11 reported CDM-less is a definition bug (A1), NOT new science. Also assert `has_cdm` returns `None` on the disconnected `K_a⊔K_b` fixture. This module is RED now (reference.py absent) — that is the intended Wave-0 state.
    `test_cdm_verifier.py`: mirror the single-perturbation mutant suite. Start from the conftest valid CDM record (which `verify_cdm_witness` must accept, returning truthy), then one test per mutation asserting `verify_cdm_witness` raises `VerificationError`: (a) empty M; (b) M not a matching (shared vertex); (c) M not connected (a V-non-adjacent edge pair); (d) M not dominating (an uncovered vertex non-adjacent to some M-edge); (e) a size-2 set that is an H-edge not a G-edge; (f) H_edges_sha256 mismatch. Import `verify_cdm_witness, VerificationError` from `alpha2.pool.cdm.verifier` with a FUNCTION-LOCAL import (or `pytest.importorskip`) so `--collect-only` succeeds while the body stays RED.
    `test_cdm_verifier_dash_O.py`: copy the subprocess `-O` canary harness verbatim in shape — spawn `python -O` on an inline snippet that feeds a non-dominating mutant record to `verify_cdm_witness` and asserts it STILL raises (proving raises-only, not assert-based). Reproduce the exit-code contract and PYTHONPATH=src handling from the analog.
  </action>
  <verify>
    <automated>uv run pytest tests/pool/cdm/test_cdm_n_le_11.py tests/pool/cdm/test_cdm_verifier.py tests/pool/cdm/test_cdm_verifier_dash_O.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - All three modules import-collect (collection succeeds; test bodies fail/error at runtime because reference.py/verifier.py are absent — expected RED).
    - `test_cdm_verifier.py` defines ≥6 mutant tests each asserting `VerificationError`, plus a "good record passes" sanity test.
    - `test_cdm_n_le_11.py` iterates the FULL connected n≤11 MTF-complement set (all 61 at n=11 + the smaller n, not a hand-picked sample) and asserts a witness for EVERY connected instance + `None` for `K_a⊔K_b`.
    - `test_cdm_verifier_dash_O.py` invokes a `-O` subprocess and encodes the 3/0/2 exit-code contract (grep: `-O` and `sys.exit`).
  </acceptance_criteria>
  <done>Definition-regression gate + full verifier mutant + `-O` canary landed and collectable (RED).</done>
</task>

<task type="auto">
  <name>Task 3: Generation, differential, transfer-lemma, disconnected, and store tests (RED)</name>
  <files>tests/pool/cdm/test_generation_counts.py, tests/pool/cdm/test_generation_crosscheck.py, tests/pool/cdm/test_dfs_cpsat_agree.py, tests/pool/cdm/test_transfer_lemma.py, tests/pool/cdm/test_disconnected_complement.py, tests/pool/cdm/test_cdm_store.py</files>
  <read_first>
    - 07-RESEARCH.md §"Reproduce and assert the exact MTF frontier counts" (147/392/1,274; sharding Σ over res=0..mod-1) and §"Pitfall 3"
    - src/alpha2/generators/tfp.py:56-62 (`is_edge_maximal_tf(adj, n)` — Lemma-2.5 cross-check predicate)
    - tests/test_cpsat_backend.py:225-251 (parametrized differential panel — mirror for test_dfs_cpsat_agree)
    - tests/test_store_append_only.py (tmp_path isolation, prefix-immutability, atomic-crash monkeypatch)
    - 07-PATTERNS.md §"Test Pattern Assignments" (all six rows)
  </read_first>
  <action>
    ALL imports of not-yet-existing `alpha2.pool.cdm.*` modules in this task MUST be FUNCTION-LOCAL (inside the test/helper body) or via `pytest.importorskip`, so `pytest --collect-only` succeeds cleanly while the test bodies stay RED.
    `test_generation_counts.py`: assert `sum(1 for _ in stream_mtf(n)) == {12:147, 13:392, 14:1274}[n]`, parametrized. Mark the n=14 case `@pytest.mark.slow` (marker registered at pyproject.toml:29). Guard availability with a `shutil.which("geng")` skip. Import `from alpha2.pool.cdm.generate import stream_mtf`.
    `test_generation_crosscheck.py`: on n≤12 survivors, cross-check `pickg -Z2` output against `tfp.is_edge_maximal_tf` over the decoded complement (they must agree on the survivor set), and assert a sharding-sum identity: Σ over res in 0..mod-1 of `len(list(stream_mtf(n, res, mod)))` equals the single-stream count (use small mod, e.g. 4). Note OEIS A216783 as the external anchor in a comment.
    `test_dfs_cpsat_agree.py`: parametrized panel asserting `(has_cdm(adj,n) is not None) == cdm_cpsat(adj,n)` on the conftest small fixtures and (marked `@pytest.mark.slow`, geng-gated) on the full n=12–14 batch, res/mod-shardable. State in a comment that any disagreement is release-blocking.
    `test_transfer_lemma.py`: property tests — (1) Lemma 2.5 equivalence: for the n≤12 survivor sample, `is_edge_maximal_tf(H)` ⟺ diameter(H)==2; (2) CDM edge-addition monotonicity: for α=2 fixtures with `has_cdm(G) is not None`, adding any non-edge that keeps α≤2 preserves `has_cdm is not None`. Use hypothesis if convenient or explicit small enumeration.
    `test_disconnected_complement.py`: assert `K_a⊔K_b` is classified disconnected-complement and its CDM-fail is catalogued, NOT escalated — target the classifier that Wave 3 exposes from adjudicate (import the classification helper; RED until 07-05).
    `test_cdm_store.py`: mirror test_store_append_only.py against `alpha2.pool.cdm.store.append_certificate` with `tmp_path`: valid-append-creates-file, second-append preserves prefix (`data[0]==first`), unverified/mutant refused with file byte-unchanged, prefix-immutability on tamper, atomic-write-survives-monkeypatch-crash. Never touch the real corpus.
  </action>
  <verify>
    <automated>uv run pytest tests/pool/cdm/test_generation_counts.py tests/pool/cdm/test_generation_crosscheck.py tests/pool/cdm/test_dfs_cpsat_agree.py tests/pool/cdm/test_transfer_lemma.py tests/pool/cdm/test_disconnected_complement.py tests/pool/cdm/test_cdm_store.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - All six modules import-collect without collection error (bodies RED — modules under test absent).
    - `test_generation_counts.py` asserts the exact triple 147/392/1,274 and marks n=14 `slow` + geng-skip.
    - `test_generation_crosscheck.py` encodes both the `is_edge_maximal_tf` agreement and the sharding-sum identity.
    - `test_dfs_cpsat_agree.py` asserts DFS/CP-SAT boolean equality per instance and marks the full batch `slow`.
    - `test_cdm_store.py` writes only to `tmp_path` (grep: no `paths.CDM_CORPUS` write target in tests) and covers all five store invariants.
  </acceptance_criteria>
  <done>All remaining Wave-0 tests landed and collectable (RED); the full 07-VALIDATION.md test map now exists on disk.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → filesystem | store tests must write ONLY to tmp_path; the real corpus and frozen 296-corpus are never touched |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-01 | Tampering | test_cdm_store writing to a real corpus path | mitigate | All store tests parametrized on `tmp_path`; acceptance grep forbids `paths.CDM_CORPUS`/`paths.CORPUS` as a write target in tests |
| T-7-02 | Tampering (integrity) | paths.py edit disturbing frozen `CORPUS` literal | mitigate | Task-1 verify asserts `paths.CORPUS` still ends with `hadwiger_alpha2_certificates.json`; only an additive `CDM_CORPUS` line is introduced |
| T-7-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH §Package Legitimacy Audit: zero new deps); nothing to slopcheck |
</threat_model>

## Open decisions (defaulted — override before execute)

Planning proceeded under `--auto` with NO discuss-phase CONTEXT.md. Both researcher-flagged
decisions adopt their recommended defaults; veto at plan-review before execution.

- **Q2 (corpus home) — DEFAULT: dedicated CDM corpus.** This plan adds `paths.CDM_CORPUS`
  (`data/corpus/cdm_certificates.json`) and a parallel test tree, leaving the frozen 296-corpus,
  `corpus/store.py`, and `corpus/verifier.py` byte-untouched. Alternative (additive schema
  extension of the had₂ corpus) was judged riskier to the frozen trust root.
- **Q1 (transfer-lemma carve-out) — DEFAULT: explicit carve-out.** Disconnected complements
  (`K_a⊔K_b` from complete-bipartite MTF) legitimately fail CDM and are classified/catalogued,
  never mis-escalated as a Hadwiger event. Encoded here as the `test_disconnected_complement`
  expectation; the prose proof (manual review) lands in 07-06.

<verification>
- `pytest tests/pool/cdm --collect-only -q` collects all 9 modules + conftest with zero collection errors.
- `pytest tests/pool/cdm -q` is RED (modules under test absent) — the intended Wave-0 baseline.
- Frozen corpus untouched: `git status` shows no change to `data/corpus/hadwiger_alpha2_certificates.json`, `corpus/store.py`, `corpus/verifier.py`, `corpus/schema.py`.
- Full existing suite still green: `pytest -q` shows no regression outside the new (RED) CDM tests.
</verification>

<success_criteria>
All 9 CDM test modules + conftest + package skeleton exist; `paths.CDM_CORPUS` added additively;
the definition-regression gate and trust-root mutant/`-O` tests are landed and collectable; the
frozen had₂ corpus and its trust root are byte-unchanged.
</success_criteria>

<output>
Create `.planning/phases/07-p0-cdm-frontier/07-01-SUMMARY.md` when done.
</output>
