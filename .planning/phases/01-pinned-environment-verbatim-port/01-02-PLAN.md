---
phase: 01-pinned-environment-verbatim-port
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - data/manifests/fingerprint.json
  - tests/test_fingerprint.py
  - tests/test_chi_no_estimate.py
autonomous: true
requirements: [ENV-03, ENV-02, CHI-01, ENV-01]

must_haves:
  truths:
    - "Regenerating n=31 seed=1 produces H_edges whose canonical sha256 equals the frozen golden 3c953d9029ea09463ec838b670aaec8619f1018c79ee89e26fbcea7b2beb41e2."
    - "The stored Appendix D.2 K16 model verifies against the freshly regenerated H via verify_model (witness re-verification, no search replay needed)."
    - "The heuristic solve reproduces a verifying K16 model on the pinned interpreter (and matches D.2 exactly as a pinned-env-only assertion)."
    - "A static guard proves chi is computed only as n - matching_number; no coloring/greedy/estimate call exists in src/ control flow."
    - "The golden manifest was frozen only after the doc-derived invariants (131/15/16/tf/maxTF) passed — the fixture cannot self-certify a porting bug."
  artifacts:
    - path: "data/manifests/fingerprint.json"
      provides: "frozen golden fixture: {tfp:n31:s1 -> m/nu/chi/h_edges_sha256}"
      contains: "3c953d9029ea09463ec838b670aaec8619f1018c79ee89e26fbcea7b2beb41e2"
    - path: "tests/test_fingerprint.py"
      provides: "test_golden_hash + test_stored_model_reverifies + test_heuristic_reproduces (+ seed-137 H-only)"
      exports: ["test_golden_hash", "test_stored_model_reverifies", "test_heuristic_reproduces"]
    - path: "tests/test_chi_no_estimate.py"
      provides: "CHI-01 static guard: no coloring/estimate call in src/"
      exports: ["test_chi_no_estimate"]
  key_links:
    - from: "tests/test_fingerprint.py"
      to: "data/manifests/fingerprint.json"
      via: "compare regenerated sha256 to frozen golden"
      pattern: "h_edges_sha256|fingerprint\\.json"
    - from: "tests/test_fingerprint.py"
      to: "src/alpha2/verify/model.py"
      via: "stored D.2 model re-verified against regenerated H"
      pattern: "verify_model"
    - from: "tests/test_chi_no_estimate.py"
      to: "src/alpha2"
      via: "static scan for coloring/greedy/estimate tokens"
      pattern: "greedy_color|coloring|estimate"
---

<objective>
Lock the reproduction contract on top of the running skeleton: freeze the byte-exact golden H_edges hash (gated behind doc-derived invariants so it cannot self-certify a bug), re-verify the stored Appendix D.2 model against the regenerated H, replay the heuristic to a verifying model, and add the CHI-01 static guard that no chromatic estimate exists anywhere in src/. Optionally pre-lock seed-137's H-only fingerprint for the Phase-4 regression.

Purpose: This is what makes "byte-exact reproduction against Appendix D" a concrete, self-defending assertion rather than a blind byte-diff. It closes ENV-03 and hardens ENV-02/CHI-01 with the two-tier fingerprint design from the research.
Output: A committed golden manifest + the full fingerprint test suite + the chi-no-estimate guard, all GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@CLAUDE.md
@.planning/reference/alpha2-program-source.md
@.planning/phases/01-pinned-environment-verbatim-port/01-RESEARCH.md
@.planning/phases/01-pinned-environment-verbatim-port/01-VALIDATION.md
@.planning/phases/01-pinned-environment-verbatim-port/01-01-SUMMARY.md

<interfaces>
Available from Plan 01 (already ported, verbatim from Appendix C.1):

From src/alpha2/generators/tfp.py:
  triangle_free_process(n, rng) -> (adj: list[set[int]], m: int)
  is_triangle_free(adj, n) -> bool
  is_edge_maximal_tf(adj, n) -> bool

From src/alpha2/invariants/matching.py:
  matching_number(adj, n) -> int   # nu; chi = n - nu (Edmonds blossom, networkx-confined)

From src/alpha2/verify/model.py:
  verify_model(sets, adj, n, k) -> bool   # asserts disjointness, valid sizes, all C(k,2) cross-adjacencies

From src/alpha2/search/heuristic.py:
  solve(adj, n, k, rng, time_budget=90.0) -> (sets, best_init, moves, restarts, elapsed)
</interfaces>

<golden_values>
Pre-validated this session by the researcher (01-RESEARCH.md, Validation Architecture) — SAFE to freeze:
- Canonical H_edges hashing: edges = sorted([min(u,v), max(u,v)] for u in range(n) for v in adj[u] if u < v);
  canonical = json.dumps(edges, separators=(",", ":")); sha256(canonical.encode()).hexdigest()
- n=31, seed=1  -> sha256 == 3c953d9029ea09463ec838b670aaec8619f1018c79ee89e26fbcea7b2beb41e2
- n=31, seed=1  -> m=131, nu=15, chi=16, is_triangle_free=True, is_edge_maximal_tf=True (Appendix D.2)
- Appendix D.2 K16 model_branch_sets (verifies against regenerated H):
  [[16,20],[14,3],[11,4],[10,19],[26,9],[6,29],[18,25],[13,24],[30,8],[15,28],[27,12],[23,7],[17,2],[0],[21,22],[1,5]]
- seed-137 (H-only, stdlib path): n=31 -> |E(H)|=177 (Appendix D.3); model needs CBC (Phase 4, OUT of scope here).
</golden_values>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Freeze golden manifest + byte-exact hash + stored-model re-verify + heuristic replay (ENV-03, ENV-02)</name>
  <files>data/manifests/fingerprint.json, tests/test_fingerprint.py</files>
  <read_first>
    - .planning/phases/01-pinned-environment-verbatim-port/01-RESEARCH.md (Validation Architecture: golden-fixture strategy, two-tier fingerprint; Code Examples: canonical hashing)
    - .planning/reference/alpha2-program-source.md (Appendix D.2: the exemplar invariants + K16 model_branch_sets)
    - tests/test_fingerprint.py (existing test_invariants from Plan 01)
  </read_first>
  <action>
    Follow the golden-fixture strategy exactly (resolves the chicken-and-egg): (1) regenerate H_edges from the ported code; (2) GATE — assert the Appendix-D invariants (m==131, nu==15, chi==16, tf/maxTF True) FIRST, sourced from the doc; (3) only after the gate passes, compute the canonical sha256 and freeze `data/manifests/fingerprint.json` as `{"tfp:n31:s1": {"m":131,"nu":15,"chi":16,"h_edges_sha256":"3c953d9029ea09463ec838b670aaec8619f1018c79ee89e26fbcea7b2beb41e2"}}`. Verify the frozen hash equals the pre-validated golden before committing.
    Extend `tests/test_fingerprint.py` with:
    - `test_golden_hash`: regenerate n=31 s=1, compute canonical H_edges sha256 (edges sorted as `[min(u,v),max(u,v)]` for `u<v`, `json.dumps(..., separators=(",",":"))`), assert it equals the value stored in `data/manifests/fingerprint.json` (load from the manifest, do not hardcode a second copy).
    - `test_stored_model_reverifies`: take the Appendix D.2 model_branch_sets literal (from golden_values above), regenerate H for n=31 s=1, and assert `verify_model(stored_model, adj, 31, 16) is True`. This is the version-proof witness check (R1) — it never replays the search.
    - `test_heuristic_reproduces`: run `solve(adj, 31, 16, random.Random(1))` under the single-RNG contract and assert it returns a model that `verify_model(...)` accepts (assert A VERIFYING model, to avoid coupling CI to heuristic fragility). Add this as a SEPARATE, dedicated test function named `test_heuristic_matches_d2_exact_pinned_env` (not a marker or comment) that asserts the returned model equals the D.2 model exactly — the distinct function name makes the strict pinned-interpreter tier unambiguous and greppable, and keeps `test_heuristic_reproduces` (the CI-safe "any verifying model" tier) separate.
  </action>
  <verify>
    <automated>uv run pytest tests/test_fingerprint.py::test_golden_hash tests/test_fingerprint.py::test_stored_model_reverifies tests/test_fingerprint.py::test_heuristic_reproduces tests/test_fingerprint.py::test_heuristic_matches_d2_exact_pinned_env -x</automated>
    <automated>uv run python -c "import json; d=json.load(open('data/manifests/fingerprint.json')); e=d['tfp:n31:s1']; assert e['h_edges_sha256']=='3c953d9029ea09463ec838b670aaec8619f1018c79ee89e26fbcea7b2beb41e2'; assert e['m']==131 and e['nu']==15 and e['chi']==16; print('golden manifest OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `data/manifests/fingerprint.json` is committed and its `tfp:n31:s1.h_edges_sha256` equals `3c953d9029ea09463ec838b670aaec8619f1018c79ee89e26fbcea7b2beb41e2`, with m=131, nu=15, chi=16.
    - `test_golden_hash` regenerates the graph and matches the frozen hash (loaded FROM the manifest, not a duplicated literal).
    - `test_stored_model_reverifies` verifies the Appendix D.2 model against a freshly regenerated H without replaying search.
    - `test_heuristic_reproduces` asserts solve returns a verifying K16 model; the additional pinned-env assertion matches D.2 exactly.
    - The golden freeze is demonstrably gated on the doc invariants (the invariant gate runs before the hash is trusted).
  </acceptance_criteria>
  <done>The byte-exact fingerprint is frozen and self-defended; regeneration matches the golden hash and the stored model re-verifies. ENV-03 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: CHI-01 static guard + seed-137 H-only fingerprint + env smoke (CHI-01, ENV-03, ENV-01)</name>
  <files>tests/test_chi_no_estimate.py, tests/test_fingerprint.py, data/manifests/fingerprint.json</files>
  <read_first>
    - .planning/phases/01-pinned-environment-verbatim-port/01-RESEARCH.md (CHI-01 guard recommendation; Open Question 2: seed-137 H-only fingerprint)
    - .planning/reference/alpha2-program-source.md (Appendix D.3: seed-137 |E(H)|=177)
    - src/alpha2/invariants/matching.py (the only chi computation)
  </read_first>
  <action>
    Create `tests/test_chi_no_estimate.py::test_chi_no_estimate` — a static guard that scans all `src/alpha2/**/*.py` and asserts NO chromatic-estimate call appears in control flow. Grep for the tokens `greedy_color`, `coloring(`, `chromatic`, `estimate`, and any `nx.*color*` usage; comments and docstrings must not trip the gate, so strip comment lines before counting (e.g. filter out lines matching `^\s*#`). Positively assert that the only chi path is `n - matching_number(...)` — confirm `invariants/matching.py` contains `max_weight_matching(...maxcardinality=True)` and no coloring import. The gate passes iff the estimate-token count over non-comment source lines is 0.
    Add seed-137 H-only fingerprint (research OQ2, cheap pre-lock for Phase 4): extend `tests/test_fingerprint.py` with `test_seed137_h_only` that regenerates n=31 seed=137 and asserts `m == 177` (Appendix D.3), then compute + freeze its canonical H_edges sha256 into `data/manifests/fingerprint.json` under key `tfp:n31:s137` as `{"m":177,"h_edges_sha256":"<computed>"}` (gate on m==177 before freezing). Do NOT assert its model/ILP (that is Phase 4, CBC — out of scope).
    Add `test_env_smoke` (ENV-01 tripwire): assert `sys.version.split()[0] == "3.12.13"` and `networkx.__version__ == "3.6.1"`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_chi_no_estimate.py -x</automated>
    <automated>uv run pytest tests/test_fingerprint.py::test_seed137_h_only tests/test_fingerprint.py::test_env_smoke -x</automated>
    <automated>uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `test_chi_no_estimate` passes: zero estimate/coloring tokens in non-comment src/ lines; matching.py confirmed as the sole chi path.
    - `test_seed137_h_only` passes: n=31 seed=137 regenerates `|E(H)|==177`; its H_edges sha256 is frozen under `tfp:n31:s137` in the manifest (frozen only after m==177 gate).
    - `test_env_smoke` passes: interpreter is 3.12.13 and networkx is 3.6.1.
    - `uv run pytest -q` (full suite) is GREEN.
  </acceptance_criteria>
  <done>CHI-01 is guarded statically (no estimate anywhere), seed-137's graph identity is pre-locked, and the full fingerprint suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| self-generated golden -> committed fixture | The golden hash is produced by the very code under test; freezing it without an independent gate would bake a porting bug into the "golden" value. |
| stored witness -> regenerated graph | The Appendix D.2 model (external authority) re-verified against our regenerated H is the version-proof arbiter — never a blind JSON byte-diff. |
| src/ control flow -> chi computation | The only permitted chi path is n - nu (Edmonds blossom); any coloring/estimate token crossing this boundary violates CHI-01. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Repudiation | self-certifying golden fixture | mitigate | Freeze the hash ONLY after the doc-derived invariants (131/15/16/tf/maxTF) pass (Task 1 gate); the doc, not our output, is the authority. |
| T-01-02 | Tampering | cross-version determinism drift | mitigate | test_golden_hash is the byte-exact tripwire; test_env_smoke asserts the exact interpreter/networkx. Research verified stable 3.9.6<->3.12.13. |
| T-01-06 | Spoofing | a chi estimate masquerading as exact | mitigate | test_chi_no_estimate statically forbids coloring/greedy/estimate tokens in src/; positively confirms n - matching_number is the sole path. |
| T-01-07 | Tampering | comment prose self-invalidating the grep gate | mitigate | The static guard strips comment lines (`^\s*#`) before counting tokens, so header prose cannot trip or mask the gate. |
| T-01-03 | Elevation | verifier assert-stripping under `python -O` | accept (defer) | Phase-1 tests are NOT run under `-O`; verifier hardening + `-O` canary is Phase 2 (VRF-01 / ENV-06). |
</threat_model>

<verification>
- Regenerated n=31 seed=1 H_edges sha256 == golden (ENV-03, byte-exact reproduction).
- Stored Appendix D.2 model re-verifies against regenerated H (ENV-02, version-proof).
- Heuristic solve reproduces a verifying K16 model; matches D.2 exactly on the pinned interpreter (ENV-02).
- No chromatic estimate exists in src/ control flow; chi is n - nu only (CHI-01).
- Full `uv run pytest -q` green; interpreter/networkx pins asserted (ENV-01).
</verification>

<success_criteria>
- `data/manifests/fingerprint.json` frozen with the golden n=31 s=1 hash (+ seed-137 H-only entry), gated on doc invariants.
- Two-tier fingerprint test green: invariants + golden hash + stored-model re-verify (hard tier) and heuristic replay (soft tier + pinned-env exact-match).
- CHI-01 static guard green: no estimate anywhere in src/.
- Full suite green on the pinned CPython 3.12.13 / networkx 3.6.1.
</success_criteria>

<output>
Create `.planning/phases/01-pinned-environment-verbatim-port/01-02-SUMMARY.md` when done.
</output>
