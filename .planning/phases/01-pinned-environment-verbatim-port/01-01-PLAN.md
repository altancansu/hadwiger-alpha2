---
phase: 01-pinned-environment-verbatim-port
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - .python-version
  - uv.lock
  - .gitignore
  - src/alpha2/__init__.py
  - src/alpha2/paths.py
  - src/alpha2/generators/__init__.py
  - src/alpha2/generators/tfp.py
  - src/alpha2/invariants/__init__.py
  - src/alpha2/invariants/matching.py
  - src/alpha2/search/__init__.py
  - src/alpha2/search/heuristic.py
  - src/alpha2/verify/__init__.py
  - src/alpha2/verify/model.py
  - src/alpha2/repro/__init__.py
  - src/alpha2/repro/baseline.py
  - tests/__init__.py
  - tests/test_fingerprint.py
autonomous: true
requirements: [ENV-01, ENV-02, CHI-01]

must_haves:
  truths:
    - "A clean `uv sync` resolves CPython 3.12.13 and installs networkx 3.6.1 / pulp 3.3.2 / ortools 9.15.6755 from a committed uv.lock; system Python 3.9.6 is never selected."
    - "The Appendix C toolkit lives in a repo-relative src-layout package (src/alpha2/...) with no /mnt paths; algorithm bodies are byte-identical to the reference source."
    - "Running the baseline entry regenerates n=31 seed=1 and yields |E(H)|=131, nu(H)=15, chi(G)=16 with H_triangle_free and H_edge_maximal both True."
    - "chi(G) is computed only as n - nu via networkx max_weight_matching(maxcardinality=True); no chromatic estimate exists in control flow."
    - "The invariant fingerprint test (m==131, nu==15, chi==16) is GREEN on the pinned interpreter."
  artifacts:
    - path: "pyproject.toml"
      provides: "uv project: requires-python >=3.12,<3.13; core deps + [nauty] extra + dev group"
      contains: "pulp==3.3.2"
    - path: ".python-version"
      provides: "exact-patch interpreter anchor for uv"
      contains: "3.12.13"
    - path: "uv.lock"
      provides: "committed lockfile pinning networkx/pulp/ortools (+ pynauty extra)"
      min_lines: 50
    - path: "src/alpha2/generators/tfp.py"
      provides: "RandomSet, triangle_free_process, is_triangle_free, is_edge_maximal_tf (verbatim)"
      exports: ["RandomSet", "triangle_free_process", "is_triangle_free", "is_edge_maximal_tf"]
    - path: "src/alpha2/invariants/matching.py"
      provides: "matching_number -> nu; the ONLY chi computation path (CHI-01)"
      exports: ["matching_number"]
    - path: "src/alpha2/search/heuristic.py"
      provides: "is_conflict, full_conflicts, update_conflicts, initial_state, assignments, valid_groups, cand_energy, solve (verbatim)"
      exports: ["solve", "is_conflict"]
    - path: "src/alpha2/verify/model.py"
      provides: "verify_model (verbatim, assert-based in Phase 1) + own is_conflict copy"
      exports: ["verify_model"]
    - path: "src/alpha2/repro/baseline.py"
      provides: "run_instance + baseline driver writing to paths.CORPUS (repo-relative)"
      exports: ["run_instance", "main"]
    - path: "src/alpha2/paths.py"
      provides: "single source of repo-relative paths (replaces /mnt/...)"
      exports: ["CORPUS"]
  key_links:
    - from: "src/alpha2/invariants/matching.py"
      to: "networkx.max_weight_matching"
      via: "nx.max_weight_matching(Hg, maxcardinality=True)"
      pattern: "max_weight_matching.*maxcardinality=True"
    - from: "src/alpha2/repro/baseline.py"
      to: "src/alpha2/paths.py"
      via: "CORPUS repo-relative path (no /mnt)"
      pattern: "paths\\.CORPUS|from .*paths import"
    - from: "tests/test_fingerprint.py"
      to: "src/alpha2/generators/tfp.py"
      via: "regenerate n=31 seed=1 and assert invariants"
      pattern: "triangle_free_process|matching_number"
---

<objective>
Stand up the pinned uv environment and port the Appendix C toolkit verbatim into a repo-relative src-layout package, so the n=31 seed=1 instance regenerates on CPython 3.12.13 and produces its historical invariants (|E(H)|=131, nu=15, chi=16). This is the Walking Skeleton spine: env scaffold -> toolkit imports and runs -> the invariant fingerprint assertion goes green.

Purpose: Every downstream phase (the verifier, corpus, exact backends, kill battery) builds on this locked, byte-reproducing foundation. If the port perturbs set-iteration order or the pins drift, all corpus reproduction downstream is corrupted.
Output: A committed uv.lock + src/alpha2/ package with verbatim algorithm bodies + a passing invariant fingerprint test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/reference/alpha2-program-source.md
@.planning/phases/01-pinned-environment-verbatim-port/01-RESEARCH.md
@.planning/phases/01-pinned-environment-verbatim-port/01-VALIDATION.md

<critical_constraints>
- The port is a PATHS-AND-IMPORTS change ONLY. Copy each function/class body character-for-character from `.planning/reference/alpha2-program-source.md` Appendix C. Do NOT refactor, rename, reformat comprehensions, merge/split loops, or reorder set expressions. Byte-reproduction depends on CPython set-iteration order, which depends on the exact code — moving a function to another module is safe; editing its body is not.
- The two determinism-sensitive expressions that MUST be preserved character-for-character:
  1. `triangle_free_process`: the `for w in adj[v]:` / `for w in adj[u]:` loops iterating int-sets, and the `RandomSet` swap-with-last layout in `discard`.
  2. `solve`: `pr = tuple(conf)[rng.randrange(len(conf))]` (iterating a set of int-tuples).
- Preserve the single-RNG contract (Contract v1): `run_instance` seeds exactly one `random.Random(seed)` and lets `triangle_free_process` THEN `solve` consume that same stream in that order. Do NOT introduce per-stage subseeds.
- Do NOT make pulp / ortools / pynauty imported at package top level or exercised by Phase 1 — they are LOCKED (in the lockfile) but NOT exercised by the fingerprint path. Only networkx runs.
- Every `/mnt/user-data/outputs/hadwiger_alpha2_certificates.json` occurrence becomes `paths.CORPUS` (repo-relative). No absolute path may appear anywhere but paths.py.
- Do NOT run `ruff --fix` / `ruff format` across generators/tfp.py or search/heuristic.py. Lint-only; never auto-rewrite the determinism-sensitive modules.
- pynauty is an OPTIONAL `nauty` extra (research Assumption A2): default `uv sync` installs core only. nauty is a brew binary and CANNOT live in uv.lock. Do NOT add pynauty to core deps.
</critical_constraints>

<packaging_contract>
Verified working this session by the researcher (01-RESEARCH.md, Code Examples):

pyproject.toml:
  [project] name = "alpha2"; requires-python = ">=3.12,<3.13"
  dependencies = ["networkx==3.6.1", "pulp==3.3.2", "ortools==9.15.6755"]
  [project.optional-dependencies] nauty = ["pynauty==2.8.8.1"]; dev = ["pytest", "ruff"]
  [tool.pytest.ini_options] with testpaths = ["tests"] and src-layout on the path
  src-layout build config pointing at src/alpha2

.python-version:
  3.12.13
</packaging_contract>
</context>

<tasks>

<task type="auto">
  <name>Task 1: uv scaffold + src package skeleton + failing invariant fingerprint test</name>
  <files>pyproject.toml, .python-version, uv.lock, .gitignore, src/alpha2/__init__.py, src/alpha2/paths.py, src/alpha2/generators/__init__.py, src/alpha2/invariants/__init__.py, src/alpha2/search/__init__.py, src/alpha2/verify/__init__.py, src/alpha2/repro/__init__.py, tests/__init__.py, tests/test_fingerprint.py</files>
  <read_first>
    - .planning/phases/01-pinned-environment-verbatim-port/01-RESEARCH.md (Standard Stack; Code Examples: the verified pyproject.toml; Validation Architecture: test map)
    - .planning/phases/01-pinned-environment-verbatim-port/01-VALIDATION.md (Wave 0 requirements)
    - CLAUDE.md (Technology Stack: exact pins, "What NOT to use")
  </read_first>
  <action>
    Initialize the uv project at the repo root. Create pyproject.toml per the packaging_contract above: name `alpha2`, requires-python `>=3.12,<3.13`, core dependencies `networkx==3.6.1`, `pulp==3.3.2`, `ortools==9.15.6755`; optional-dependency group `nauty = ["pynauty==2.8.8.1"]`; dev group `["pytest", "ruff"]`; a src-layout build backend; `[tool.pytest.ini_options]` with `testpaths=["tests"]` and src on the path. Create `.python-version` containing exactly `3.12.13`. Run `uv python install 3.12.13`, then `uv lock` to produce the committed uv.lock, then `uv sync` (core only) and `uv sync --group dev` for pytest/ruff. Create a `.gitignore` covering `.venv/`, `__pycache__/`, `data/corpus/*.json`, `*.pyc`.
    Create the package skeleton: empty `src/alpha2/__init__.py` and an empty `__init__.py` in each of generators/, invariants/, search/, verify/, repro/, and tests/. Create `src/alpha2/paths.py` defining `CORPUS` as a repo-relative path (resolve repo root from `__file__`, point at `data/corpus/hadwiger_alpha2_certificates.json`) with a helper that ensures the parent dir exists; also create `data/corpus/.gitkeep` and `data/manifests/.gitkeep`.
    Create `tests/test_fingerprint.py` with `test_invariants` that regenerates n=31 seed=1 by importing `triangle_free_process`, `is_triangle_free`, `is_edge_maximal_tf` from `alpha2.generators.tfp` and `matching_number` from `alpha2.invariants.matching`, then asserts `m == 131`, `matching_number(adj, 31) == 15`, `31 - matching_number(adj, 31) == 16`, `is_triangle_free(adj, 31) is True`, `is_edge_maximal_tf(adj, 31) is True`. These modules do not exist yet, so the test MUST fail with ImportError (RED). Source the numbers 131/15/16 from Appendix D.2 (the doc), not from any generated output.
  </action>
  <verify>
    <automated>uv sync && uv run python -c "import sys; assert sys.version.split()[0]=='3.12.13', sys.version; import networkx; assert networkx.__version__=='3.6.1', networkx.__version__; print('env OK')"</automated>
    <automated>uv run pytest tests/test_fingerprint.py::test_invariants -x 2>&1 | grep -Eq 'ImportError|ModuleNotFoundError|error' && echo 'RED as expected'</automated>
  </verify>
  <acceptance_criteria>
    - `uv sync` succeeds on a clean resolve and selects CPython 3.12.13 (NOT system 3.9.6); `networkx.__version__ == "3.6.1"`.
    - uv.lock exists, is committed, and pins networkx 3.6.1, pulp 3.3.2, ortools 9.15.6755 (grep uv.lock for each version string).
    - pyproject.toml contains `pulp==3.3.2` (hard pin) and lists pynauty ONLY under the `nauty` optional group, NOT in core dependencies.
    - `.python-version` contains exactly `3.12.13`.
    - `tests/test_fingerprint.py::test_invariants` FAILS with ImportError/ModuleNotFoundError (RED — the port does not exist yet).
    - No absolute/`/mnt` path exists outside `src/alpha2/paths.py`.
  </acceptance_criteria>
  <done>uv-pinned env reproduces from committed lockfile; package skeleton + paths.py exist; the invariant fingerprint test is RED pending the port.</done>
</task>

<task type="auto">
  <name>Task 2: Verbatim port — generation + matching core (ENV-02, CHI-01)</name>
  <files>src/alpha2/generators/tfp.py, src/alpha2/invariants/matching.py</files>
  <read_first>
    - .planning/reference/alpha2-program-source.md (C.1: RandomSet, triangle_free_process, is_triangle_free, is_edge_maximal_tf, matching_number — COPY THESE BODIES VERBATIM)
    - .planning/phases/01-pinned-environment-verbatim-port/01-RESEARCH.md (Architecture Pattern 1: the library/entry cut; determinism-safety notes)
    - src/alpha2/paths.py (import conventions)
  </read_first>
  <action>
    Copy into `src/alpha2/generators/tfp.py`, VERBATIM from Appendix C.1, the class `RandomSet` (with `__slots__`, add/discard swap-with-last, random, __len__) and the functions `triangle_free_process(n, rng)`, `is_triangle_free(adj, n)`, `is_edge_maximal_tf(adj, n)`. Keep only the imports actually used (`random`, `itertools` as needed); do NOT import networkx here. Do NOT alter loop order, comprehensions, or the RandomSet layout.
    Copy into `src/alpha2/invariants/matching.py`, VERBATIM from Appendix C.1, `matching_number(adj, n)` — including its local `import networkx as nx` and the exact call `nx.max_weight_matching(Hg, maxcardinality=True)` followed by `return len(M)`. networkx is confined to this module. This is the ONLY chi computation path (CHI-01): chi = n - matching_number(adj, n); there is no coloring/greedy/estimate call anywhere.
    Change ONLY module location and cross-module import statements. No body edits.
  </action>
  <verify>
    <automated>uv run python -c "import random; from alpha2.generators.tfp import triangle_free_process, is_triangle_free, is_edge_maximal_tf; from alpha2.invariants.matching import matching_number; adj,m=triangle_free_process(31, random.Random(1)); nu=matching_number(adj,31); assert m==131,('m',m); assert nu==15,('nu',nu); assert 31-nu==16,('chi',31-nu); assert is_triangle_free(adj,31); assert is_edge_maximal_tf(adj,31); print('core OK: m=131 nu=15 chi=16')"</automated>
    <automated>grep -q 'maxcardinality=True' src/alpha2/invariants/matching.py && ! grep -Rqi 'import networkx' src/alpha2/generators/tfp.py && echo 'networkx confined to matching.py'</automated>
  </verify>
  <acceptance_criteria>
    - Regenerating n=31 seed=1 via `triangle_free_process(31, random.Random(1))` yields `m == 131`.
    - `matching_number(adj, 31) == 15`, so `chi = 31 - 15 == 16`.
    - `is_triangle_free(adj, 31)` is True and `is_edge_maximal_tf(adj, 31)` is True.
    - `matching_number` uses `nx.max_weight_matching(Hg, maxcardinality=True)`; no other module imports networkx.
    - RandomSet and the two determinism-sensitive loops are byte-identical to Appendix C.1 (no reformatting).
  </acceptance_criteria>
  <done>Generation + matching core reproduces the n=31 seed=1 invariants (131/15/16) on the pinned interpreter; chi is n-nu only.</done>
</task>

<task type="auto">
  <name>Task 3: Verbatim port — search + verify + thin entry; invariant fingerprint GREEN (ENV-02)</name>
  <files>src/alpha2/search/heuristic.py, src/alpha2/verify/model.py, src/alpha2/repro/baseline.py</files>
  <read_first>
    - .planning/reference/alpha2-program-source.md (C.1: is_conflict, full_conflicts, update_conflicts, initial_state, assignments, valid_groups, cand_energy, solve, verify_model, run_instance, main — COPY VERBATIM)
    - .planning/phases/01-pinned-environment-verbatim-port/01-RESEARCH.md (Architecture Patterns 1/2/3: library/entry cut, single-RNG contract v1, verifier keeps its own is_conflict)
    - src/alpha2/paths.py (CORPUS repo-relative path)
    - tests/test_fingerprint.py (the RED test this task turns green)
  </read_first>
  <action>
    Copy into `src/alpha2/search/heuristic.py`, VERBATIM from Appendix C.1: `is_conflict`, `full_conflicts`, `update_conflicts`, `initial_state`, `assignments`, `valid_groups`, `cand_energy`, `solve` (with `import time, itertools` as needed). Preserve `pr = tuple(conf)[rng.randrange(len(conf))]` exactly.
    Copy into `src/alpha2/verify/model.py`, VERBATIM: `verify_model(sets, adj, n, k)` (assert-based — do NOT harden to exceptions; that is Phase 2). Include a byte-identical private copy of `is_conflict` in this module so the verifier imports nothing from `search` (trust-root import boundary; research Pattern 3). Do NOT import from alpha2.search here.
    Copy into `src/alpha2/repro/baseline.py`: `run_instance(n, seed, records)` and the baseline driver from `main()` (the 12-instance `instances` list, the loop, the JSON dump, the stdout summary). Change the output path from `/mnt/user-data/outputs/hadwiger_alpha2_certificates.json` to `paths.CORPUS`, creating the parent dir if needed. Import `triangle_free_process`, `is_triangle_free`, `is_edge_maximal_tf` from `alpha2.generators.tfp`; `matching_number` from `alpha2.invariants.matching`; `solve` from `alpha2.search.heuristic`; `verify_model` from `alpha2.verify.model`. Preserve the single-RNG contract v1 (one `random.Random(seed)`, process then solve). Expose `main()` and guard with `if __name__ == "__main__": main()` so `python -m alpha2.repro.baseline` runs.
    The RED `test_invariants` from Task 1 now imports successfully and passes.
  </action>
  <verify>
    <automated>uv run pytest tests/test_fingerprint.py::test_invariants -x</automated>
    <automated>uv run python -m alpha2.repro.baseline 2>&1 | grep -Eq "n=  31 seed=  1 .*chi\(G\)=  16" && echo "baseline ran; n=31 s=1 chi=16"</automated>
    <automated>test -f data/corpus/hadwiger_alpha2_certificates.json && uv run python -c "import json; recs=json.load(open('data/corpus/hadwiger_alpha2_certificates.json')); r=[x for x in recs if x['n']==31 and x['seed']==1][0]; assert r['verified'] is True; assert r['chi_G']==16; assert r['matching_number_H']==15; assert len(r['H_edges'])==131; print('cert OK: verified K16 model, |E(H)|=131')"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_fingerprint.py::test_invariants -x` is GREEN.
    - `uv run python -m alpha2.repro.baseline` runs end-to-end, prints the n=31 seed=1 line with `chi(G)=  16`, and writes `data/corpus/hadwiger_alpha2_certificates.json` (repo-relative, no /mnt).
    - The n=31 seed=1 stored record has `verified==True`, `chi_G==16`, `matching_number_H==15`, and `len(H_edges)==131`.
    - `verify/model.py` contains its own `is_conflict` and imports nothing from `alpha2.search` (grep confirms no `from alpha2.search` / `import ...search`).
    - Search + verify bodies are byte-identical to Appendix C.1; the single-RNG contract v1 is preserved in run_instance.
  </acceptance_criteria>
  <done>The full toolkit runs from the repo on the pinned interpreter; n=31 seed=1 produces a verified K16 model and the invariant fingerprint is GREEN. Walking Skeleton spine complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package pins -> installed environment | uv.lock + .python-version are the only guard against silent dependency/interpreter drift; a wrong resolve corrupts every downstream corpus value. |
| reference source -> ported module | Verbatim copy is the boundary where a body edit could silently change set-iteration order and break byte-reproduction. |
| self-generated output -> claimed fingerprint | The port's own output must be gated against doc-derived invariants, never trusted as its own authority (golden freeze is Plan 02). |

This is an offline mathematical research harness: no network surface, no untrusted input, no auth, no PII. Classical ASVS categories V2/V3/V4 are N/A. The domain-appropriate integrity threat is epistemic drift of the trust chain (research, Security Domain).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | dependency resolution (uv sync) | mitigate | Exact pins in pyproject (`pulp==3.3.2`, networkx/ortools ==) + committed uv.lock + `.python-version=3.12.13`; Task 1 verify asserts `sys.version==3.12.13` and `networkx.__version__==3.6.1`. |
| T-01-02 | Tampering | cross-version set-iteration determinism | mitigate | Pin exact CPython patch; port bodies verbatim (no reformatting of tfp.py/heuristic.py); invariant fingerprint (Task 3) is the tripwire. Research verified the seed-1 hash is stable 3.9.6<->3.12.13. |
| T-01-03 | Elevation | verifier assert-stripping under `python -O` | accept (defer) | Phase 1 ports verify_model verbatim (assert-based) per research Pattern 3; hardening to real checks + a `-O` canary is Phase 2 (VRF-01). Do NOT run Phase-1 tests under `-O`. |
| T-01-04 | Repudiation | port silently importing an unlocked/wrong package | mitigate | Only networkx runs in the fingerprint path; pulp/ortools/pynauty stay unimported at top level; grep confirms networkx confined to matching.py. |
| T-01-SC | Tampering | pip/uv package installs | accept | 01-RESEARCH.md Package Legitimacy Audit approved all four packages (networkx/pulp/ortools/pynauty) by direct install+import+registry inspection; no [ASSUMED]/[SUS]/[SLOP] — no blocking human checkpoint required. |
</threat_model>

<verification>
- `uv sync` on a clean checkout resolves 3.12.13 and installs core deps from committed uv.lock (ENV-01).
- `uv run python -m alpha2.repro.baseline` runs the toolkit from the repo (no /mnt) and produces a verified n=31 seed=1 K16 model (ENV-02).
- n=31 seed=1 reproduces |E(H)|=131, nu=15, chi=16 (the invariant fingerprint) — GREEN (ENV-02 / feeds ENV-03).
- chi is computed only as n - matching_number via Edmonds blossom; networkx is confined to invariants/matching.py (CHI-01).
</verification>

<success_criteria>
- Committed uv.lock reproduces CPython 3.12.13 + networkx 3.6.1 / pulp 3.3.2 / ortools 9.15.6755; system 3.9.6 untouched.
- src/alpha2/ src-layout package holds the verbatim Appendix C toolkit split into library modules + a thin repro entry; algorithm bodies unchanged.
- `tests/test_fingerprint.py::test_invariants` is GREEN (131/15/16/tf/maxTF).
- No estimate/coloring path exists for chi anywhere in src/ (only n - nu).
</success_criteria>

<output>
Create `.planning/phases/01-pinned-environment-verbatim-port/01-01-SUMMARY.md` when done.
</output>
