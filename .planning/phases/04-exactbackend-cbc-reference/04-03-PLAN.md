---
phase: 04-exactbackend-cbc-reference
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - tests/test_had2_problem.py
autonomous: true
requirements: [EXACT-02]
must_haves:
  truths:
    - "The obstruction enumeration is SET-equal (not merely count-equal) to the naive O(|E_G|^2) loop at n=31 on BOTH seeds 1 and 137 — identical constraint semantics proven, with frozenset dedup on C4 diagonals"
    - "The structural checksum triples match the empirically-pinned literals: seed-1 (131, 998, 726) and seed-137 (177, 1913, 3782) for (single-single, single-pair, pair-pair)"
    - "A mutated conflict set makes the checksum gate raise ChecksumError (raise-based, never assert) — the gate is proven non-vacuous"
    - "Exhaustive brute-force had2 agrees with CBC optimize on every n<=8 differential instance — the enumeration+adapter pipeline has no small-case semantic bug"
  artifacts:
    - path: "tests/test_had2_problem.py"
      provides: "Set-equality differential vs test-local naive loops; checksum literals; ChecksumError mutation; brute-force n<=8 differential"
      contains: "ChecksumError"
  key_links:
    - from: "tests/test_had2_problem.py"
      to: "alpha2.solvers.problems.had2.build_had2_problem"
      via: "enumeration under test vs test-local naive reference"
      pattern: "build_had2_problem|enumerate_had2"
    - from: "tests/test_had2_problem.py"
      to: "alpha2.generators.tfp.triangle_free_process"
      via: "regenerate seed-1/seed-137 H (read-only frozen generator)"
      pattern: "triangle_free_process\\(31"
---

<objective>
Prove ROADMAP SC3: the obstruction-based constraint generation (C4s/cherries/H-edges
of triangle-free H) is semantically identical to the legacy O(|E_G|^2) loop — by
SET-equality at n=31 on seeds 1 and 137 (LOCKED: set-equal, not count-equal, with
frozenset dedup on C4 diagonals), by the empirically-pinned checksum literals, by a
mutation test proving the gate raises, and by an exhaustive n<=8 brute-force
differential against CBC optimize.

Purpose: obstruction under-generation is the radioactive direction — a silently
dropped conflict class inflates had2 and fabricates models that the verifier would
catch, but a subtler under-count could survive to a WRONG proven optimum on
impossibility-flavored questions. The naive loops from Appendix C.4 live ONLY as
test-local reference code here; they never become a production path.
Output: tests/test_had2_problem.py, green, in the every-commit tier.
</objective>

<phase_goal>
**As the** α = 2 program author, **I want** exact had₂ solving behind a status-honest
interface with CBC as the reference engine, **so that** a timed-out incumbent can never
masquerade as an exact had₂ and every exact claim is independently verified before it counts.
</phase_goal>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-exactbackend-cbc-reference/04-RESEARCH.md
@.planning/phases/04-exactbackend-cbc-reference/04-PATTERNS.md

<interfaces>
<!-- From plan 04-01 (wave 1, complete before this plan runs): -->
From src/alpha2/solvers/problems/had2.py:
  enumerate_had2(adj, n) -> (Gedges, ss, sp, pp)   # ss: set of (u,v) u<v; sp: set of (v,(a,b)); pp: set of frozenset({e1,e2})
  build_had2_problem(adj, n) -> Had2Problem        # triangle-free re-check + checksum gate, raises ChecksumError
  ChecksumError

From src/alpha2/solvers/backend.py:
  get_backend("cbc").solve_had2(adj, n, mode="optimize", params=SolveParams(...)) -> ExactOutcome

From src/alpha2/generators/tfp.py (FROZEN, read-only):
  triangle_free_process(n, rng) -> (adj, m)

LOCKED regression constants (RESEARCH, live-verified):
  checksum formulas: nss = |E(H)|, nsp = sum_v C(deg_H(v), 2), npp = (1/2) sum_{u<v} C(codeg_H(u,v), 2)
  seed-1  (n=31): (nss, nsp, npp) == (131, 998, 726)
  seed-137 (n=31): (nss, nsp, npp) == (177, 1913, 3782)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: set-equality differential vs naive loops + checksum literals + mutation raise</name>
  <files>tests/test_had2_problem.py</files>
  <read_first>
    - .planning/reference/alpha2-program-source.md (Appendix C.4 lines 516–534 — THE naive-loop reference to adapt test-locally: pair-pair scan over ordered i<j G-edge pairs with the `len({a,b,c,d}) < 4` skip and 4-way adjacency test; single-pair scan over (v, G-edge); single-single over H-edges. Adapt to COLLECT sets instead of adding constraints; canonicalize pair-pair entries as frozenset({e1,e2}) so they compare against the enumeration's dedup convention)
    - .planning/phases/04-exactbackend-cbc-reference/04-RESEARCH.md (Pattern 2 lines 190–234 — the constraint-class/H-substructure table with the pinned counts; Pitfall 3 lines 312–314 — the C4 double-count failure mode this test must be able to catch; Pitfall 4 lines 316–318 — H/G adjacency flip shows up as nss counting ~334 instead of 177)
    - .planning/phases/04-exactbackend-cbc-reference/04-PATTERNS.md (section `tests/test_had2_problem.py` lines 166–181 — regenerate-from-seed pattern + embedded-literal discipline)
    - src/alpha2/solvers/problems/had2.py (the module under test — its exact set-element conventions, created in 04-01)
  </read_first>
  <behavior>
    - Test 1 (set-equality, seeds 1 and 137): regenerate H via `triangle_free_process(31, random.Random(seed))` for seed in (1, 137); run `enumerate_had2(adj, 31)` AND the test-local naive loops; assert ss_enum == ss_naive, sp_enum == sp_naive, pp_enum == pp_naive as SETS (LOCKED: set-equality, not count-equality — a count match with different members would be a masked bug).
    - Test 2 (checksum literals): assert (len(ss), len(sp), len(pp)) == (131, 998, 726) for seed 1 and == (177, 1913, 3782) for seed 137 — literals embedded in-file.
    - Test 3 (mutation -> raise): on seed-1's enumeration, drop one element from each conflict class in turn and drive the checksum gate; each mutation raises ChecksumError (pytest.raises) — proves the gate is a real raise-based mechanism, not an optimization-strippable statement.
    - Test 4 (triangle-containing H refused): build a tiny H containing a triangle (e.g. edges 0-1, 1-2, 0-2 on n=4) and assert build_had2_problem raises (the enumeration is only total on triangle-free H).
  </behavior>
  <action>
    Create tests/test_had2_problem.py. Embed the Appendix C.4 loops as private test-local helpers (adapted to collect (ss, sp, pp) sets with the same element conventions as enumerate_had2: (u,v) u<v for ss; (v,(a,b)) with a<b for sp; frozenset of two G-edge tuples for pp). The naive helpers must remain test-only — nothing imports them from production code. Embed the two checksum triples and the formulas' expected values as in-file literals (no cross-test imports). For the mutation test, call whatever checksum-gate entry had2.py exposes (the gate inside build_had2_problem, or a public checksum function if 04-01 factored one) with the mutated sets; if the gate is only reachable through build_had2_problem, monkeypatch-free approach: recompute expectations locally and assert had2.py raises when handed an adj whose enumeration you tampered via a thin wrapper — prefer exposing the gate as a module-level function in had2.py if it is not already (additive, no behavior change).
  </action>
  <verify>
    <automated>uv run --frozen --extra dev pytest tests/test_had2_problem.py -q -k "equality or checksum or mutation or triangle"</automated>
  </verify>
  <acceptance_criteria>
    - Set-equality holds on seeds 1 AND 137 at n=31 for all three conflict classes (test names make the seed explicit).
    - Checksum literals (131, 998, 726) / (177, 1913, 3782) asserted and green.
    - Each single-element mutation raises ChecksumError; the triangle-containing H raises at build.
    - The naive loops exist only inside tests/test_had2_problem.py: `grep -rl "def _naive" src/alpha2/` returns nothing.
  </acceptance_criteria>
  <done>The enumeration is proven semantically identical to the legacy loop on the two anchor instances, the pinned checksums hold, and the gate provably raises on any dropped conflict.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: exhaustive brute-force differential at n<=8</name>
  <files>tests/test_had2_problem.py</files>
  <read_first>
    - .planning/phases/04-exactbackend-cbc-reference/04-RESEARCH.md (Pattern 6 lines 279–287 — the brute-force differential is "Pitfall 1's decisive test" for the fast panel; Pattern 2's domain sanity line 234 — had2 >= omega(G) = alpha(H) always)
    - src/alpha2/solvers/backend.py + src/alpha2/solvers/result.py (get_backend/SolveParams/Status API from 04-01)
    - src/alpha2/corpus/verifier.py (_is_conflict lines 34–47, READ-ONLY — the independent conflict semantics; do not import it into the brute force, re-derive locally: two branch sets are mutually usable iff disjoint AND joined by at least one G-edge)
  </read_first>
  <behavior>
    - Test 5 (exhaustive differential): for a deterministic panel of small triangle-free H instances — at minimum: C5 (n=5), empty H (n=5), a perfect matching H (n=6, edges 0-1/2-3/4-5), and `triangle_free_process(n, random.Random(s))` for (n, s) in a fixed list covering n in {6, 7, 8} with 2 seeds each — compute had2 two ways and assert equality: (a) test-local exhaustive reference: candidates = all singletons + all G-edges; compatibility(A, B) = disjoint AND some a in A, b in B with b not in adj_H[a] and a != b (i.e., ab is a G-edge); had2_brute = max clique in the candidate-compatibility graph found by plain recursive branch-and-bound (candidate count <= 8 + C(8,2) = 36 — trivially exhaustive, no heuristics); (b) `get_backend("cbc").solve_had2(adj, n, mode="optimize")` -> status must be PROVED_OPTIMAL and exact_value() == had2_brute.
    - Test 6 (domain sanity): on every panel instance, exact_value() >= size of any independent set of H found greedily (had2 >= alpha(H) = omega(G) — singletons on a max clique of G are always a valid family); implement the alpha(H) lower-bound witness exhaustively at these sizes.
  </behavior>
  <action>
    Extend tests/test_had2_problem.py with the brute-force reference (test-local, exhaustive, no pruning cleverness beyond disjointness — correctness over speed at n<=8) and the differential + domain-sanity tests per the behavior block. The brute force must NOT import anything from alpha2.solvers.problems.had2 (independent semantics — that is the point of a differential); it may import the frozen generator and the backend under test. Keep total runtime for the file under ~30 s (each CBC solve at n<=8 is milliseconds).
  </action>
  <verify>
    <automated>uv run --frozen --extra dev pytest tests/test_had2_problem.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run --frozen --extra dev pytest tests/test_had2_problem.py -q` exits 0 with all differential instances agreeing: brute == CBC PROVED_OPTIMAL value on every panel member (>= 9 instances incl. C5, empty, perfect matching).
    - Every CBC leg asserts status == PROVED_OPTIMAL before reading exact_value() (no ungated value reads even in tests).
    - had2 >= alpha(H) holds on every instance.
    - Full suite green: `uv run --frozen --extra dev pytest -q -m "not slow"` exits 0; frozen files untouched: `git diff --quiet src/alpha2/corpus src/alpha2/repro src/alpha2/generators data/` exits 0.
  </acceptance_criteria>
  <done>An independent exhaustive reference agrees with the obstruction+CBC pipeline on every small instance — the enumeration semantics are pinned from both sides (naive-loop equality above, brute-force value equality here).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| enumeration -> ILP model | A dropped/duplicated conflict constraint silently changes the feasible region — the wrong-optimum vector. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-04 | Tampering | obstruction under-generation (wrong optimum) | mitigate | SET-equality vs the Appendix C.4 naive loops at n=31 on seeds 1 & 137; checksum literals (131,998,726)/(177,1913,3782); mutation test proves the gate raises on any single dropped conflict; brute-force n<=8 differential pins the VALUE from an independent exhaustive reference. |
| T-4-07 | Tampering | H/G adjacency flip or C4 double-count regression | mitigate | The checksum class-level tripwire (nss would count ~334 G-edges, npp would double) + set-equality both fail loudly; triangle-containing-H refusal proves the totality precondition is enforced. |
| T-4-05 | Elevation | gate stripped under -O | mitigate | ChecksumError is raise-based (never assert); plan 04-02's -O canary exercises it live; zero-assert AST guard covers had2.py. |
| T-4-SC | Tampering | supply chain | accept | No package installs this phase; test-only file added. |
</threat_model>

<verification>
- `uv run --frozen --extra dev pytest tests/test_had2_problem.py -q` green (< ~30 s, every-commit tier).
- Full suite green: `uv run --frozen --extra dev pytest -q -m "not slow"`.
- `git diff --quiet src/alpha2/corpus src/alpha2/repro src/alpha2/generators data/` — frozen artifacts untouched.
</verification>

<success_criteria>
- ROADMAP SC3 proven: obstruction enumeration replaces the O(|E_G|^2) loop with set-equality demonstrated at n=31 (seeds 1 & 137) and the structural checksum validating every build, raise-based.
- The pipeline's VALUES are independently pinned at n<=8 by exhaustive brute force.
- The naive loop survives only as test-local reference code.
</success_criteria>

<output>
Create `.planning/phases/04-exactbackend-cbc-reference/04-03-SUMMARY.md` when done.
</output>
