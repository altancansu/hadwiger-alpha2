---
phase: 04-exactbackend-cbc-reference
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/alpha2/solvers/__init__.py
  - src/alpha2/solvers/result.py
  - src/alpha2/solvers/backend.py
  - src/alpha2/solvers/cbc.py
  - src/alpha2/solvers/problems/__init__.py
  - src/alpha2/solvers/problems/had2.py
  - tests/test_solver_result.py
  - tests/test_cbc_backend.py
autonomous: true
requirements: [EXACT-01, EXACT-02]
must_haves:
  truths:
    - "A tiny real instance (H=C5) solves end-to-end to PROVED_OPTIMAL with had2=3 and its family passes verify_certificate — the full slice: enumeration -> CBC -> status gate -> trust root"
    - "exact_value() raises NotProvedOptimal for every status except PROVED_OPTIMAL — 'had2 < chi from an incumbent' is unrepresentable at the type level"
    - "PROVED_OPTIMAL is assigned ONLY when prob.status == LpStatusOptimal AND prob.sol_status == LpSolutionOptimal (never prob.status alone)"
    - "Decision and optimize modes both work; every ExactOutcome carries value, bound, and backend_version including the probed CBC binary version 2.10.3"
    - "pulp is imported ONLY by solvers/cbc.py; result.py, backend.py, problems/had2.py are stdlib-only with zero asserts"
  artifacts:
    - path: "src/alpha2/solvers/result.py"
      provides: "Status enum (MODEL_FOUND/PROVED_OPTIMAL/PROVED_INFEASIBLE/INCUMBENT_ONLY/UNKNOWN/ERROR), NotProvedOptimal, SolveParams, frozen ExactOutcome with raising exact_value()"
      contains: "NotProvedOptimal"
    - path: "src/alpha2/solvers/backend.py"
      provides: "ExactBackend Protocol + lazy get_backend registry (stdlib-only)"
      contains: "get_backend"
    - path: "src/alpha2/solvers/problems/had2.py"
      provides: "Obstruction enumeration (H-edges/cherries/C4-diagonals) + raise-based structural checksum gate"
      contains: "ChecksumError"
    - path: "src/alpha2/solvers/cbc.py"
      provides: "PULP_CBC_CMD adapter: two-field status gate, guarded extraction, bound parse, CBC version probe"
      contains: "sol_status"
    - path: "tests/test_cbc_backend.py"
      provides: "End-to-end happy path: C5/empty-H solved PROVED_OPTIMAL, families through verify_certificate; decision-mode legs"
      contains: "verify_certificate"
  key_links:
    - from: "src/alpha2/solvers/cbc.py"
      to: "src/alpha2/solvers/problems/had2.py"
      via: "build_had2_problem consumed to emit conflict rows"
      pattern: "build_had2_problem"
    - from: "src/alpha2/solvers/cbc.py"
      to: "pulp.PULP_CBC_CMD"
      via: "msg=0, threads=1, timeLimit, logPath — the sole pulp import in the repo's new code"
      pattern: "PULP_CBC_CMD"
    - from: "tests/test_cbc_backend.py"
      to: "alpha2.corpus.verifier.verify_certificate"
      via: "solver family routed through an in-memory schema-v1 record"
      pattern: "verify_certificate\\("
---

<objective>
Land the thinnest real vertical slice of Phase 4: the status-honest `ExactBackend`
interface + the CBC adapter, solving ONE real instance (H=C5) end-to-end to
PROVED_OPTIMAL with the family independently verified by the frozen trust root.

This closes the incumbent-as-optimum soundness hole at the TYPE level in the same
plan that first runs a solver: `ExactOutcome.exact_value()` raises unless
`prob.status == LpStatusOptimal AND prob.sol_status == LpSolutionOptimal` — nothing
weaker (LOCKED regression constant; pulp 3.3.2 maps a timed-out incumbent to
`status=Optimal`, so `prob.status` alone is provably insufficient).

Purpose: everything downstream of "family produced" already exists and is
adversarially proven (verify_certificate, build_record, extract_witness). This plan
composes the four new solver modules against those frozen primitives; it does NOT
reimplement verification, hashing, or storage, and it does NOT touch any frozen file.
Output: `src/alpha2/solvers/{result,backend,cbc}.py` + `solvers/problems/had2.py`,
unit tests for the status contract, and a green end-to-end CBC panel on closed-form
instances.
</objective>

<phase_goal>
**As the** α = 2 program author, **I want** exact had₂ solving behind a status-honest
interface with CBC as the reference engine, **so that** a timed-out incumbent can never
masquerade as an exact had₂ and every exact claim is independently verified before it counts.

> Note: the ROADMAP `**Goal:**` line for Phase 4 is outcome-shaped, not in
> `As a / I want to / so that` form. The story above is a faithful restatement
> (status-honest interface + CBC reference + soundness hole closed), not an invented
> capability. If a canonical user story is required, run `/gsd mvp-phase 4` first.
</phase_goal>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-exactbackend-cbc-reference/04-RESEARCH.md
@.planning/phases/04-exactbackend-cbc-reference/04-PATTERNS.md

<interfaces>
<!-- Contracts the executor uses directly — no codebase exploration needed. -->
<!-- FROZEN (read-only, never modified): verifier.py, schema.py, store.py, tfp.py,
     cayley.py, repro/*, verify/model.py, data/corpus/*, data/manifests/*,
     tests/test_corpus_r{1,2,3}.py -->

From src/alpha2/corpus/verifier.py (FROZEN):
  verify_certificate(rec) -> k        # line 213; raises VerificationError on any defect
  VerificationError                   # line 30

From src/alpha2/corpus/schema.py (FROZEN):
  build_record(*, provenance, H_edges, nu_H, chi_G, model_branch_sets, matching_M,
               tutte_berge_U, method, omega_G=None, verified=True, ...) -> dict
                                      # line 246; derives had_2=len(model_branch_sets),
                                      # RAISES if len < chi; reproduction/backends
                                      # auto-attached (overridable per 02-02 decision)
  provenance_seed(family, n, seed, process) -> dict   # line 104

From src/alpha2/invariants/witness.py:
  extract_witness(adj, n) -> (M, U, nu)               # line 35 (emission-time, UNTRUSTED)

From src/alpha2/invariants/matching.py:
  matching_number(adj, n) -> int                      # line 11 (sole blossom call — CHI-01)

From src/alpha2/generators/tfp.py (FROZEN):
  triangle_free_process(n, rng) -> (adj, m)           # line 33; adj is list[set[int]] for H
  is_triangle_free(adj, n) -> bool                    # line 49

pulp 3.3.2 constants (verified against installed source):
  LpStatusOptimal=1, LpStatusNotSolved=0, LpStatusInfeasible=-1, LpStatusUnbounded=-2
  LpSolutionOptimal=1, LpSolutionIntegerFeasible=2, LpSolutionNoSolutionFound=0,
  LpSolutionInfeasible=-1
  prob.sol_status exists on LpProblem after solve (verified live)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — status-contract unit tests + end-to-end happy-path test</name>
  <files>tests/test_solver_result.py, tests/test_cbc_backend.py</files>
  <read_first>
    - .planning/phases/04-exactbackend-cbc-reference/04-RESEARCH.md (Pattern 1 lines 129–188: the status-mapping table + ExactOutcome design; Pattern 3 lines 236–241: decision vs optimize; Pattern 4 lines 243–262: trust-root routing)
    - .planning/phases/04-exactbackend-cbc-reference/04-PATTERNS.md (sections `tests/test_solver_result.py` lines 148–162 and `tests/test_cbc_backend.py` lines 184–208)
    - tests/test_schema_roundtrip.py (analog — `_synthetic_empty_h` lines ~82–97 for tiny closed-form records; refusal-test shape lines ~212–224)
    - src/alpha2/corpus/verifier.py (verify_certificate line 213 — the trust-root signature every family must pass)
    - src/alpha2/corpus/schema.py (build_record line 246, provenance_seed line 104)
    - src/alpha2/invariants/witness.py (extract_witness line 35)
  </read_first>
  <behavior>
    tests/test_solver_result.py (pure unit, imports only alpha2.solvers.result — no pulp, no solver run):
    - Test 1: `exact_value()` raises `NotProvedOptimal` for each of MODEL_FOUND, PROVED_INFEASIBLE, INCUMBENT_ONLY, UNKNOWN, ERROR; returns the int value for PROVED_OPTIMAL.
    - Test 2: `ExactOutcome.__post_init__` raises ValueError when value is not None for UNKNOWN or ERROR.
    - Test 3: `__post_init__` raises ValueError when status is PROVED_OPTIMAL and value != bound.
    - Test 4: ExactOutcome is frozen — assigning to a field raises dataclasses.FrozenInstanceError.
    tests/test_cbc_backend.py (fast integration, uses get_backend("cbc")):
    - Test 5 (mapping table): a pure `map_status(status, sol_status, mode)` in alpha2.solvers.cbc reproduces the full empirically-pinned table: (Optimal, LpSolutionOptimal, optimize)->PROVED_OPTIMAL; (Optimal, LpSolutionIntegerFeasible, optimize)->INCUMBENT_ONLY; (NotSolved, NoSolutionFound, optimize)->UNKNOWN; (Infeasible, *, optimize)->ERROR; (Unbounded, *, optimize)->ERROR; (Optimal, LpSolutionOptimal, decision)->MODEL_FOUND; (Infeasible, *, decision)->PROVED_INFEASIBLE; (NotSolved, *, decision)->UNKNOWN. Use pulp's exported constants, not raw ints.
    - Test 6 (E2E happy path — the MVP slice): H=C5 (adj for edges 0-1,1-2,2-3,3-4,4-0; n=5, nu=2, chi=3): optimize -> status==PROVED_OPTIMAL, exact_value()==3, bound==3, len(family)==3; then extract_witness(adj,5) -> build_record(provenance_params or provenance_seed, method="exact ILP (CBC): had_2(G)=3") -> verify_certificate(rec)==3. Use provenance_params(family="synthetic_c5", n=5, params={"cycle": 5}).
    - Test 7 (closed form): H=empty n=5 (G=K5): optimize -> PROVED_OPTIMAL, exact_value()==5, family=5 singletons, verify_certificate green (chi=5, nu=0, M=[], U=[]).
    - Test 8 (both modes, SC4): C5 decision target_k=3 -> MODEL_FOUND with len(family)>=3; C5 decision target_k=4 -> PROVED_INFEASIBLE with value None and family None. Every outcome in tests 6–8 asserts backend_version contains both "pulp==3.3.2" and "2.10.3", and wall_time_s > 0.
    - RED now: both files fail at import (alpha2.solvers does not exist).
  </behavior>
  <action>
    Create the two test files per the behavior block. Embed the C5 and empty-H adjacencies as in-file literals (embedded-literal discipline, tests/test_corpus_r1.py precedent — no cross-test imports). Route every trust decision through raise-based `verify_certificate` — the verify call itself must not sit inside a pytest `assert` expression (call it, then assert on the returned k), matching the R1 discipline that survives `python -O`. For Test 5 import the pulp constants inside the test (tests are not stdlib-bound; only production modules are). Do NOT write any solver production code in this task.
  </action>
  <verify>
    <automated>uv run --frozen --extra dev pytest tests/test_solver_result.py tests/test_cbc_backend.py -q; test $? -ne 0  # RED: alpha2.solvers absent</automated>
  </verify>
  <acceptance_criteria>
    - `uv run --frozen --extra dev pytest tests/test_solver_result.py tests/test_cbc_backend.py -q` exits non-zero with collection/import errors referencing `alpha2.solvers` (RED).
    - tests/test_cbc_backend.py contains `verify_certificate(` and the string `PROVED_OPTIMAL`; tests/test_solver_result.py contains `NotProvedOptimal`.
    - Existing suite untouched and green: `uv run --frozen --extra dev pytest -q --ignore=tests/test_solver_result.py --ignore=tests/test_cbc_backend.py` exits 0 (54 passed baseline).
  </acceptance_criteria>
  <done>Both test files exist, are RED against the missing solvers package, and encode the locked status-mapping table, the raising accessor, and the C5 end-to-end slice through the trust root.</done>
</task>

<task type="auto">
  <name>Task 2: stdlib-only contracts — result.py + backend.py (unit tests GREEN)</name>
  <files>src/alpha2/solvers/__init__.py, src/alpha2/solvers/result.py, src/alpha2/solvers/backend.py, src/alpha2/solvers/problems/__init__.py</files>
  <read_first>
    - .planning/phases/04-exactbackend-cbc-reference/04-RESEARCH.md (Pattern 1 lines 149–188 — the verified result.py skeleton: Status enum members, NotProvedOptimal, frozen ExactOutcome, __post_init__ invariants, raising exact_value; this IS the design, transcribe it)
    - .planning/phases/04-exactbackend-cbc-reference/04-PATTERNS.md (section `src/alpha2/solvers/result.py` lines 27–44: schema.py raise-style analog + docstring-token hygiene; section `backend.py` lines 48–50)
    - src/alpha2/corpus/schema.py (lines 275–280 — the refuse-invalid raise shape to copy)
    - src/alpha2/corpus/verifier.py (lines 1–24 — module-docstring trust-contract convention)
  </read_first>
  <action>
    Create the `alpha2.solvers` package (empty `__init__.py`, plus `problems/__init__.py`).
    `result.py` (stdlib-only: dataclasses, enum): Status enum with EXACTLY the six members MODEL_FOUND, PROVED_OPTIMAL, PROVED_INFEASIBLE, INCUMBENT_ONLY, UNKNOWN, ERROR; `NotProvedOptimal(Exception)`; frozen dataclass `SolveParams(time_limit_s: float | None = None, threads: int = 1)`; frozen dataclass `ExactOutcome` with fields problem, mode, status, value (int | None), bound (int | float | None), bound_source (str — "cbc_log" | "trivial_n" | "definition" | "none"), family (tuple of tuples | None — UNTRUSTED proposal), backend, backend_version, params, wall_time_s. `__post_init__` raises ValueError if status in {UNKNOWN, ERROR} and value is not None, and if status is PROVED_OPTIMAL and value != bound. `exact_value()` is the ONLY battery-facing exact accessor: raises NotProvedOptimal unless status is PROVED_OPTIMAL (per the locked gate — an INCUMBENT_ONLY outcome is structurally unable to represent an exact had2).
    `backend.py` (stdlib-only: typing, importlib): `ExactBackend` Protocol with `name: str`, `backend_version() -> str`, and `solve_had2(adj, n, *, mode: str, target_k: int | None = None, params: SolveParams | None = None) -> ExactOutcome`; a module-level registry dict + `register_backend(name, factory)` + `get_backend(name)` that lazily `importlib.import_module(f"alpha2.solvers.{name}")` when the name is unregistered (so importing backend.py never pulls pulp; pulp loads only when the cbc backend is requested — the matching.py confinement precedent applied at registry level). Unknown name after lazy import raises ValueError.
    All guards raise, never assert — zero `assert` statements in both modules (python -O discipline; the Phase 2 AST-guard mechanism will pin this in plan 04-02). Keep grep-trigger tokens (pulp, networkx, "assert") out of docstring prose (02-01 deviation-2 precedent).
  </action>
  <verify>
    <automated>uv run --frozen --extra dev pytest tests/test_solver_result.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run --frozen --extra dev pytest tests/test_solver_result.py -q` exits 0 (all result-contract units GREEN).
    - `uv run --frozen python -c "import ast; m=[ast.parse(open(f).read()) for f in ['src/alpha2/solvers/result.py','src/alpha2/solvers/backend.py']]; assert not any(isinstance(x, ast.Assert) for t in m for x in ast.walk(t)); print('zero-assert ok')"` prints `zero-assert ok`.
    - `uv run --frozen python -c "import sys; import alpha2.solvers.result, alpha2.solvers.backend; assert 'pulp' not in sys.modules; print('stdlib-only ok')"` prints `stdlib-only ok` (importing the contracts never loads pulp).
    - `uv run --frozen python -c "from alpha2.solvers.result import Status; print(sorted(s.name for s in Status))"` prints exactly the six locked members.
  </acceptance_criteria>
  <done>The status contract exists as stdlib-only importable types: six-member Status, raising exact_value(), frozen invariant-enforcing ExactOutcome, Protocol + lazy registry — unit tests green, E2E still red.</done>
</task>

<task type="auto">
  <name>Task 3: engine — problems/had2.py + cbc.py (E2E slice GREEN)</name>
  <files>src/alpha2/solvers/problems/had2.py, src/alpha2/solvers/cbc.py</files>
  <read_first>
    - .planning/reference/alpha2-program-source.md (Appendix C.3 `ilp_had2` lines 394–425 — THE byte-compat model source: `LpProblem("had2", LpMaximize)`, `m_{u}_{v}`/`s_{v}` Binary var naming, objective `lpSum(mv)+lpSum(sv)`, per-vertex disjointness row lines 401–402, extraction shape lines 423–424. What must NOT be copied: the ungated `pulp.value` read line 420, `int(round(val))` line 422, and the caller's `fam[:chi]` truncation line 443 — these are the soundness holes this phase closes)
    - .planning/phases/04-exactbackend-cbc-reference/04-RESEARCH.md (enumeration implementation lines 205–224 — transcribe; correctness subtleties lines 227–231: frozenset dedup of the double C4 discovery, triangle-freeness makes the enumeration total; checksum code lines 370–379; CBC invocation + two-field gate lines 341–355; parse_bound lines 359–366; cbc_binary_version lines 384–393; Pattern 3 decision mode lines 236–241)
    - .planning/phases/04-exactbackend-cbc-reference/04-PATTERNS.md (section `problems/had2.py` lines 54–107 incl. the Pitfall-5 docstring note: pair vars exist only for G-edges — that IS the size-2 connectivity constraint; section `cbc.py` lines 110–144)
    - src/alpha2/generators/tfp.py (lines 33–47 — module shape analog: pure functions over (adj, n); is_triangle_free line 49)
    - src/alpha2/invariants/matching.py (lines 11–16 — third-party import confinement precedent cbc.py copies for pulp)
  </read_first>
  <action>
    `problems/had2.py` (stdlib-only): `ChecksumError(Exception)`; frozen dataclass `Had2Problem(n, Gedges, ss, sp, pp)` (plain data — Phase 5's CP-SAT translates the same object); `enumerate_had2(adj, n)` transcribed from the verified RESEARCH implementation (G-edges = non-H pairs; ss = H-edges; sp = cherries as 2-subsets of each N_H(v); pp = C4 diagonals as `frozenset({e1,e2})` over 2-subsets of N_H(a)&N_H(b) per G-edge {a,b} — the frozenset membership dedups the double discovery, without it npp doubles); `build_had2_problem(adj, n)` which (1) re-checks triangle-freeness raise-based (raise ValueError, not assert — the enumeration is only total on triangle-free H), (2) enumerates, (3) runs the structural checksum gate: independently recompute nss = sum(deg)//2, nsp = sum(C(deg,2)), npp = sum over u<v of C(codeg,2) // 2 with codegrees via len(adj[u] & adj[v]), and raise ChecksumError on ANY mismatch (this gate validates EVERY model build — LOCKED). Docstring documents Pitfall 5 (variable indexing IS the connectivity constraint) without grep-trigger tokens.
    `cbc.py` (the ONLY module importing pulp): module-level `import pulp` is acceptable here since cbc.py is only imported via the lazy registry; `map_status(status, sol_status, mode) -> Status` pure function implementing the locked table — PROVED_OPTIMAL requires `status == pulp.LpStatusOptimal and sol_status == pulp.LpSolutionOptimal`, NOTHING WEAKER; (Optimal, IntegerFeasible) -> INCUMBENT_ONLY; NotSolved -> UNKNOWN; Infeasible -> PROVED_INFEASIBLE in decision mode but ERROR in optimize mode (the empty family is always feasible — optimize-infeasible is an encoding bug); Unbounded -> ERROR. `CBCBackend` implementing the Protocol: build the LpProblem from `build_had2_problem` output using the Appendix C.3 shape (name "had2", LpMaximize, `m_{u}_{v}`/`s_{v}` Binary vars, objective lpSum(mv)+lpSum(sv), per-vertex disjointness rows), conflict rows from ss/sp/pp; decision mode replaces the objective with constant 0 (`prob += 0`) and adds `lpSum(mv)+lpSum(sv) >= target_k`. Solve with `pulp.PULP_CBC_CMD(msg=0, threads=1, timeLimit=params.time_limit_s, logPath=str(tmp))` — logPath on EVERY solve (evidence + dual bound; msg=0 hides the Stopped line otherwise). Extraction ONLY for {PROVED_OPTIMAL, MODEL_FOUND, INCUMBENT_ONLY}: value()>0.5 over pair vars then singleton vars (C.3 shape), guarded by (a) integrality — every binary within 1e-6 of 0/1 else status ERROR, (b) objective recompute — count of extracted sets equals reported objective within 1e-6 else ERROR (defends pulp#517 garbage class); NEVER read pulp.value or var.value() for UNKNOWN/ERROR/PROVED_INFEASIBLE (fractional LP junk 23.25 / stale 1.0 observed live). Bound: PROVED_OPTIMAL -> bound=value, bound_source="definition"; stopped runs -> parse `Upper bound:` from the log (fixture-pinned grammar), fallback to trivial n with bound_source="trivial_n". `cbc_binary_version()`: subprocess `[PULP_CBC_CMD().path, "-exit"]`, parse the `Version:` banner; `backend_version = f"pulp==3.3.2 / CBC {ver}"` stamped in every outcome, params recorded verbatim, wall_time_s via time.monotonic. Register the backend as "cbc" (module-level `register_backend` call so the lazy import wires it). Zero asserts anywhere; all guards raise or map to Status.ERROR.
  </action>
  <verify>
    <automated>uv run --frozen --extra dev pytest tests/test_solver_result.py tests/test_cbc_backend.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run --frozen --extra dev pytest tests/test_solver_result.py tests/test_cbc_backend.py -q` exits 0 — the C5 slice is PROVED_OPTIMAL had2=3 with verify_certificate(rec)==3, empty-H gives 5, decision k=3/k=4 give MODEL_FOUND/PROVED_INFEASIBLE, mapping table exact (GREEN end-to-end).
    - Source assertion: src/alpha2/solvers/cbc.py contains both `LpStatusOptimal` and `sol_status` in the same gating expression (the two-field conjunction), and contains `threads=1` and `logPath`.
    - `uv run --frozen python -c "import ast,glob; bad=[f for f in glob.glob('src/alpha2/solvers/**/*.py', recursive=True) if f != 'src/alpha2/solvers/cbc.py' and any(('pulp' in getattr(n,'module','') or '') or any(a.name=='pulp' for a in getattr(n,'names',[]) if hasattr(a,'name')) for n in ast.walk(ast.parse(open(f).read())) if isinstance(n,(ast.Import,ast.ImportFrom)))]; assert not bad, bad; print('pulp confined ok')"` prints `pulp confined ok`.
    - Full suite green with no regression: `uv run --frozen --extra dev pytest -q` exits 0.
    - Frozen files byte-untouched: `git diff --quiet src/alpha2/corpus src/alpha2/verify src/alpha2/repro src/alpha2/generators data/ tests/test_corpus_r1.py tests/test_corpus_r2.py tests/test_corpus_r3.py` exits 0.
  </acceptance_criteria>
  <done>The MVP slice is real: enumeration -> checksum gate -> CBC -> two-field status gate -> guarded extraction -> trust-root verification, green on closed-form instances in both modes, with pulp confined to cbc.py and every frozen file untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CBC solution file -> battery-facing result | Solver output (status, objective, var values) is UNTRUSTED; the status gate + guarded extraction decide what may cross. |
| solver family -> corpus/trust root | Every family is an UNTRUSTED proposal; only `verify_certificate` confers truth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-01 | Spoofing | cbc.py status gate / result.py exact_value | mitigate | PROVED_OPTIMAL ⇔ `LpStatusOptimal AND LpSolutionOptimal` (two-field conjunction, nothing weaker); `exact_value()` raises NotProvedOptimal on every other status — an incumbent is structurally unable to read as an exact had2. Timeout proof lands in plan 04-02. |
| T-4-02 | Tampering | cbc.py extraction | mitigate | Extraction only in {PROVED_OPTIMAL, MODEL_FOUND, INCUMBENT_ONLY}; integrality guard (1e-6 of {0,1}) + objective recompute equality, both mapping to ERROR; UNKNOWN/ERROR carry value=None enforced by `__post_init__` (raise-based). |
| T-4-03 | Repudiation | ExactOutcome.backend_version | mitigate | `cbc_binary_version()` subprocess probe parses the CBC banner ("2.10.3") and stamps `pulp==3.3.2 / CBC {ver}` in every outcome; params + wall time recorded verbatim. |
| T-4-04 | Tampering | problems/had2.py enumeration | mitigate | Raise-based structural checksum on EVERY build (nss=|E(H)|, nsp=ΣC(deg,2), npp=½ΣC(codeg,2), independently recomputed); triangle-free re-check at build; set-equality proof vs naive loop lands in plan 04-03. |
| T-4-05 | Elevation | solver guards under `python -O` | mitigate | Zero asserts in all four solver modules (AST-checkable now, AST-guard test + -O canary land in plan 04-02); every guard raises or maps to Status.ERROR. |
| T-4-SC | Tampering | supply chain (package installs) | accept | No package-manager installs this phase (RESEARCH Package Legitimacy Audit: none); env reproduced via `uv sync --frozen`; CI already lockfile-authoritative with SHA-pinned actions. |
</threat_model>

<verification>
- `uv run --frozen --extra dev pytest tests/test_solver_result.py tests/test_cbc_backend.py -q` green.
- Full suite green: `uv run --frozen --extra dev pytest -q` (54 baseline tests + new; no regression).
- `git diff --quiet src/alpha2/corpus src/alpha2/verify src/alpha2/repro src/alpha2/generators data/` — frozen trust primitives and corpus byte-unchanged.
- pulp import confined to cbc.py (AST scan, Task 3 acceptance).
</verification>

<success_criteria>
- One real instance solves end-to-end: C5 optimize -> PROVED_OPTIMAL, exact_value()==3, family verified by the frozen trust root.
- The status contract is a type-level guarantee: six statuses, raising accessor, two-field gate.
- Decision + optimize both work; value, bound, bound_source, backend_version ("pulp==3.3.2 / CBC 2.10.3") present in every outcome.
- stdlib-only trust boundary holds: result/backend/had2 importable without pulp.
</success_criteria>

<output>
Create `.planning/phases/04-exactbackend-cbc-reference/04-01-SUMMARY.md` when done.
</output>
