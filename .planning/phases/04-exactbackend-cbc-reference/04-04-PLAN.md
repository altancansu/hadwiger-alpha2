---
phase: 04-exactbackend-cbc-reference
plan: 04
type: execute
wave: 3
depends_on: ["04-01", "04-02", "04-03"]
files_modified:
  - tests/test_seed137_regression.py
  - tests/test_cbc_backend.py
autonomous: true
requirements: [EXACT-01, EXACT-02]
must_haves:
  truths:
    - "seed-137 regression: had2 = 17, status PROVED_OPTIMAL (two-field gate), bound == 17, full 17-set family extracted and verify_certificate-green via an IN-MEMORY schema-v1 record — the frozen corpus, repro drivers, R1/R2/R3 and manifest stay byte-untouched (LOCKED reconciliation)"
    - "Metamorphic anchor: exact_value() == 17 > 16 == len(stored D.3 family) — a PROVED_OPTIMAL value below any stored verified family size would be a CRITICAL encoding bug"
    - "296-lineage exact values reproduce on the CI panel: decision-mode kills at k = chi = 16 on seed-1 and seed-137 (~2.5 s each) with families independently verified — the every-commit leg of ROADMAP SC2"
    - "The optimize regression is @pytest.mark.slow and joins the existing release-gate '-m slow' selector automatically; the every-commit job's wall time is unaffected"
  artifacts:
    - path: "tests/test_seed137_regression.py"
      provides: "Slow optimize regression: had2=17 PROVED_OPTIMAL + 17-set family through the trust root + metamorphic 17>16 vs frozen corpus"
      contains: "pytest.mark.slow"
    - path: "tests/test_cbc_backend.py"
      provides: "Every-commit decision-mode kill panel (seed-1, seed-137 at k=16) + slow Cayley p=31 kill, families verified"
      contains: "MODEL_FOUND"
  key_links:
    - from: "tests/test_seed137_regression.py"
      to: "alpha2.corpus.schema.build_record"
      via: "in-memory record (never written to data/corpus/)"
      pattern: "build_record"
    - from: "tests/test_seed137_regression.py"
      to: "alpha2.corpus.verifier.verify_certificate"
      via: "trust-root arbitration of the 17-set family"
      pattern: "verify_certificate\\("
    - from: "tests/test_cbc_backend.py"
      to: "alpha2.solvers.backend.get_backend"
      via: "decision-mode solves at k=chi"
      pattern: "mode=.decision"
---

<objective>
Land the phase capstone: the seed-137 regression proving had2 = 17 PROVED_OPTIMAL
with the full 17-set family independently verified — as a REGRESSION-AS-TEST routing
an in-memory schema-v1 record through the trust root (LOCKED: the frozen corpus's
16-set D.3 literal, the repro drivers, R1/R2/R3 and the manifest stay byte-untouched).
Plus the every-commit 296-lineage exact-value panel: decision-mode kills at k = chi
on seed-1 and seed-137 (and a Cayley p=31 kill in the slow tier).

Purpose: this satisfies ROADMAP Success Criterion 2 verbatim while preserving the
Phase-3 trust anchor. Decision mode is the kill path (2.3–2.6 s live-verified, 60x
cheaper than the ~149 s optimality proof), so the panel runs on every commit and the
optimize proof joins the release gate via the existing `-m slow` selector — no CI
workflow edit needed.

DEFERRED (explicitly NOT in this phase): upgrading the stored seed-137 corpus record
to the 17-set family. That is a deliberate future freeze amendment (single lockstep
commit: frozen driver + refreeze + R1 literal) — first real consumer is the Phase-11
Falsification-Rule harness. Record it in the SUMMARY as a deferred item; do not
implement any part of it.
Output: tests/test_seed137_regression.py (slow) + the extended test_cbc_backend.py
panel, both green, frozen artifacts byte-identical.
</objective>

<phase_goal>
**As the** α = 2 program author, **I want** exact had₂ solving behind a status-honest
interface with CBC as the reference engine, **so that** a timed-out incumbent can never
masquerade as an exact had₂ and every exact claim is independently verified before it counts.
</phase_goal>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-exactbackend-cbc-reference/04-RESEARCH.md
@.planning/phases/04-exactbackend-cbc-reference/04-PATTERNS.md

<interfaces>
<!-- From wave 1/2 plans: -->
From src/alpha2/solvers/backend.py:
  get_backend("cbc").solve_had2(adj, n, *, mode, target_k=None, params=None) -> ExactOutcome

From src/alpha2/solvers/result.py:
  Status.PROVED_OPTIMAL / Status.MODEL_FOUND; SolveParams(time_limit_s, threads=1)
  ExactOutcome.exact_value() -> int (raises NotProvedOptimal otherwise); .bound; .family; .backend_version

FROZEN primitives (read-only):
From src/alpha2/corpus/schema.py:
  build_record(*, provenance, H_edges, nu_H, chi_G, model_branch_sets, matching_M,
               tutte_berge_U, method, omega_G=None, verified=True, ...) -> dict  # line 246
  provenance_seed(family, n, seed, process) -> dict                              # line 104
From src/alpha2/corpus/verifier.py:
  verify_certificate(rec) -> k                                                   # line 213
From src/alpha2/invariants/witness.py:
  extract_witness(adj, n) -> (M, U, nu)                                          # line 35
From src/alpha2/invariants/matching.py:
  matching_number(adj, n) -> int                                                 # line 11
From src/alpha2/generators/tfp.py:
  triangle_free_process(n, rng) -> (adj, m)                                      # line 33
From src/alpha2/generators/cayley.py:
  random_maximal_symmetric_sumfree(p, rng) -> S                                  # line 30
  cayley_adj(p, S) -> adj                                                        # line 42
From src/alpha2/paths.py:
  CORPUS  # committed 296-record corpus path (read-only in this plan)

LOCKED regression constants (RESEARCH, live-verified):
  seed-137 (n=31): |E(H)|=177, nu=15, chi=16, omega=14; had2 = 17 PROVED_OPTIMAL (~149 s
  single-thread); decision k=16 and k=17 each ~2.5 s. seed-1 (n=31): |E(H)|=131, nu=15, chi=16.
  Cayley panel seed convention: seed = 5000 + 10*p + k, so p=31, k=0 -> seed 5310
  (manifest key cayley:p31:s5310).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: every-commit CI panel — decision-mode kills at k=chi with verified families</name>
  <files>tests/test_cbc_backend.py</files>
  <read_first>
    - tests/test_cbc_backend.py (being extended — created in plan 04-01; reuse its record-assembly helpers rather than duplicating)
    - .planning/phases/04-exactbackend-cbc-reference/04-RESEARCH.md (Pattern 3 lines 236–241 — decision mode IS the kill path, 60x cheaper; Pattern 6 lines 279–287 — the every-commit vs slow tier split; Pattern 4 lines 243–262 — the in-memory record shape for trust-root routing)
    - .planning/phases/04-exactbackend-cbc-reference/04-PATTERNS.md (section `tests/test_cbc_backend.py` lines 184–208; `slow` marker pattern lines 293–295 — marker already registered in pyproject.toml line 26, register nothing new)
    - src/alpha2/generators/cayley.py (lines 30–47, READ-ONLY — S from rng, then cayley_adj; chi = p - nu)
  </read_first>
  <behavior>
    - Test A (seed-1 kill, every-commit): regenerate H via triangle_free_process(31, random.Random(1)); assert nu == 15 and chi == 16 (regeneration gate before trusting any solve — the R2 discipline); solve mode="decision", target_k=16 -> status == MODEL_FOUND, len(family) >= 16; route the family through extract_witness + build_record(provenance_seed("triangle_free_process_complement", 31, 1, "Bohman uniform triangle-free process"), method="exact ILP (CBC): decision k=16") + verify_certificate -> returns k >= 16.
    - Test B (seed-137 kill, every-commit): same shape with random.Random(137), |E(H)| gate == 177, chi == 16, target_k=16 -> MODEL_FOUND, family verified. This is the instance the heuristic misses — the exact method resolves it in ~2.5 s, the "reproduces 296-lineage exact values" every-commit leg (chi-level values from the frozen corpus lineage, exact-method confirmed).
    - Test C (Cayley p=31 kill, @pytest.mark.slow): S = random_maximal_symmetric_sumfree(31, random.Random(5310)); adj = cayley_adj(31, S); structural gate: adj is |S|-regular and symmetric (the R2 Cayley tripwire); chi = 31 - matching_number(adj, 31); decision target_k=chi -> MODEL_FOUND with family verified via build_record using provenance_params (family "cayley_maximal_sumfree_Zp", params including p and sorted S) — mirror the stored corpus record's provenance shape (read the committed cayley:p31:s5310 record for the exact field shape, read-only).
    - Every solve asserts backend_version contains "2.10.3"; every family length is NEVER truncated before build_record (schema derives had_2 = len and would refuse len < chi).
  </behavior>
  <action>
    Extend tests/test_cbc_backend.py with the three panel tests per the behavior block. Tests A/B stay unmarked (every-commit tier, ~5 s combined); Test C carries @pytest.mark.slow. Route all trust decisions through raise-based verify_certificate (call, then assert on returned k). Never open data/corpus/ for writing; the Cayley provenance-shape read is read-only via paths.CORPUS + json.load.
  </action>
  <verify>
    <automated>uv run --frozen --extra dev pytest tests/test_cbc_backend.py -q -m "not slow" && uv run --frozen --extra dev pytest tests/test_cbc_backend.py -q -m slow</automated>
  </verify>
  <acceptance_criteria>
    - Seed-1 and seed-137 decision kills at k=16 return MODEL_FOUND with verify_certificate(rec) >= 16, in the every-commit tier (file runtime without slow < ~20 s).
    - The seed-137 leg's regeneration gate asserts |E(H)| == 177 and chi == 16 before any solve is trusted.
    - Cayley p=31 (seed 5310) kill is green under `-m slow` with the structural gate asserted before the solve.
    - `git diff --quiet data/ src/alpha2/repro src/alpha2/generators` exits 0 (frozen corpus/drivers/generators untouched).
  </acceptance_criteria>
  <done>The CI panel reproduces 296-lineage exact values every commit via decision-mode kills with independently verified families; the Cayley leg guards the second generator family in the slow tier.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: seed-137 optimize regression — had2=17 PROVED_OPTIMAL through the trust root (slow)</name>
  <files>tests/test_seed137_regression.py</files>
  <read_first>
    - .planning/phases/04-exactbackend-cbc-reference/04-PATTERNS.md (section `tests/test_seed137_regression.py` lines 218–259 — the composite recipe: slow marker + never-touch-repo-corpus from test_corpus_r3.py lines 84–91; regeneration lines from repro/seed137.py lines 44–48 READ-ONLY; the in-memory build_record shape lines 243–257; assertions list line 259 — assert on value/status/verifiability, NEVER on model bytes per Assumption A1)
    - .planning/phases/04-exactbackend-cbc-reference/04-RESEARCH.md (Pattern 5 lines 264–277 — the LOCKED reconciliation: regression-as-test, corpus untouched; the metamorphic direction 17 > 16)
    - src/alpha2/repro/seed137.py (lines 44–48, READ-ONLY frozen driver — do NOT modify; its docstring already promises "the true had_2(G)=17 family arrives in Phase 4 via CBC", which THIS TEST delivers without touching the driver)
    - tests/test_corpus_r1.py (lines 55–64 — the _find_seed_record helper pattern for locating the committed (31,137) record, read-only)
  </read_first>
  <behavior>
    - @pytest.mark.slow (marker registered in pyproject.toml line 26 — register nothing).
    - Regenerate: rng = random.Random(137); adj, m = triangle_free_process(31, rng); gate m == 177; nu = matching_number(adj, 31) == 15; chi = 31 - nu == 16 (raise/fail before any solve if a gate misses — a drifted generator must not self-certify).
    - Solve: get_backend("cbc").solve_had2(adj, 31, mode="optimize", params=SolveParams(time_limit_s=600)) — headroom above the ~149 s observed single-thread proof.
    - Assert the LOCKED constants: outcome.status == Status.PROVED_OPTIMAL (which by construction means LpStatusOptimal AND LpSolutionOptimal held); outcome.exact_value() == 17; outcome.bound == 17; len(outcome.family) == 17; "2.10.3" in outcome.backend_version.
    - Trust root: M, U, nu2 = extract_witness(adj, 31); rec = build_record(provenance=provenance_seed("triangle_free_process_complement", 31, 137, "Bohman uniform triangle-free process"), H_edges=sorted([min(u,v),max(u,v)] for u in range(31) for v in adj[u] if u < v), nu_H=15, chi_G=16, model_branch_sets=[list(s) for s in outcome.family], matching_M=M, tutte_berge_U=U, method="exact ILP (CBC): had_2(G)=17", omega_G=14, verified=True); verify_certificate(rec) == 17. The record stays IN MEMORY — no store.append_certificate call, no file write (LOCKED: the corpus upgrade to the 17-set stored family is a DEFERRED freeze amendment, not a task).
    - Metamorphic anchor: load the committed corpus (paths.CORPUS, read-only), find the (31, 137) record, assert len(stored model_branch_sets) == 16 and outcome.exact_value() == 17 > 16 — a PROVED_OPTIMAL value below a stored verified family size would be a CRITICAL encoding bug (verifier trumps solver).
    - Backend-version stamping closure: if build_record's backends block is overridable (02-02 decision), pass the probed CBC binary version so the in-memory record carries "2.10.3" rather than the Phase-2 "bundled-with-pulp-3.3.2" stub; if the kwarg is absent, assert on outcome.backend_version only and note it in the SUMMARY — do NOT modify schema.py (frozen).
  </behavior>
  <action>
    Create tests/test_seed137_regression.py per the behavior block, as a single slow test function (plus at most one helper). It joins the release gate automatically via the existing ci.yml `-m slow` selector — make NO CI edit. Assert on value/status/bound/verifiability only, never on which 17-set family CBC returned (single-thread CBC is deterministic per platform, but the regression contract for exact methods is semantic, not byte — ENV-05).
  </action>
  <verify>
    <automated>uv run --frozen --extra dev pytest tests/test_seed137_regression.py -q -m slow</automated>
  </verify>
  <acceptance_criteria>
    - `uv run --frozen --extra dev pytest tests/test_seed137_regression.py -q -m slow` exits 0: had2 == 17, status PROVED_OPTIMAL, bound == 17, 17-set family, verify_certificate(rec) == 17 — ROADMAP SC2's regression satisfied verbatim.
    - Metamorphic 17 > 16 vs the committed D.3 record asserted.
    - The frozen trust anchor is byte-identical: `git diff --quiet data/ src/alpha2/repro tests/test_corpus_r1.py tests/test_corpus_r2.py tests/test_corpus_r3.py` exits 0, and the R1/R2/R3 suite is green: `uv run --frozen --extra dev pytest tests/test_corpus_r1.py tests/test_corpus_r2.py -q` exits 0.
    - Full suite including slow green: `uv run --frozen --extra dev pytest -q` exits 0 (~3–4 min with the optimize proof).
    - tests/test_seed137_regression.py contains no call to store.append_certificate and no open(...,"w") on any data/ path.
  </acceptance_criteria>
  <done>The phase capstone is green: seed-137 had2=17 PROVED_OPTIMAL with the full 17-set family independently verified through an in-memory record, the frozen corpus untouched, and the deferred corpus-upgrade decision recorded — Phase 4's four success criteria all have executable evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| solver family -> trust root | The 17-set family is an UNTRUSTED proposal until verify_certificate returns 17. |
| this plan -> frozen corpus/drivers | Read-only consumption; any write would break the Phase-3 trust anchor. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-01 | Spoofing | seed-137 optimize outcome | mitigate | PROVED_OPTIMAL asserted via the two-field-gated Status (proven under timeout in 04-02); exact_value()==17 AND bound==17 — value/bound coincidence is part of the acceptance, not assumed. |
| T-4-04 | Tampering | wrong optimum from a bad model build | mitigate | Metamorphic anchor 17 > 16 == stored verified family size (verifier trumps solver); the regeneration gates (|E(H)|==177, chi==16, Cayley structural identity) refuse a drifted generator before any solve is trusted. |
| T-4-08 | Tampering | frozen-corpus mutation via this phase | mitigate | Record stays in memory; no store call; `git diff --quiet` over data/, repro/, and R1/R2/R3 tests is an explicit acceptance criterion; the corpus upgrade is a named DEFERRED freeze amendment. |
| T-4-03 | Repudiation | unstamped solver provenance on the capstone claim | mitigate | backend_version ("pulp==3.3.2 / CBC 2.10.3") asserted on every outcome; overridable backends block used for the in-memory record where the frozen schema permits. |
| T-4-SC | Tampering | supply chain | accept | No package installs this phase; test-only files; no CI edit in this plan. |
</threat_model>

<verification>
- `uv run --frozen --extra dev pytest tests/test_cbc_backend.py -q -m "not slow"` green (every-commit panel).
- `uv run --frozen --extra dev pytest -q` green INCLUDING slow (seed-137 optimize ~149 s + R3) — the phase gate.
- `uv run --frozen python -O -m pytest tests/test_verifier_dash_O.py tests/test_corpus_r1.py tests/test_solver_isolation.py -q` green.
- `git diff --quiet data/ src/alpha2/repro src/alpha2/generators src/alpha2/corpus src/alpha2/verify` — every frozen artifact byte-identical.
</verification>

<success_criteria>
- ROADMAP SC2 verbatim: seed-137 regression passes — had2 = 17, PROVED_OPTIMAL, full 17-set family extracted and independently verified; 296-lineage exact values reproduce on the CI panel.
- The frozen Phase-3 trust anchor (corpus, drivers, manifest, R1/R2/R3) is byte-untouched.
- The optimize proof lives in the slow/release tier; every-commit CI wall time unaffected.
- Deferred item recorded: corpus upgrade to the 17-set stored family = future deliberate freeze amendment (first consumer: Phase-11 Falsification-Rule harness).
</success_criteria>

<output>
Create `.planning/phases/04-exactbackend-cbc-reference/04-04-SUMMARY.md` when done.
</output>
