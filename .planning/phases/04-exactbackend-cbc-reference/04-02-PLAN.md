---
phase: 04-exactbackend-cbc-reference
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - tests/test_cbc_status_honesty.py
  - tests/test_solver_isolation.py
  - .github/workflows/ci.yml
autonomous: true
requirements: [EXACT-01]
must_haves:
  truths:
    - "A live time-limited CBC run on seed-137 lands in {INCUMBENT_ONLY, UNKNOWN} and exact_value() raises NotProvedOptimal — a CBC incumbent can NEVER read as an exact had2 (ROADMAP SC1 proven by execution)"
    - "The bound parser reproduces the captured CBC 2.10.3 log grammar (Upper bound: 20.879 on stopped runs) and falls back to trivial n with bound_source provenance when the line is absent"
    - "An AST guard pins: pulp imported only by solvers/cbc.py; zero assert statements in all solvers modules; the guards are mechanism checks, not prose greps"
    - "The python -O canary covers the solver path: NotProvedOptimal and ChecksumError still raise with asserts stripped, in CI on every commit"
  artifacts:
    - path: "tests/test_cbc_status_honesty.py"
      provides: "Live timeout test (seed-137, short timeLimit) + bound-parse fixture from captured log text"
      contains: "NotProvedOptimal"
    - path: "tests/test_solver_isolation.py"
      provides: "AST import-boundary + zero-assert guard over solvers/*; python -O subprocess canary exercising solver raises"
      contains: "ast.Assert"
    - path: ".github/workflows/ci.yml"
      provides: "Every-commit -O canary step extended over the solver-path test"
      contains: "test_solver_isolation.py"
  key_links:
    - from: "tests/test_cbc_status_honesty.py"
      to: "alpha2.solvers.result.NotProvedOptimal"
      via: "pytest.raises on exact_value() after a time-limited solve"
      pattern: "pytest.raises\\(NotProvedOptimal"
    - from: ".github/workflows/ci.yml"
      to: "tests/test_solver_isolation.py"
      via: "python -O pytest step (every commit)"
      pattern: "python -O .*test_solver_isolation"
---

<objective>
Prove — by live execution, not by construction alone — that the incumbent-as-optimum
soundness hole is closed: a time-limited CBC run on the real seed-137 instance must
land in INCUMBENT_ONLY or UNKNOWN and `exact_value()` must raise. Then harden the
guarantee against the two erosion vectors: `python -O` stripping (extend the canary
over the solver path) and future refactors moving pulp or reintroducing asserts
(AST import-boundary guard).

Purpose: pulp 3.3.2 deliberately maps "Stopped ... objective value" to
`status=Optimal` (live-reproduced: 20 s limit -> status=1, incumbent 17, true bound
20.879). Plan 04-01 built the type-level defense; this plan is the adversarial proof
that it holds under a real timeout, plus the mechanism guards that keep it held.
Output: the SC1 timeout test, the bound-parse fixture, the solver isolation guard,
and the CI -O step extension.
</objective>

<phase_goal>
**As the** α = 2 program author, **I want** exact had₂ solving behind a status-honest
interface with CBC as the reference engine, **so that** a timed-out incumbent can never
masquerade as an exact had₂ and every exact claim is independently verified before it counts.
</phase_goal>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-exactbackend-cbc-reference/04-RESEARCH.md
@.planning/phases/04-exactbackend-cbc-reference/04-PATTERNS.md

<interfaces>
<!-- From plan 04-01 (wave 1, complete before this plan runs): -->
From src/alpha2/solvers/result.py:
  Status (MODEL_FOUND, PROVED_OPTIMAL, PROVED_INFEASIBLE, INCUMBENT_ONLY, UNKNOWN, ERROR)
  NotProvedOptimal(Exception); SolveParams(time_limit_s, threads=1)
  ExactOutcome.exact_value() -> int   # raises NotProvedOptimal unless PROVED_OPTIMAL

From src/alpha2/solvers/backend.py:
  get_backend("cbc") -> ExactBackend  # lazy-imports alpha2.solvers.cbc

From src/alpha2/solvers/cbc.py:
  parse_bound(log_text) -> float | None   # parses "Upper bound:" lines
  CBCBackend.solve_had2(adj, n, *, mode, target_k=None, params=None) -> ExactOutcome

From src/alpha2/solvers/problems/had2.py:
  ChecksumError; build_had2_problem(adj, n) -> Had2Problem

From src/alpha2/generators/tfp.py (FROZEN):
  triangle_free_process(n, rng) -> (adj, m)   # random.Random(137) regenerates the seed-137 H

Captured CBC 2.10.3 log grammar (RESEARCH, live-verified — pin as fixture literals):
  "Result - Optimal solution found" / "Result - Stopped on time limit"
  "Objective value: 17.00000000"
  "Upper bound: 20.879"   (stopped runs; dual bound)
  "Gap: ..."
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SC1 live timeout test + bound-parse fixture</name>
  <files>tests/test_cbc_status_honesty.py</files>
  <read_first>
    - .planning/phases/04-exactbackend-cbc-reference/04-RESEARCH.md (Pattern 1 status table lines 133–147 — the (20 s -> Optimal+IntegerFeasible incumbent) and (0.05 s -> NotSolved, fractional 23.25) live rows this test re-proves; test design line 188 — accept EITHER INCUMBENT_ONLY or UNKNOWN; Pattern 3 lines 236–241 — log grammar + trivial-n fallback)
    - .planning/phases/04-exactbackend-cbc-reference/04-PATTERNS.md (section `tests/test_cbc_status_honesty.py` lines 212–214; regenerate-from-seed pattern lines 170–177)
    - src/alpha2/solvers/cbc.py (parse_bound + SolveParams usage — the API under test, created in 04-01)
    - src/alpha2/repro/seed137.py (lines 44–48, READ-ONLY analog — `random.Random(137)` -> `triangle_free_process(31, rng)` regenerates H; do NOT modify this frozen driver)
  </read_first>
  <behavior>
    - Test 1 (THE soundness-hole proof): regenerate seed-137 H via `triangle_free_process(31, random.Random(137))`; solve mode="optimize" with SolveParams(time_limit_s=10) (far below the ~149 s proof time). Assert `outcome.status in {Status.INCUMBENT_ONLY, Status.UNKNOWN}` (which of the two depends on whether CBC found the incumbent in time — both are honest); assert `pytest.raises(NotProvedOptimal)` on `outcome.exact_value()`; if status is INCUMBENT_ONLY assert `outcome.value` is an int and `outcome.bound` is not None and `outcome.bound > outcome.value` is allowed but NEVER asserted equal; if status is UNKNOWN assert `outcome.value is None` (the fractional-garbage class 23.25 must not surface).
    - Test 2 (bound-parse fixture, no solver run): feed `parse_bound` the captured stopped-run log literal (embed in-file: "Result - Stopped on time limit\nObjective value: 17.00000000\nUpper bound: 20.879\nGap: ...") -> returns 20.879; feed text without an "Upper bound:" line -> returns None.
    - Test 3 (fallback provenance, no solver run): construct/solve a case where the bound line is absent is impractical live — instead assert via the fixture path that when parse_bound returns None the adapter's documented fallback is bound=n with bound_source=="trivial_n" by unit-driving whatever helper cbc.py exposes for outcome assembly, OR (if no helper is exposed) assert on Test 1's outcome that `outcome.bound_source in {"cbc_log", "trivial_n"}` and bound is populated whenever status is INCUMBENT_ONLY. Do not weaken Test 1 to accommodate this.
  </behavior>
  <action>
    Create tests/test_cbc_status_honesty.py per the behavior block. Embed the captured log text as an in-file literal (embedded-literal discipline). The timeout test regenerates H fresh each run (deterministic generator, frozen); it must NOT read or write data/corpus/. Keep the wall clock bounded: time_limit_s=10 with a generous pytest-side ceiling — this test stays in the every-commit tier (~10–15 s), it is the phase's central soundness evidence. Route no trust decision through a bare pytest assert on solver output beyond status/value/bound checks — the point IS the status honesty, there is no family to verify on a stopped run.
  </action>
  <verify>
    <automated>uv run --frozen --extra dev pytest tests/test_cbc_status_honesty.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run --frozen --extra dev pytest tests/test_cbc_status_honesty.py -q` exits 0.
    - The test proves: status after a 10 s-limited seed-137 optimize is in {INCUMBENT_ONLY, UNKNOWN} AND `exact_value()` raises NotProvedOptimal (source contains `pytest.raises(NotProvedOptimal)` applied to `exact_value`).
    - parse_bound fixture: 20.879 extracted from the captured literal; None on absent line.
    - No file under data/ or src/alpha2/repro/ modified: `git diff --quiet data/ src/alpha2/repro` exits 0.
  </acceptance_criteria>
  <done>The soundness hole is proven closed by live execution: a time-limited CBC run cannot surface an exact had2, and the dual-bound evidence path (log parse + fallback provenance) is fixture-pinned.</done>
</task>

<task type="auto">
  <name>Task 2: solver isolation guard (AST) + python -O canary + CI step extension</name>
  <files>tests/test_solver_isolation.py, .github/workflows/ci.yml</files>
  <read_first>
    - tests/test_verifier_isolation.py (lines 17–49 — the AST guard mechanism to copy: parse module, allow-list imports, count ast.Assert == 0, count raises >= N)
    - tests/test_verifier_dash_O.py (lines 25–55 — the -O subprocess canary shape: textwrap.dedent script, exit 3 if __debug__ is True, exit 0 iff the raise fired, parent runs [sys.executable, "-O", "-c", SCRIPT] with PYTHONPATH=src)
    - .github/workflows/ci.yml (line 36–39 — the existing `python -O assert-stripping canary` step to extend; both action SHA pins MUST stay untouched)
    - .planning/phases/04-exactbackend-cbc-reference/04-PATTERNS.md (Shared Patterns `AST import-boundary guard` lines 289–291 — incl. the caution: keep grep-trigger tokens out of guarded modules' docstring prose)
  </read_first>
  <behavior>
    tests/test_solver_isolation.py:
    - Guard 1 (stdlib boundary): AST-parse src/alpha2/solvers/result.py, backend.py, problems/had2.py — every Import/ImportFrom resolves to an allow-listed stdlib set (dataclasses, enum, typing, importlib, math, time, collections, itertools — adjust to actual imports); any third-party name fails the test.
    - Guard 2 (pulp confinement): AST-scan ALL modules under src/alpha2/ — `pulp` may appear as an import target ONLY in src/alpha2/solvers/cbc.py (module-level or function-level). This is the CHI-01-style mechanism guard pinning the lazy-import precedent.
    - Guard 3 (zero-assert): count of ast.Assert nodes == 0 across all four solver modules (result, backend, problems/had2, cbc).
    - Guard 4 (-O canary): subprocess `[sys.executable, "-O", "-c", SCRIPT]` with PYTHONPATH=src where SCRIPT (a) exits 3 if `__debug__` is True (proves -O is active via a real branch), (b) triggers `NotProvedOptimal` by calling exact_value() on a hand-built INCUMBENT_ONLY ExactOutcome, (c) triggers `ChecksumError` by calling the checksum gate with a deliberately mutated conflict set (drop one element from ss after enumeration on a tiny H), (d) exits 0 iff BOTH raised. Parent asserts returncode == 0 and != 3.
    .github/workflows/ci.yml:
    - The existing every-commit `python -O assert-stripping canary` step gains tests/test_solver_isolation.py: `uv run --frozen python -O -m pytest tests/test_verifier_dash_O.py tests/test_corpus_r1.py tests/test_solver_isolation.py -q`. Nothing else in the workflow changes; both 40-char action SHA pins byte-identical; the release-gate `-m slow` selector picks up plan 04-04's slow test automatically later — do not touch it.
  </behavior>
  <action>
    Create tests/test_solver_isolation.py copying the two proven mechanisms (AST guard from test_verifier_isolation.py, -O subprocess canary from test_verifier_dash_O.py) and apply them to the solvers package per the behavior block. Then edit .github/workflows/ci.yml appending ONLY the one test path to the existing -O step's pytest file list.
  </action>
  <verify>
    <automated>uv run --frozen --extra dev pytest tests/test_solver_isolation.py -q && uv run --frozen python -O -m pytest tests/test_verifier_dash_O.py tests/test_corpus_r1.py tests/test_solver_isolation.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run --frozen --extra dev pytest tests/test_solver_isolation.py -q` exits 0 (all four guards green).
    - The -O invocation `uv run --frozen python -O -m pytest tests/test_verifier_dash_O.py tests/test_corpus_r1.py tests/test_solver_isolation.py -q` exits 0 (solver raises survive assert-stripping).
    - Guard non-vacuity: temporarily injecting `assert True` into src/alpha2/solvers/result.py makes Guard 3 fail (verify by injection then revert — the mechanism check is real, mirroring the CHI-01 injected-greedy_color precedent); final tree has the injection reverted.
    - .github/workflows/ci.yml contains `test_solver_isolation.py` inside the -O step; `grep -cE '[0-9a-f]{40}' .github/workflows/ci.yml` still reports the same SHA-pin count as before the edit (pins untouched).
  </acceptance_criteria>
  <done>The status-honesty guarantee is mechanism-guarded: pulp pinned to cbc.py, zero asserts AST-enforced, solver raises proven live under python -O, and CI runs that proof on every commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| time-limited CBC run -> battery-facing result | The exact case where pulp reports status=Optimal on an unproven incumbent; the two-field gate must hold under live fire. |
| interpreter flags (-O) -> solver guards | Assert-stripping must not be able to turn any soundness guard into a no-op. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-01 | Spoofing | incumbent-as-optimum (THE hole) | mitigate | Live timeout test: 10 s-limited seed-137 optimize -> status in {INCUMBENT_ONLY, UNKNOWN}, exact_value() raises NotProvedOptimal. This is the executable proof of ROADMAP SC1. |
| T-4-02 | Tampering | garbage objective on stopped/infeasible runs | mitigate | UNKNOWN leg asserts value is None (fractional 23.25 class never surfaces); bound evidence comes from the log parse with fixture-pinned grammar, falling back to trivial n with recorded provenance. |
| T-4-05 | Elevation | `python -O` stripping solver guards | mitigate | -O subprocess canary exercises NotProvedOptimal + ChecksumError with asserts stripped; zero-assert AST guard proven non-vacuous by injection; wired into the every-commit CI -O step. |
| T-4-06 | Tampering | future refactor moving pulp / widening imports | mitigate | AST import-boundary guard: pulp only in cbc.py, stdlib-only allow-list for result/backend/had2 — same mechanism class as CHI-01. |
| T-4-SC | Tampering | supply chain | accept | No package installs this phase; ci.yml edit adds one test path only, action SHA pins byte-identical (acceptance-checked). |
</threat_model>

<verification>
- `uv run --frozen --extra dev pytest tests/test_cbc_status_honesty.py tests/test_solver_isolation.py -q` green.
- `uv run --frozen python -O -m pytest tests/test_verifier_dash_O.py tests/test_corpus_r1.py tests/test_solver_isolation.py -q` green.
- Full suite green: `uv run --frozen --extra dev pytest -q` — no regression; frozen files untouched (`git diff --quiet src/alpha2/corpus src/alpha2/repro src/alpha2/generators data/`).
</verification>

<success_criteria>
- ROADMAP SC1 proven by execution: a CBC incumbent under timeout can never read as an exact had2; "had2 < chi from an incumbent" is unrepresentable AND live-tested.
- Bound + evidence recorded on stopped runs (log-parsed dual bound or provenance-tagged trivial fallback).
- Isolation guards green and non-vacuous; CI -O canary covers the solver path on every commit.
</success_criteria>

<output>
Create `.planning/phases/04-exactbackend-cbc-reference/04-02-SUMMARY.md` when done.
</output>
