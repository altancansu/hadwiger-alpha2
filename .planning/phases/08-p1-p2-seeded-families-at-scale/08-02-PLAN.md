---
phase: 08-p1-p2-seeded-families-at-scale
plan: 02
type: tdd
wave: 2
depends_on: [08-01]
files_modified:
  - src/alpha2/pool/sumfree/group.py
  - src/alpha2/pool/sumfree/rng.py
  - src/alpha2/pool/sumfree/generate.py
  - src/alpha2/pool/sumfree/dedup.py
  - src/alpha2/solvers/result.py
  - src/alpha2/solvers/cpsat.py
  - src/alpha2/solvers/cbc.py
autonomous: true
requirements: [POOL-2]
must_haves:
  truths:
    - "A verified symmetric sum-free set S over any finite abelian Γ can be generated by structured (Andrásfai middle-interval, Green–Ruzsa) and random-greedy routes"
    - "H = Cay(Γ, S) is triangle-free (so G = H̄ is α=2) for every generated S"
    - "Isomorphic H from distinct (Γ,S) descriptors collapse to one canonical class; WL-hash is never used"
    - "RNG contract v2 sha256 per-stage subseeds are stage-independent; instances rebuild byte-exactly from the stored descriptor"
    - "A DETERMINISTIC solver budget is settable on BOTH backends for recorded verdicts — max_deterministic_time on CP-SAT, a node-count cap (maxNodes) on CBC — never wall-clock; the existing wall-clock path is undisturbed when the budget fields are None"
  artifacts:
    - path: "src/alpha2/pool/sumfree/group.py"
      provides: "Abelian invariant-factor group: add/neg/elements, bounded + validated"
      min_lines: 40
    - path: "src/alpha2/pool/sumfree/generate.py"
      provides: "structured + random-greedy maximal symmetric sum-free S; abelian cayley_adj; sum-free/maximal re-check"
      min_lines: 80
    - path: "src/alpha2/pool/sumfree/dedup.py"
      provides: "canonical-certificate dedup over merged (Γ,S) streams"
      min_lines: 30
    - path: "src/alpha2/solvers/result.py"
      provides: "additive deterministic-budget fields on SolveParams (det_time for CP-SAT, det_nodes for CBC)"
      contains: "det"
  key_links:
    - from: "src/alpha2/pool/sumfree/generate.py"
      to: "src/alpha2/pool/sumfree/group.py"
      via: "Abelian.add / Abelian.neg in can_add"
      pattern: "group\\.(add|neg)"
    - from: "src/alpha2/solvers/cpsat.py"
      to: "solver.parameters.max_deterministic_time"
      via: "SolveParams.det_time budget"
      pattern: "max_deterministic_time"
    - from: "src/alpha2/solvers/cbc.py"
      to: "PULP_CBC_CMD(maxNodes=...)"
      via: "SolveParams.det_nodes deterministic node cap"
      pattern: "maxNodes"
---

<objective>
Build the POOL-2 generation foundation: a stdlib finite-abelian-group representation, the three
sum-free generators (structured middle-interval / Green–Ruzsa-type + random-greedy maximal),
canonical-certificate dedup for the merged streams, the RNG-contract-v2 subseed helper, and an
additive **deterministic solver budget on BOTH exact backends** so every recorded verdict downstream
can be pinned to a work-count budget (CP-SAT `max_deterministic_time`) and a node-count budget (CBC
`maxNodes`) — never wall-clock — on the shared 64–250-core box.

Purpose: after this plan a real, verified symmetric sum-free instance over any abelian Γ can be
generated, its H proven triangle-free, and merged structured/random streams canonically deduped —
the raw material the g(G) screen adjudicates. Both co-equal backends can now be bounded
DETERMINISTICALLY, so a reported DECIDED/RESISTANT verdict is a function of (n, seed), not machine speed.
Output: `pool/sumfree/{group,rng,generate,dedup}.py` + additive `SolveParams.det_time`/`det_nodes`
fields wired into CP-SAT and CBC.
Turns test_group / test_generate / test_structured / test_dedup / test_rng_v2 GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-p1-p2-seeded-families-at-scale/08-CONTEXT.md
@.planning/phases/08-p1-p2-seeded-families-at-scale/08-RESEARCH.md
@src/alpha2/generators/cayley.py
@src/alpha2/pool/cdm/generate.py
@src/alpha2/solvers/cbc.py
@src/alpha2/solvers/cpsat.py

<interfaces>
```python
# Frozen Z_p reference to MIRROR structurally — NEVER edit (byte-reproduction of the 296 corpus):
def can_add(S, p, a): ...                          # sum-relation check over Z_p
def random_maximal_symmetric_sumfree(p, rng): ...  # greedy loop, rng.shuffle(elems)
def cayley_adj(p, S): ...                           # list[set[int]] adjacency

# Existing solver param the det-budget extends (src/alpha2/solvers/result.py):
@dataclass(frozen=True)
class SolveParams:
    time_limit_s: float | None = None   # wall-clock (max_time_in_seconds / CBC -sec) — KEEP as-is
    threads: int = 1                     # CBC reference lineage is single-thread by contract — KEEP

# CP-SAT recorded-mode determinism already present (src/alpha2/solvers/cpsat.py ~209):
#   solver.parameters.num_workers = 1
#   solver.parameters.random_seed = _RANDOM_SEED
#   if p.time_limit_s is not None: solver.parameters.max_time_in_seconds = p.time_limit_s

# CBC backend today (src/alpha2/solvers/cbc.py ~281, BOTH solve_had2 + solve_had3):
#   solver = pulp.PULP_CBC_CMD(msg=0, threads=1, timeLimit=p.time_limit_s, logPath=str(log_path))
#   # ^ wall-clock ONLY today — the gap this plan closes. VERIFIED available: PULP_CBC_CMD accepts
#   #   maxNodes=<int> -> CBC "-maxNodes N"; on hitting the cap CBC returns
#   #   (LpStatusOptimal, LpSolutionIntegerFeasible) "Stopped on node limit", which the EXISTING
#   #   map_status already maps to INCUMBENT_ONLY in optimize mode (NOT PROVED_OPTIMAL) — so a
#   #   node-capped run that has not proved optimality flows to differential INSUFFICIENT -> RESISTANT.
#   #   Deterministic under threads=1 (node count is machine-speed-independent). map_status needs NO change.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Abelian group + RNG contract v2 subseeds</name>
  <files>src/alpha2/pool/sumfree/group.py, src/alpha2/pool/sumfree/rng.py</files>
  <behavior>
    - Abelian(factors): .n = product; .elements() deterministic itertools.product enumeration; add/neg component-wise mod invariant factors; neg(neg(a))==a.
    - Input validation (V5, T-8-05): reject non-int / bool factors, reject n > ~500 (DoS bound before any enumeration), mirror pool/cdm/generate._validate.
    - subseed(master, stage) = int from sha256(f"{master}:{stage}"); stable; distinct stages independent.
  </behavior>
  <action>Create `group.py` with the `Abelian` class per RESEARCH "Group arithmetic" (invariant-factor tuples, `itertools.product` enumeration, component-wise `add`/`neg`). Add a `_validate_factors` that int-validates (reject `bool`, reject non-int) and bounds the group order (cap ≈500, T-8-05 DoS) BEFORE constructing — mirror `pool/cdm/generate._validate`. Create `rng.py` with `subseed(master: int, stage: str) -> int` per RESEARCH Pattern 2 (sha256 of `f"{master}:{stage}"`, first 8 bytes big-endian) and thin `gen_rng(seed)` / `search_rng(seed)` factories returning `random.Random(subseed(seed, "sumfree-generate"))` and `random.Random(subseed(seed, "heuristic-search"))`. No new dependency (stdlib hashlib/random/itertools/math only).</action>
  <verify>
    <automated>uv run pytest tests/pool/sumfree/test_group.py tests/pool/sumfree/test_rng_v2.py -q</automated>
  </verify>
  <done>test_group + test_rng_v2 GREEN; group order is validated + bounded; subseeds are stage-independent and stable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Sum-free generators (structured + random) + abelian Cayley adjacency</name>
  <files>src/alpha2/pool/sumfree/generate.py</files>
  <behavior>
    - can_add(S, group, a): abelian generalization of the frozen Z_p can_add (uses group.add/neg).
    - random_greedy_maximal(group, rng): greedy loop, symmetric maximal S; expected to PACK.
    - middle_interval(group|Zn): Andrásfai middle-third symmetric sum-free set; size 18 on Z_53.
    - green_ruzsa(group): extremal S keyed on the ARITHMETIC condition (smallest prime ≡2 mod3 / 3|n / all ≡1 mod3), size 10 on Z_31.
    - cayley_adj_abelian(group, S): list[set[int]] over an index map of group.elements().
    - Every generator's output re-checked: symmetric, sum-free (S ∩ (S+S) == ∅), 0 ∉ S, maximal (raise-based, no add possible) — NEVER trust a density formula un-rechecked.
  </behavior>
  <action>Create `generate.py` mirroring the frozen `generators/cayley.py` STRUCTURE but over `Abelian` (do NOT import from or edit the frozen module — Pitfall 4). Implement `can_add(S, group, a)` per RESEARCH Pattern 1; `random_greedy_maximal(group, rng)` generalizing `random_maximal_symmetric_sumfree` (consume the caller's rng via shuffle over `group.elements()`); the structured `middle_interval` (RESEARCH "Middle-third structured sum-free set") and `green_ruzsa` extremal builders keyed off the arithmetic condition (factor n by stdlib trial division, n≤500) — NOT an I/II/III numeral (Pitfall 6, A1). Provide `cayley_adj_abelian(group, S)` returning `list[set[int]]` via a stable element→index map. Add a `verify_sumfree_maximal(group, S)` raise-based re-check (symmetric, sum-free, 0∉S, maximal) and CALL it inside every generator before returning — the mandatory `can_add`-style guard (Pitfall 6). This runtime re-check is the load-bearing net that makes the Green–Ruzsa extremal-set-membership uncertainty (RESEARCH Open Q1, A1/A2) non-fatal: a wrong density formula cannot produce a bad S because the raise-based verify rejects it. Tag each S with its provenance kind ("structured:middle_interval" | "structured:green_ruzsa" | "random_greedy"). Emit a descriptor `{"invariant_factors": list, "S": sorted element tuples, "kind": tag}` for descriptor-driven rebuild (Pattern 2). Keep this module OUT of `[tool.ruff] extend-exclude` unless byte-order determinism requires it.</action>
  <verify>
    <automated>uv run pytest tests/pool/sumfree/test_generate.py tests/pool/sumfree/test_structured.py -q</automated>
  </verify>
  <done>test_generate + test_structured GREEN; Z_31 structured size 10, Z_53 middle-interval size 18; every S is verified symmetric+sum-free+maximal and its H triangle-free; the frozen `generators/cayley.py` is untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Canonical dedup + additive DETERMINISTIC solver budget on BOTH backends</name>
  <files>src/alpha2/pool/sumfree/dedup.py, src/alpha2/solvers/result.py, src/alpha2/solvers/cpsat.py, src/alpha2/solvers/cbc.py</files>
  <behavior>
    - canonical_key(adj_H, n): a single canonical certificate (shortg batch OR pynauty.certificate in-process) — ONE convention per key set, never mixed (Pitfall 5); WL-hash never used.
    - dedup_stream(descriptors): collapses isomorphic H, KEEPS the first (Γ,S) per class as provenance, LOGS collapsed duplicates and any structured==random collision (RESEARCH dedup discipline).
    - SolveParams gains TWO additive optional deterministic-budget fields: det_time (float, CP-SAT) and det_nodes (int, CBC node cap). time_limit_s and threads are unchanged.
    - CP-SAT: when det_time is set, use max_deterministic_time (work-count); num_workers stays 1, random_seed pinned.
    - CBC: when det_nodes is set, pass maxNodes to PULP_CBC_CMD (deterministic node cap under threads=1); the existing map_status already routes a node-limit stop to INCUMBENT_ONLY (not PROVED_OPTIMAL). No wall-clock time_limit_s is required for a recorded verdict.
    - REPRODUCIBILITY INVARIANT: whether a reported verdict is DECIDED (both backends PROVED_OPTIMAL) or RESISTANT is a deterministic function of (n, seed) — independent of machine speed or core count. A wall-clock cutoff on CBC (or CP-SAT) is forbidden for any path feeding a reported verdict.
  </behavior>
  <action>Create `dedup.py` with `canonical_key(adj_H, n)` and `dedup_stream(descriptors)`. Use `shortg` via `subprocess` arg-lists-only (never a shell string; validate numeric args first — T-8-04 shell-injection) for batch merges, OR `pynauty.certificate` in-process — pick ONE convention per key set and assert WL-hash is never the key (Pitfall 5, CLAUDE.md forbidden). On an isomorphic collision keep the FIRST descriptor and record the collapsed duplicates (including a structured-vs-random collision, which is itself a datapoint — never silently drop the structured one).

In `solvers/result.py` add TWO ADDITIVE optional fields to `SolveParams`: `det_time: float | None = None` (CP-SAT deterministic work-count) and `det_nodes: int | None = None` (CBC node-count cap) — do not remove or change `time_limit_s` or `threads`.

In `solvers/cpsat.py`, in BOTH `solve_had2` and `solve_had3`, when `p.det_time is not None` set `solver.parameters.max_deterministic_time = p.det_time` (Pattern 4) while keeping `num_workers = 1` + pinned `random_seed`; the wall-clock `max_time_in_seconds` path is unchanged when `det_time` is None.

In `solvers/cbc.py`, FIRST confirm at edit time that `pulp.PULP_CBC_CMD` accepts `maxNodes` (VERIFIED during revision: it is a first-class kwarg mapping to CBC `-maxNodes N`; under `threads=1` the node count is deterministic). Then, in BOTH `solve_had2` and `solve_had3`, add `maxNodes=p.det_nodes` to the `PULP_CBC_CMD(...)` construction alongside the existing `msg=0, threads=1, timeLimit=p.time_limit_s, logPath=...`. When `det_nodes is None`, PuLP leaves `maxNodes` unbounded (default 2147483647) → the frozen 296-corpus reproduction path is BYTE-UNCHANGED. Do NOT touch `map_status`: a node-limit stop already returns `(LpStatusOptimal, LpSolutionIntegerFeasible)` which maps to `INCUMBENT_ONLY` in optimize mode — the correct "not proved → INSUFFICIENT → RESISTANT" behavior, never a false PROVED_OPTIMAL. Do NOT map any deterministic budget to wall-clock `timeLimit` for a recorded verdict — that would reintroduce machine-speed nondeterminism (the exact failure this closes). These are the SOLE edits to the frozen solver modules and MUST leave the 296-corpus reproduction green.

If the confirmation somehow fails (a PuLP build without `maxNodes`), fall back to the documented alternative: CBC runs bounded by a deterministic node cap only; if none is genuinely available, CBC is used opportunistically and ONLY CP-SAT's deterministic proof drives the reported DECIDED/RESISTANT classification, with CBC agreement checked async and never gating determinism. Prefer the `maxNodes` solution; only document this fallback if `maxNodes` is truly unavailable (it is available in the pinned pulp==3.3.2).</action>
  <verify>
    <automated>uv run pytest tests/pool/sumfree/test_dedup.py -q && uv run pytest tests/test_reproduction_contract.py tests/test_corpus_r1.py tests/test_corpus_r2.py tests/test_corpus_r3.py tests/test_seed137_regression.py -q -m "not slow"</automated>
  </verify>
  <done>test_dedup GREEN with a single canonical convention and an explicit WL-hash-forbidden guard; `SolveParams.det_time` wires CP-SAT `max_deterministic_time` AND `SolveParams.det_nodes` wires CBC `maxNodes` additively; both backends can be bounded deterministically; the frozen 296-corpus reproduction stays green (both budget fields default None → unbounded → byte-unchanged).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Γ invariant factors (caller) → group enumeration | untrusted-but-bounded ints; validate before enumerating |
| descriptor graph6 → shortg subprocess | numeric args + graph6 cross the process boundary to nauty |
| SolveParams → CP-SAT / CBC parameters | budget must be DETERMINISTIC (work-count / node-count) for any recorded verdict — never wall-clock |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8-04 | Tampering | shortg subprocess args | mitigate | `subprocess.Popen` arg-lists only, never a shell string; validate numeric args first (pool/cdm/generate pattern) |
| T-8-05 | DoS | unbounded Γ enumeration | mitigate | cap group order ≈500 and validate factors BEFORE `itertools.product` |
| T-8-06 | Spoofing | non-canonical dedup collision | mitigate | canonical certificate only (shortg/pynauty), one convention per key set; WL-hash forbidden guard (Pitfall 5) |
| T-8-07 | Repudiation | wall-clock verdict flip under contention | mitigate | BOTH backends bounded deterministically: CP-SAT `max_deterministic_time` + num_workers=1, CBC `maxNodes` + threads=1; wall-clock forbidden for recorded verdicts (Pitfall 2) |
| T-8-SC | Tampering | package installs | accept | zero new packages this phase (audit trivially clean) |
</threat_model>

<verification>
- test_group / test_rng_v2 / test_generate / test_structured / test_dedup all GREEN.
- `git diff src/alpha2/generators/cayley.py` → EMPTY (frozen module untouched, Pitfall 4).
- `SolveParams` carries additive `det_time` (CP-SAT) + `det_nodes` (CBC); `cpsat.py` sets `max_deterministic_time`, `cbc.py` sets `PULP_CBC_CMD(maxNodes=...)`; neither touches `map_status`.
- A node-capped CBC solve that has not proved optimality reads INCUMBENT_ONLY (not PROVED_OPTIMAL) and is reproducible across two runs.
- `uv run pytest tests/test_reproduction_contract.py tests/test_corpus_r1.py tests/test_corpus_r2.py tests/test_corpus_r3.py tests/test_seed137_regression.py -q -m "not slow"` → 296-corpus reproduction green (both budget edits additive; default None → unbounded).
</verification>

<success_criteria>
Verified symmetric sum-free S (structured + random) over any abelian Γ with triangle-free H;
canonical dedup with WL-hash forbidden; RNG v2 subseeds stage-independent + descriptor rebuild
byte-exact; additive DETERMINISTIC budgets on BOTH backends (CP-SAT det_time + CBC det_nodes),
wall-clock forbidden for recorded verdicts; frozen generator + corpus untouched.
</success_criteria>

<output>
Create `.planning/phases/08-p1-p2-seeded-families-at-scale/08-02-SUMMARY.md` when done.
</output>
</content>
</invoke>
