---
phase: 08-p1-p2-seeded-families-at-scale
plan: 07
type: execute
wave: 3
depends_on: [08-01, 08-02]
files_modified:
  - src/alpha2/pool/sumfree/p1.py
autonomous: true
requirements: [POOL-1]
must_haves:
  truths:
    - "The TFP critical-size sweep is extended to n=31–32 with many new seeds, each instance exact-had₂ adjudicated and every kill certificated"
    - "Large-n showpieces (toward n≈1001–2001) are existence-only: heuristic HIT → trust-root verified; a heuristic miss is RESISTANT, never a result"
    - "All new P1 instances use RNG contract v2 subseeds and rebuild exactly from the stored descriptor"
    - "Resistance is tracked in the derived queue; zero heuristic-only claims"
  artifacts:
    - path: "src/alpha2/pool/sumfree/p1.py"
      provides: "P1 driver: n=31–32 exact sweep + large-n showpiece existence loop"
      min_lines: 70
  key_links:
    - from: "src/alpha2/pool/sumfree/p1.py"
      to: "search.heuristic.solve + corpus.verifier.verify_certificate"
      via: "existence-only showpiece (HIT→verify, MISS→RESISTANT)"
      pattern: "verify_certificate"
    - from: "src/alpha2/pool/sumfree/p1.py"
      to: "generators.tfp.triangle_free_process"
      via: "RNG v2 gen subseed"
      pattern: "triangle_free_process"
---

<objective>
Deliver P1 (the secondary, deliberately lighter track): extend the signature TFP-complement family
under the battery with resistance discipline. Two pieces — (1) the critical-size sweep at n=31–32
with many new seeds, exact-had₂ adjudicated via the existing dual-backend battery; (2) showpieces
pushed toward n≈1001–2001 as **existence-only** results (heuristic finds a size-≤2 K_χ model, the
trust root verifies it — no exact solve at these sizes, which are far past the ILP range). All new
instances use RNG contract v2 and rebuild from their stored descriptor.

Purpose: keep the P1 lineage growing at scale under the same zero-heuristic-only discipline, without
becoming the phase spine (Locked Decision 1: P1 is secondary to the P2 break-hunt).
Output: `pool/sumfree/p1.py`. Turns test_p1_sweep + test_showpiece GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-p1-p2-seeded-families-at-scale/08-CONTEXT.md
@.planning/phases/08-p1-p2-seeded-families-at-scale/08-RESEARCH.md
@src/alpha2/battery/pipeline.py

<interfaces>
```python
from alpha2.battery.pipeline import run_candidate, Budgets    # run_candidate("tfp", n, seed, ...) for n=31-32 exact
from alpha2.generators.tfp import triangle_free_process        # showpiece generation (existence-only)
from alpha2.invariants.matching import matching_number         # chi = n - nu at n≈2001 (blossom still exact/fast)
from alpha2.search.heuristic import solve                      # size-<=2 K_chi model search
from alpha2.corpus.verifier import verify_certificate          # SOLE trust root — nothing counts until it passes
from alpha2.corpus import schema                               # build_record + provenance_seed
from alpha2.pool.sumfree.rng import subseed, gen_rng, search_rng
# Path (additive, from 08-01): paths.P1_SHOWPIECE_CORPUS
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TFP critical-size sweep n=31–32, many new seeds (exact, corpus-consistent)</name>
  <files>src/alpha2/pool/sumfree/p1.py</files>
  <action>Create `p1.py` with `critical_sweep(ns=(31, 32), seeds, *, det_time=None, log_path=None)`. For each (n, seed) call the existing `run_candidate("tfp", n, seed, budgets=...)` — n=32 is the even-n critical row (G1 uses `nu = n//2 = 16`, already handled) and n=31 matches the corpus lineage (seed=137 exemplar). Collect terminal_state per instance (KILLED / KILLED-BY-GATE / SHC-CANDIDATE / RESISTANT / QUARANTINED), aggregate counts, and surface the RESISTANT set as the derived E3 queue (resistance tracking, SC1). Do NOT write to the frozen 296-corpus — run_candidate keeps its SC1 records in memory; this driver aggregates the returned dicts and logs via `battery.log.append_event(subsystem="pool/p1", ...)`. Where a deterministic budget is wanted for a recorded verdict, thread the deterministic budget through (via the 08-02 SolveParams fields — `det_time` for CP-SAT, `det_nodes` for CBC) rather than wall-clock, so a reported n=31–32 verdict is a deterministic function of (n, seed) on both co-equal backends. Assert n=31 results are consistent with the corpus lineage in the test.</action>
  <verify>
    <automated>uv run pytest tests/pool/sumfree/test_p1_sweep.py -q</automated>
  </verify>
  <done>test_p1_sweep GREEN; n=31–32 many-seed sweep produces exact-had₂ verdicts consistent with corpus lineage; the RESISTANT set is exposed as the derived queue; the frozen corpus is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Large-n showpiece existence loop (n≈1001–2001, heuristic HIT → verify)</name>
  <files>src/alpha2/pool/sumfree/p1.py</files>
  <action>Add `showpiece(n, seed, *, time_budget=..., corpus_path=None)` — EXISTENCE-ONLY (RESEARCH P1 scope + SRCH-02). Generate the TFP instance with RNG contract v2: `gen_rng(seed)` feeds `triangle_free_process(n, rng)`; store the descriptor for descriptor-driven rebuild (never RNG replay). Compute `chi = n - matching_number(adj, n)` (blossom is exact + fast to n≈2001). Run `solve(adj, n, chi, search_rng(seed), time_budget=...)` for a size-≤2 K_χ model. On a HIT, build a record via `corpus.schema.build_record` (+ Tutte–Berge witness) and route it through `verify_certificate` (the SOLE authority) — a verified model is a genuine existence certificate; append it to `paths.P1_SHOWPIECE_CORPUS` (a NEW dedicated corpus, NOT the frozen 296). On a MISS, the outcome is **RESISTANT** and queues for E3 — NEVER a result, NEVER an impossibility claim (there is no exact solve at this size). No CP-SAT/CBC at these n (far past the ILP range). Mark the large-n test `@pytest.mark.slow`.</action>
  <verify>
    <automated>uv run pytest tests/pool/sumfree/test_showpiece.py -q</automated>
  </verify>
  <done>test_showpiece GREEN; a large-n showpiece HIT is trust-root verified and stored to the dedicated P1 corpus; a MISS is RESISTANT (queue), never a result; RNG v2 descriptor rebuild is byte-exact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| heuristic model (untrusted) → trust root | verify_certificate is the sole authority on existence |
| showpiece descriptor → rebuild | descriptor-driven, not RNG-replay (cross-platform safety) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8-11 | Repudiation | heuristic miss mislabeled as a result | mitigate | a MISS is RESISTANT (E3 queue) only; existence needs verify_certificate (SRCH-02) |
| T-8-12 | Tampering | frozen 296-corpus write | mitigate | showpieces append to a NEW dedicated P1 corpus; run_candidate stays in-memory |
| T-8-10 | DoS | unbounded n≈2001 generation | mitigate | existence-only (no exact solve); blossom bounded; time_budget on the heuristic |
| T-8-SC | Tampering | package installs | accept | zero new packages this phase |
</threat_model>

<verification>
- test_p1_sweep + test_showpiece GREEN.
- `git status data/corpus/` → frozen `hadwiger_alpha2_certificates.json` untouched (P1 uses its own corpus).
- A showpiece descriptor rebuilt on a second run yields byte-identical adjacency (RNG v2).
</verification>

<success_criteria>
P1 extended under discipline: n=31–32 many-seed exact sweep with resistance tracking; large-n
showpieces existence-only (HIT→verified, MISS→RESISTANT); RNG contract v2 + descriptor rebuild;
zero heuristic-only claims; frozen corpus untouched.
</success_criteria>

<output>
Create `.planning/phases/08-p1-p2-seeded-families-at-scale/08-07-SUMMARY.md` when done.
</output>
