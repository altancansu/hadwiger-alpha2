---
phase: 08-p1-p2-seeded-families-at-scale
plan: 06
type: execute
wave: 5
depends_on: [08-04, 08-05]
files_modified:
  - src/alpha2/pool/sumfree/sweep.py
autonomous: false
requirements: [POOL-2]
must_haves:
  truths:
    - "The structured-vs-random grid over odd |Γ| = 31–~500 (cyclic + a curated non-cyclic set) runs under the battery, instance-parallel, with per-verdict determinism preserved"
    - "Per family (structured:middle_interval / structured:green_ruzsa / random_greedy) the sweep tracks: gate-survival rate, verified-g points (g≤0 dense), exact g>0 points (only within the measured ILP window), and RESISTANT-rate"
    - "The plot data (g vs |Γ|, structured vs random, resistant-rate) is emitted for the knee/upward-trend/crossing analysis — the LOCKED prediction is falsifiable from it"
    - "Isomorphic (Γ,S) across merged structured/random streams are canonically deduped before adjudication; collisions are logged, not silently dropped"
  artifacts:
    - path: "src/alpha2/pool/sumfree/sweep.py"
      provides: "run_sweep(...) — the grid driver + aggregation into paths.SUMFREE_SWEEP"
      min_lines: 90
  key_links:
    - from: "src/alpha2/pool/sumfree/sweep.py"
      to: "src/alpha2/pool/sumfree/adjudicate.py"
      via: "adjudicate_grid_point per (Γ, kind, seed)"
      pattern: "adjudicate_grid_point|adjudicate_sumfree"
    - from: "src/alpha2/pool/sumfree/sweep.py"
      to: "src/alpha2/pool/sumfree/frontier.py"
      via: "exact_window_max bounds the g>0 window"
      pattern: "exact_window_max|frontier"
    - from: "src/alpha2/pool/sumfree/sweep.py"
      to: "src/alpha2/pool/sumfree/dedup.py"
      via: "canonical dedup of merged streams"
      pattern: "dedup_stream|canonical_key"
---

<objective>
Run the break-hunt: drive the g(G) adjudicator over the structured-vs-random grid across odd
|Γ| = 31–~500 (all cyclic odd n plus a curated non-cyclic set — e.g. Z_3^4, Z_3×Z_27, Z_5^3,
Z_3^2×Z_5, Z_9×Z_9), instance-parallel over the shared box, and aggregate the plot data the whole
phase exists to produce: `g` vs group order, structured vs random, gate-survival rate, and
RESISTANT-rate by family. Per RESEARCH §"Consequence for the sweep design", the curve is DENSE with
verified `g≤0` points everywhere (cheap heuristic+verify) and SPARSE with exact `g>0` points only
inside the measured ILP window (08-05); above it, a non-packing structured instance is RESISTANT and
queues for E3 — never a reported `g>0`. The LOCKED prediction ("structured breaks first") first
manifests as structured RESISTANT-rate climbing above random as |Γ| grows; a flat/negative curve
FALSIFIES it (itself publishable).

Purpose: emit the falsifiable evidence (knee / upward trend / crossing) that decides the hunt.
Output: `pool/sumfree/sweep.py` + the aggregated `paths.SUMFREE_SWEEP` plot dataset.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-p1-p2-seeded-families-at-scale/08-CONTEXT.md
@.planning/phases/08-p1-p2-seeded-families-at-scale/08-RESEARCH.md

<interfaces>
```python
from alpha2.pool.sumfree.group import Abelian
from alpha2.pool.sumfree.generate import green_ruzsa, middle_interval, random_greedy_maximal, cayley_adj_abelian
from alpha2.pool.sumfree.dedup import dedup_stream, canonical_key
from alpha2.pool.sumfree.adjudicate import adjudicate_grid_point, adjudicate_sumfree
from alpha2.pool.sumfree.frontier import exact_window_max        # measured ILP window (08-05)
from alpha2.pool.sumfree.rng import gen_rng
from alpha2.battery.log import append_event
# Path (additive, from 08-01): paths.SUMFREE_SWEEP  (data/results/sumfree_sweep.jsonl)

# DETERMINISTIC-BUDGET threading (08-02/08-04): run_sweep passes BOTH det_time (CP-SAT) and
# det_nodes (CBC) down to adjudicate_grid_point, so every recorded verdict is deterministic in
# (n, seed) regardless of worker contention. No wall-clock cutoff on any recorded-verdict path.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: grid enumeration + canonical dedup of merged structured/random streams</name>
  <files>src/alpha2/pool/sumfree/sweep.py</files>
  <action>Create `sweep.py` with `grid_descriptors(order_max=500, seed=0, non_cyclic=DEFAULT_NONCYCLIC)`. Enumerate all ODD cyclic orders 31..order_max as `Abelian((n,))` plus a curated non-cyclic set (RESEARCH Open Q2: Z_3^4=81, Z_3×Z_27, Z_5^3=125, Z_3^2×Z_5=45, Z_9×Z_9=81, …). EXPLICITLY EXCLUDE the ambiguous Green–Ruzsa case RESEARCH flags as unresolved (Open Q1, A1/A2): any NON-CYCLIC Γ whose every prime divisor is ≡ 1 (mod 3) — the "all primes ≡1 mod 3" non-cyclic type whose exact coset-membership extremal formula is not yet pinned. Implement the exclusion as a guard (factor the group order by stdlib trial division; if the group is non-cyclic AND every distinct prime divisor ≡ 1 mod 3, skip it and log the skip with reason "unresolved GR non-cyclic type-II"). The curated DEFAULT_NONCYCLIC set above already avoids this (each involves the primes 3 and/or 5, which are ≢1 mod3), but the guard makes the exclusion explicit and future-proof. Regardless of the exclusion, the `verify_sumfree_maximal` net (08-02) still runs on every generated S — a wrong density formula can never yield a bad instance. For each remaining Γ, build the structured descriptors (`green_ruzsa`, `middle_interval` where applicable) and `random_greedy_maximal(group, gen_rng(seed))`, tagging each with its `kind` and its group order. Merge the structured + random streams and pass through `dedup_stream` (canonical certificate; WL-hash forbidden) — KEEP the first descriptor per canonical class as provenance, LOG collapsed duplicates AND any structured-vs-random collision (a structured S and a random S yielding the same H is a recorded datapoint, never a silent drop). Return the deduped descriptor list with provenance kinds intact.</action>
  <verify>
    <automated>uv run pytest tests/pool/sumfree -q -k "sweep or grid or dedup" -m "not slow"</automated>
  </verify>
  <done>The grid yields deduped structured + random descriptors across odd |Γ|=31–500 (cyclic + curated non-cyclic); the ambiguous "all primes ≡1 mod3" non-cyclic case is excluded with a logged reason; collisions logged not dropped; kinds preserved; verify_sumfree_maximal net applied throughout.</done>
</task>

<task type="auto">
  <name>Task 2: instance-parallel sweep run + g-vs-|Γ| aggregation (per-verdict determinism preserved)</name>
  <files>src/alpha2/pool/sumfree/sweep.py</files>
  <action>Add `run_sweep(descriptors=None, *, det_time, det_nodes, seed=0, workers=None, order_max=500)`. For each deduped descriptor call `adjudicate_grid_point(descriptor, seed, det_time, det_nodes)` to get its compact row `{order, kind, gate_survived, terminal_state, g, had_2, had_3}`. Parallelize at the INSTANCE level (pytest-xdist-style fan-out or a simple process/job pool — breadth only). CRITICAL (Locked Decision 3): the deterministic budget makes each recorded verdict reproducible regardless of worker contention — every reported had_k<χ / RESISTANT verdict was produced under a DETERMINISTIC budget on BOTH co-equal backends inside the adjudicator (CP-SAT `SolveParams(det_time=...)` + num_workers=1, CBC `SolveParams(det_nodes=...)` + threads=1), so parallelism scales breadth, NEVER the determinism of a single verdict. A wall-clock cutoff on either backend is forbidden on the recorded-verdict path. Use `exact_window_max()` (08-05) to annotate each row: a non-packing outcome at order ≤ window is an exact `g>0` candidate; at order > window a heuristic-miss/timeout is RESISTANT (E3 queue), NOT a `g>0` point (RESEARCH §Consequence). Aggregate per (kind, order): gate-survival rate, count of verified g≤0, count of exact g>0, RESISTANT-rate, and the raw g values. Append every row + the aggregate to `paths.SUMFREE_SWEEP` (jsonl, atomic append via append_event discipline). Emit a summary table (structured vs random: g-vs-order series + resistant-rate-vs-order series) suitable for the knee/upward-trend/crossing plot.</action>
  <verify>
    <automated>uv run pytest tests/pool/sumfree -q -k "sweep or resistant or aggregate" -m "not slow"</automated>
  </verify>
  <done>The sweep runs the grid instance-parallel, preserves per-verdict determinism (both backends deterministically bounded), annotates the exact-window boundary, and emits structured-vs-random g-vs-|Γ| + resistant-rate series to SUMFREE_SWEEP; a small end-to-end grid slice adjudicates + stores honest certificates.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The FULL structured-vs-random g(G) sweep over odd |Γ|=31–~500 (order_max≈500 — the SC2 deliverable), with the aggregated plot dataset (g vs group order + RESISTANT-rate, structured vs random) written to `data/results/sumfree_sweep.jsonl`, and honest verified g(G) certificates in `data/corpus/sumfree_gscreen_certificates.json`. The phase is not delivered until the full-range hunt has actually run.</what-built>
  <how-to-verify>
    1. PRIMARY (required for delivery, SC2): run the FULL sweep over the whole grid —
       `uv run python -m alpha2.pool.sumfree.sweep --order-max 500 --det-time <budget> --det-nodes <budget>`
       (or the driver's documented entrypoint). `--order-max 151` is ONLY a fast pre-check to smoke-test
       wiring before committing the full run; it does NOT satisfy the phase deliverable.
    2. Open `data/results/sumfree_sweep.jsonl` and confirm each family (structured:middle_interval, structured:green_ruzsa, random_greedy) has, across the full 31–~500 range: gate-survival rate, dense verified g≤0 points, exact g>0 points ONLY within the measured ILP window, and a RESISTANT-rate series.
    3. Spot-check one g>0 certificate in the corpus: its statement must read "no K_χ minor with branch sets ≤3 … does not prove had(G)<χ; queued for E3" and contain NO "counterexample"/"had(G) <".
    4. Inspect the structured-vs-random comparison over the full range: is the g curve flat/negative (FALSIFIES the LOCKED prediction — a clean publishable negative) or is structured RESISTANT-rate climbing above random with a knee (a candidate family for E3)? Neither is announced as a break here.
    5. Confirm two runs of the same instance produce identical verdicts (determinism panel — both backends deterministically bounded, no wall-clock).
  </how-to-verify>
  <resume-signal>Type "approved" (record which outcome — falsified / resistant-tail / inconclusive — and confirm the FULL --order-max 500 sweep ran), or describe issues (partial run only, mislabeled cert, non-deterministic verdict, missing family series).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| merged structured/random streams → adjudication | isomorphic (Γ,S) must be canonically deduped first |
| parallel workers → recorded verdict | breadth only; per-verdict determinism must survive contention |
| sweep aggregate → publication narrative | a RESISTANT tail is a screen, not a break (E3 + referee gate) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8-07 | Repudiation | contended verdict flip across workers | mitigate | every recorded verdict produced under a DETERMINISTIC budget on BOTH backends inside the adjudicator (CP-SAT det_time, CBC det_nodes); wall-clock forbidden (Locked Decision 3) |
| T-8-06 | Spoofing | non-canonical dedup over merged streams | mitigate | dedup_stream canonical certificate; WL-hash forbidden; collisions logged (Pitfall 5) |
| T-8-16 | Repudiation | RESISTANT tail narrated as a break | mitigate | screen-only; g>0 = necessary-not-sufficient; E3 + human referee gate the word "break" (LD-4) |
| T-8-13 | Spoofing | gate non-survivor as g point | mitigate | g-plot over gate survivors only; gate-survival rate reported separately (Pitfall 3) |
| T-8-17 | Repudiation | unresolved GR non-cyclic type-II mis-generated | mitigate | non-cyclic "all primes ≡1 mod3" case excluded from the grid with a logged reason; verify_sumfree_maximal net regardless (Open Q1, A1/A2) |
| T-8-SC | Tampering | package installs | accept | zero new packages this phase |
</threat_model>

<verification>
- Non-slow sweep tests GREEN; a bounded grid slice adjudicates + stores honest certificates.
- The FULL `--order-max 500` sweep has run (SC2); `data/results/sumfree_sweep.jsonl` carries per-family g-vs-order + resistant-rate series across the whole 31–~500 range.
- Exact g>0 rows appear only at order ≤ the measured ILP window; above it, non-packing = RESISTANT.
- The ambiguous non-cyclic "all primes ≡1 mod3" case is absent from the grid (logged skip).
- Determinism panel: two runs of one recorded verdict agree (both backends deterministically bounded).
- Human checkpoint records the outcome (falsified / resistant-tail / inconclusive) and that the full-range run happened — no "break" announced.
</verification>

<success_criteria>
The structured-vs-random g(G) grid over the FULL odd |Γ|=31–~500 range runs under the battery with
instance-level parallelism and preserved per-verdict determinism (both co-equal backends
node/work-count bounded); the plot dataset (g vs group order + resistant-rate, structured vs random)
is emitted for the knee/trend/crossing analysis; the LOCKED prediction is falsifiable from it; every
kill is an honest verified certificate; RESISTANT instances queue for E3; the unresolved non-cyclic
Green–Ruzsa type is excluded.
</success_criteria>

<output>
Create `.planning/phases/08-p1-p2-seeded-families-at-scale/08-06-SUMMARY.md` when done.
</output>
</content>
</invoke>
