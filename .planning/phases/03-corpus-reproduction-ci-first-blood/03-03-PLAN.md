---
phase: 03-corpus-reproduction-ci-first-blood
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - src/alpha2/corpus/manifest.py
  - data/manifests/corpus-v1.manifest.json
  - tests/test_corpus_r1.py
  - tests/test_corpus_r2.py
  - tests/test_corpus_r3.py
autonomous: true
requirements: [ENV-04, ENV-06]
must_haves:
  truths:
    - "A golden-hash manifest (sha256 of canonical H_edges per instance, via the frozen schema convention) exists for all 296 instances and is committed"
    - "R1 asserts the corpus is exactly 296 records with per-family counts (284 TFP, 12 Cayley) and every record re-verifies from JSON"
    - "R2 regenerates H from seed and matches its sha256 to the manifest for both a TFP and a Cayley instance, gated by a doc-derived invariant"
    - "R3 replays a driver slice on the pinned interpreter and asserts byte-identical stored JSON (marked slow)"
  artifacts:
    - path: "src/alpha2/corpus/manifest.py"
      provides: "296-entry golden manifest builder reusing schema.h_edges_sha256"
      contains: "h_edges_sha256"
    - path: "data/manifests/corpus-v1.manifest.json"
      provides: "Frozen 296-entry golden-hash manifest (committed)"
    - path: "tests/test_corpus_r2.py"
      provides: "R2 generator-determinism panel (regenerate -> hash -> compare manifest)"
      contains: "h_edges_sha256"
    - path: "tests/test_corpus_r3.py"
      provides: "R3 pinned-interpreter replay gate (marked slow)"
      contains: "slow"
  key_links:
    - from: "src/alpha2/corpus/manifest.py"
      to: "alpha2.corpus.schema.h_edges_sha256"
      via: "per-instance hash reuse (never re-hash)"
      pattern: "schema\\.h_edges_sha256|from alpha2.corpus.schema import h_edges_sha256"
    - from: "tests/test_corpus_r2.py"
      to: "data/manifests/corpus-v1.manifest.json"
      via: "regenerated-hash equality check"
      pattern: "corpus-v1.manifest.json"
---

<objective>
Freeze the golden-hash manifest for the 296 and lock in the three-level reproduction contract
(R1 witness re-verify, R2 generator determinism, R3 pinned-interpreter replay) as the permanent
regression harness — with R1 tightened to the hard 296 / (284,12) count gate.

Purpose: Success Criteria 2 and 3 require the golden manifest frozen + committed and the R1/R2/R3
tests that CI (Plan 04) runs on every commit / release. The manifest MUST hash via
`schema.h_edges_sha256` so its bytes byte-match what `verifier.verify_model_record` recomputes;
the existing 2-entry `fingerprint.json` stays untouched — `corpus-v1.manifest.json` is additive.
Output: `corpus/manifest.py`, the committed 296-entry manifest, R2 + R3 tests, and a strict R1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-corpus-reproduction-ci-first-blood/03-RESEARCH.md
@.planning/phases/03-corpus-reproduction-ci-first-blood/03-PATTERNS.md
@.planning/phases/03-corpus-reproduction-ci-first-blood/03-02-SUMMARY.md

<interfaces>
From src/alpha2/corpus/schema.py:
  h_edges_sha256(H_edges) -> hexdigest   # lines 91-98: json.dumps(canonical_edges, separators=(",",":")) then sha256 — the FROZEN convention the verifier recomputes (verifier.py lines 89-92)
  canonical_edges(H_edges) -> sorted [min,max] pairs (lines 48-62)

From src/alpha2/generators/tfp.py: triangle_free_process(n, rng) -> (adj, m)
From src/alpha2/generators/cayley.py: random_maximal_symmetric_sumfree(p, rng); cayley_adj(p, S)
From src/alpha2/invariants/matching.py: matching_number(adj, n)
From src/alpha2/paths.py: CORPUS

Manifest entry shape (mirror data/manifests/fingerprint.json; keep the `h_edges_sha256` key the verifier convention produces):
  "tfp:n31:s1":      {"m": 131, "nu": 15, "chi": 16, "h_edges_sha256": "3c953d90...41e2"}
  "cayley:p31:s5310":{"m": ..., "nu": ..., "chi": ..., "h_edges_sha256": "..."}
Key scheme (Assumption A5): tfp:n{N}:s{S} and cayley:p{P}:s{S}.

From tests/test_fingerprint.py (analogs):
  test_golden_hash lines ~73-82 (regenerate -> hash -> compare, with a doc-invariant gate FIRST)
  test_heuristic_matches_d2_exact_pinned_env lines ~111-126 (pinned-interpreter single-RNG replay)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: corpus/manifest.py builder + freeze data/manifests/corpus-v1.manifest.json</name>
  <files>src/alpha2/corpus/manifest.py, data/manifests/corpus-v1.manifest.json</files>
  <read_first>
    - src/alpha2/corpus/manifest.py (being created)
    - src/alpha2/corpus/schema.py (h_edges_sha256 lines 91-98; canonical_edges lines 48-62 — reuse, never re-hash)
    - data/manifests/fingerprint.json (exact entry shape to mirror; MUST stay untouched)
    - .planning/phases/03-corpus-reproduction-ci-first-blood/03-PATTERNS.md (section `corpus/manifest.py` lines ~91-113 + "Do NOT overwrite fingerprint.json")
    - src/alpha2/paths.py (CORPUS)
  </read_first>
  <action>
    Create `src/alpha2/corpus/manifest.py` that loads the frozen 296-record corpus from `paths.CORPUS` and builds a dict keyed by the A5 scheme: for TFP records `tfp:n{invariants.n}:s{provenance.seed}`, for Cayley records
    `cayley:p{invariants.n}:s{provenance.seed}`. Each value is `{"m": num_H_edges, "nu": nu_H, "chi": chi_G, "h_edges_sha256": schema.h_edges_sha256(rec["H_edges"])}` — reuse `schema.h_edges_sha256` (do NOT hand-roll sha256; it must
    byte-match what the verifier recomputes). Write the manifest to `data/manifests/corpus-v1.manifest.json` (compact/stable key order). Assert the manifest has exactly 296 entries and that each entry's stored `h_edges_sha256`
    equals the record's own `H_edges_sha256` field (they must agree — same frozen convention). Do NOT touch or overwrite `data/manifests/fingerprint.json` (it remains the ENV-03 exemplar fixture). Provide a `main()` so
    `python -m alpha2.corpus.manifest` freezes the file; run it to produce the committed manifest.
  </action>
  <verify>
    <automated>uv run --frozen python -m alpha2.corpus.manifest && uv run --frozen python -c "import json; m=json.load(open('data/manifests/corpus-v1.manifest.json')); assert len(m)==296, len(m); assert m['tfp:n31:s1']['h_edges_sha256']=='3c953d9029ea09463ec838b670aaec8619f1018c79ee89e26fbcea7b2beb41e2'; assert any(k.startswith('cayley:') for k in m); print('manifest 296 ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `manifest.py` imports/uses `h_edges_sha256` from `alpha2.corpus.schema` and contains no local `hashlib.sha256` call.
    - `data/manifests/corpus-v1.manifest.json` has exactly 296 entries; `tfp:n31:s1.h_edges_sha256` == the frozen golden `3c953d90...41e2`; at least one `cayley:p*` key exists.
    - `data/manifests/fingerprint.json` is byte-unchanged (`git diff --quiet data/manifests/fingerprint.json`).
  </acceptance_criteria>
  <done>The 296-entry golden manifest is frozen and committed alongside the untouched fingerprint.json, hashed via the frozen verifier convention.</done>
</task>

<task type="auto">
  <name>Task 2: R2 determinism panel + tighten R1 to the hard 296/(284,12) count gate</name>
  <files>tests/test_corpus_r2.py, tests/test_corpus_r1.py</files>
  <read_first>
    - tests/test_corpus_r2.py (being created)
    - tests/test_corpus_r1.py (from Plan 01 — tighten the count assertions)
    - tests/test_fingerprint.py (analog: test_golden_hash lines ~73-82 — doc-invariant gate BEFORE trusting the hash)
    - .planning/phases/03-corpus-reproduction-ci-first-blood/03-PATTERNS.md (section `tests/test_corpus_r2.py` lines ~277-296)
    - .planning/phases/03-corpus-reproduction-ci-first-blood/03-RESEARCH.md (Code Examples "R2" lines ~330-346)
    - src/alpha2/corpus/schema.py (h_edges_sha256); src/alpha2/generators/{tfp,cayley}.py
  </read_first>
  <action>
    Create `tests/test_corpus_r2.py`: for a TFP instance regenerate via `triangle_free_process(n, random.Random(seed))`, build canonical `H_edges`, compute `h_edges_sha256`, and assert it equals `manifest["tfp:n{n}:s{seed}"]["h_edges_sha256"]`;
    gate the comparison behind a doc-derived invariant first (e.g. for (31,1) assert m==131 before trusting the hash — a porting bug must not self-certify). Add a Cayley R2 leg: `S = random_maximal_symmetric_sumfree(p, random.Random(seed))`,
    `adj = cayley_adj(p, S)`, hash, and compare to `manifest["cayley:p{p}:s{seed}"]`. Per-commit runs a small slice (e.g. tfp:n31:s1 + one Cayley); a nightly variant may loop all 296 (keep the heavy full-panel loop behind the `slow` marker or a
    separate function). Then TIGHTEN `tests/test_corpus_r1.py`: assert `len(records) == 296` and per-family counts `(tfp, cay) == (284, 12)`, and assert the seed-137 record's `model_branch_sets` equals the D.3 16-set literal (byte-equal to Appendix D.3).
    Keep the trust decision routed through raise-based `verify_certificate` (no assert governs verification).
  </action>
  <verify>
    <automated>uv run --frozen pytest tests/test_corpus_r2.py tests/test_corpus_r1.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_corpus_r2.py` uses `h_edges_sha256`, compares against `data/manifests/corpus-v1.manifest.json`, and has both a TFP leg and a Cayley leg (`cayley_adj` present); a doc-invariant gate precedes the hash compare.
    - `tests/test_corpus_r1.py` now contains `== 296` and `(284, 12)` and a seed-137 model equality against the D.3 literal.
    - `uv run --frozen pytest tests/test_corpus_r2.py tests/test_corpus_r1.py -q` exits 0.
  </acceptance_criteria>
  <done>R2 proves generator determinism against the frozen manifest (TFP + Cayley); R1 hard-gates the 296/(284,12) accounting and seed-137 == D.3.</done>
</task>

<task type="auto">
  <name>Task 3: R3 pinned-interpreter replay gate (marked slow)</name>
  <files>tests/test_corpus_r3.py</files>
  <read_first>
    - tests/test_corpus_r3.py (being created)
    - tests/test_fingerprint.py (analog: test_heuristic_matches_d2_exact_pinned_env lines ~111-126 — pinned single-RNG replay)
    - .planning/phases/03-corpus-reproduction-ci-first-blood/03-PATTERNS.md (section `tests/test_corpus_r3.py` lines ~300-305)
    - .planning/phases/03-corpus-reproduction-ci-first-blood/03-RESEARCH.md (Pitfall 4 "R3 not the only reproduction notion" lines ~289-292)
    - src/alpha2/repro/baseline.py (finalized run_instance — replay source); src/alpha2/paths.py (CORPUS)
    - pyproject.toml ([tool.pytest.ini_options] markers — the `slow` marker registered in Plan 01)
  </read_first>
  <action>
    Create `tests/test_corpus_r3.py` marked with `@pytest.mark.slow`: replay a driver slice (e.g. the baseline (31,1) instance, and one Cayley instance) into a `tmp_path` corpus using the SAME single-RNG contract v1 order the drivers use,
    then assert the produced record's load-bearing fields (`H_edges`, `model_branch_sets`, `matching_M`, `tutte_berge_U`, `invariants`) are byte-identical to the committed record for that instance in `paths.CORPUS`. Add a guard that only asserts the
    strict byte-equality when `sys.version.split()[0] == "3.12.13"` (the pinned interpreter) — on any other interpreter, downgrade to "a verifying model is produced" so the newer-Python canary in CI fails for a genuine hash reason, not R3 fragility
    (Pitfall 4). Do NOT make R3 the only reproduction notion — R1/R2 remain the version-proof primary checks. Register nothing new in pyproject (the `slow` marker already exists from Plan 01).
  </action>
  <verify>
    <automated>uv run --frozen pytest tests/test_corpus_r3.py -q -m slow</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_corpus_r3.py` uses `@pytest.mark.slow`, replays into `tmp_path` (does not touch the repo corpus), and asserts byte-identical stored fields against `paths.CORPUS` under the pinned interpreter guard `3.12.13`.
    - `uv run --frozen pytest tests/test_corpus_r3.py -q -m slow` exits 0 on the pinned interpreter.
    - `uv run --frozen pytest -q` (default, slow deselected) still green with no regression.
  </acceptance_criteria>
  <done>R3 replays the pipeline on the pinned interpreter and proves byte-identical stored JSON, gated as a slow release/nightly check.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frozen manifest -> verifier convention | The manifest must recompute hashes identically to the verifier; a divergent hashing routine would be a common-mode integrity gap. |
| regenerated H -> committed manifest | R2 crosses the environment/interpreter boundary: regenerated bytes are compared to the committed golden hash to catch drift. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-01 | Tampering | data/manifests/corpus-v1.manifest.json | mitigate | Manifest hashes via `schema.h_edges_sha256` (single frozen convention), asserts agreement with each record's own `H_edges_sha256`, and is committed; R2 re-derives and compares every commit. No hand-rolled sha256. |
| T-3-02 | Tampering (accidental drift) | generator determinism (TFP + Cayley) | mitigate | R2 determinism panel with a doc-invariant gate before trusting any hash; the manifest is the R2 anchor; Plan 04 wires a newer-Python canary that fails on purpose on drift. |
| T-3-03 | Elevation/Repudiation | R1/R2/R3 verified paths | mitigate | Trust decisions route through raise-based `verify_certificate`; no `assert` governs verification, so the Plan 04 `python -O` job stays meaningful. |
</threat_model>

<verification>
- `uv run --frozen pytest tests/test_corpus_r1.py tests/test_corpus_r2.py -q` green; `pytest tests/test_corpus_r3.py -q -m slow` green.
- Full suite green: `uv run --frozen pytest -q`.
- `data/manifests/corpus-v1.manifest.json` committed (296 entries); `fingerprint.json` byte-unchanged.
</verification>

<success_criteria>
- Golden manifest frozen + committed (296 entries, verifier-convention hashes).
- R1 hard-gates 296 / (284,12) + seed-137 == D.3; R2 proves TFP + Cayley determinism; R3 proves pinned-interpreter byte-replay.
</success_criteria>

<output>
Create `.planning/phases/03-corpus-reproduction-ci-first-blood/03-03-SUMMARY.md` when done.
</output>
