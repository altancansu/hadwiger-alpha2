---
phase: 03-corpus-reproduction-ci-first-blood
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/alpha2/repro/baseline.py
  - tests/test_corpus_r1.py
  - pyproject.toml
  - data/corpus/hadwiger_alpha2_certificates.json
autonomous: true
requirements: [ENV-04]
must_haves:
  truths:
    - "Running the finalized baseline driver produces schema-v1 TFP certificates stored through the trust root (not the old ad-hoc dict)"
    - "Every stored record re-verifies from JSON alone via verify_certificate (raise-based)"
    - "The stored (31, seed=1) model is byte-equal to Appendix D.2"
    - "No assert governs any correctness decision in the driver's verified path (fails safe under python -O)"
  artifacts:
    - path: "src/alpha2/repro/baseline.py"
      provides: "TFP baseline+showpiece driver emitting schema-v1 via store.append_certificate"
      contains: "append_certificate"
    - path: "tests/test_corpus_r1.py"
      provides: "R1 corpus-validity test: verify every stored record from JSON alone"
      contains: "verify_certificate"
    - path: "data/corpus/hadwiger_alpha2_certificates.json"
      provides: "Regenerated TFP baseline+showpiece slice (>=14 schema-v1 records)"
  key_links:
    - from: "src/alpha2/repro/baseline.py"
      to: "alpha2.corpus.store.append_certificate"
      via: "import + emission tail"
      pattern: "store\\.append_certificate"
    - from: "tests/test_corpus_r1.py"
      to: "alpha2.corpus.verifier.verify_certificate"
      via: "per-record loop"
      pattern: "verify_certificate\\("
---

<objective>
Wire the full corpus pipeline end-to-end for the TFP baseline slice — the thinnest real
vertical slice of Phase 3 — then prove it re-verifies from stored JSON alone.

This finalizes `repro/baseline.py` (currently emits an ad-hoc pre-schema dict) to the
schema-v1 path: `triangle_free_process` -> `extract_witness` -> `schema.build_record` ->
`store.append_certificate`, preserving the single-RNG contract v1 that byte-reproduces
Appendix D. It also lands the R1 corpus-validity test that every later wave scales to 296.

Purpose: Establish the proven assembly path (generator -> witness -> record -> verify-at-append)
on 14 real instances before scaling to 296. Every trust/algorithmic primitive already exists
and is adversarially proven (Phases 1-2); this plan composes them, it does NOT reimplement
verification, hashing, or storage.
Output: finalized `baseline.py`, `tests/test_corpus_r1.py` (green on the slice), a regenerated
14-record `data/corpus/hadwiger_alpha2_certificates.json`, and `pyproject.toml` test-infra pins.
</objective>

<phase_goal>
**As the** α = 2 program author, **I want to** regenerate the full 296-instance corpus and
have every instance independently re-verified from stored JSON, with that reproduction wired
into CI, **so that** every downstream phase builds on a trusted, tamper-evident regression anchor.

> Note to reviewer: the ROADMAP `**Goal:**` line for Phase 3 is outcome-shaped but not written
> in `As a / I want to / so that` form. The story above is a faithful restatement of that goal
> (corpus regenerates + independently re-verifies + runs as the permanent test suite), not an
> invented capability. If a canonical user story is required, run `/gsd mvp-phase 3` to set it.
</phase_goal>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-corpus-reproduction-ci-first-blood/03-RESEARCH.md
@.planning/phases/03-corpus-reproduction-ci-first-blood/03-PATTERNS.md

<interfaces>
<!-- Contracts the executor uses directly — no codebase exploration needed. -->

From src/alpha2/repro/baseline.py (FINALIZE — RNG core lines 27-36 kept verbatim; emission tail 44-67 replaced):
  def run_instance(n, seed, records): rng=random.Random(seed); adj,m=triangle_free_process(n,rng); nu=matching_number(adj,n); chi=n-nu; sets,init_conf,moves,restarts,tsolve=solve(adj,n,chi,rng)
  Canonical H_edges (line 46): sorted([min(u,v),max(u,v)] for u in range(n) for v in adj[u] if u < v)

From src/alpha2/corpus/schema.py:
  build_record(*, provenance, H_edges, nu_H, chi_G, model_branch_sets, matching_M, tutte_berge_U, method, omega_G=None, verified=True) -> dict   # derives had_2=len(model), RAISES if len<chi
  provenance_seed(family, n, seed, process) -> dict

From src/alpha2/invariants/witness.py:
  extract_witness(adj, n) -> (M, U, nu)

From src/alpha2/corpus/store.py:
  append_certificate(rec, path=None) -> None   # verify-at-append (BOTH verifier legs) + prefix re-verify + hash-chain + os.replace atomic write

From src/alpha2/corpus/verifier.py:
  verify_certificate(rec) -> k   # raises VerificationError on any violation; k = demonstrated had_2

From src/alpha2/search/heuristic.py:
  solve(adj, n, k, rng, time_budget=90.0) -> (sets, init_conf, moves, restarts, tsolve)

From src/alpha2/paths.py:
  CORPUS (repo corpus path); ensure_parent(path=CORPUS)

From tests/test_fingerprint.py:
  D2_MODEL literal (Appendix D.2 K16 model for n=31 seed=1) — reuse for the R1 byte-equality assertion.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: R1 corpus-validity test (RED) + pytest test-infra pins</name>
  <files>tests/test_corpus_r1.py, pyproject.toml</files>
  <read_first>
    - tests/test_corpus_r1.py (being created)
    - tests/test_fingerprint.py (analog — `test_stored_model_reverifies` lines ~85-94 for the "re-verify stored witness, never replay search" discipline; `D2_MODEL` literal near top)
    - .planning/phases/03-corpus-reproduction-ci-first-blood/03-PATTERNS.md (section `tests/test_corpus_r1.py`, lines ~253-273)
    - .planning/phases/03-corpus-reproduction-ci-first-blood/03-RESEARCH.md (Code Examples "R1" lines ~312-328)
    - src/alpha2/corpus/verifier.py (verify_certificate line 213; VerificationError line 30)
    - src/alpha2/paths.py (CORPUS)
    - pyproject.toml ([tool.pytest.ini_options] and dev extra)
  </read_first>
  <behavior>
    - Loads `paths.CORPUS`; for every stored record `verify_certificate(rec)` returns k and does not raise; assert k >= rec["invariants"]["chi_G"].
    - Asserts the slice is non-trivial: len(records) >= 14.
    - Asserts a seed-provenance record with n==31 and seed==1 exists whose `model_branch_sets` equals the Appendix D.2 literal (D2_MODEL) — the byte-equal-to-D.2 requirement.
    - Provides a family-count HELPER (counts family=="triangle_free_process_complement" vs "cayley_maximal_sumfree_Zp") but asserts only >=1 TFP here; the strict (284,12)/296 assertion is added in Plan 03 after the full freeze.
    - Test FAILS now (corpus is empty / .gitkeep only) — RED.
  </behavior>
  <action>
    Create `tests/test_corpus_r1.py` implementing `test_r1_all_records_reverify` per the behavior block, routing the trust decision ONLY through the raise-based `verify_certificate` (no `assert`
    inside the verification decision itself — asserts may only wrap post-conditions the -O job does not depend on; keep the verify call outside any assert). Import `D2_MODEL` by copying the literal
    into this test module (do NOT import from `test_fingerprint`), matching the "embedded literal, no cross-test import" discipline in test_verifier_dash_O.py. Read the corpus with `json.load(open(paths.CORPUS))`.
    In `pyproject.toml`: (1) replace the bare `"pytest"` in the `dev` extra with an exact 8.x pin — set `pytest==8.3.4` (or the latest 8.x uv resolves; record the exact pin) so the newer-Python canary cannot silently drift the runner (Assumption A6); (2) register a `slow` marker under `[tool.pytest.ini_options]`: `markers = ["slow: release/nightly replay gate"]` (used by Plan 03's R3). Run `uv lock` after the pin.
    Do NOT add the Cayley ruff exclude here — that belongs with the Cayley port in Plan 02.
  </action>
  <verify>
    <automated>uv run --frozen pytest tests/test_corpus_r1.py -q ; test $? -ne 0  # RED: expected to fail on the empty corpus</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_corpus_r1.py` contains `verify_certificate(` and does not contain `assert verify` (verification decision is not an assert).
    - `grep -c 'pytest==8' pyproject.toml` >= 1; `grep -c 'slow:' pyproject.toml` >= 1.
    - `uv run --frozen pytest tests/test_corpus_r1.py -q` exits non-zero (RED — corpus empty).
    - `uv lock` succeeds and `uv.lock` reflects the pytest pin.
  </acceptance_criteria>
  <done>R1 test exists, is RED against the empty corpus, and routes its trust check through verify_certificate; pytest is pinned and the `slow` marker is registered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Finalize baseline.py to schema-v1 store path; regenerate the 14-record slice (GREEN)</name>
  <files>src/alpha2/repro/baseline.py, data/corpus/hadwiger_alpha2_certificates.json</files>
  <read_first>
    - src/alpha2/repro/baseline.py (being finalized — keep RNG core lines 27-36; replace emission tail lines 44-67; keep canonical H_edges line 46; path usage lines 65-67)
    - .planning/phases/03-corpus-reproduction-ci-first-blood/03-PATTERNS.md (section `repro/baseline.py (FINALIZE)`, lines ~117-159 — the exact witness->build_record->append replacement + "Remove assert" note)
    - .planning/phases/03-corpus-reproduction-ci-first-blood/03-RESEARCH.md (Pattern 1 "MVP vertical slice", lines ~165-197)
    - src/alpha2/corpus/schema.py (build_record lines 246-310; provenance_seed lines 104-106)
    - src/alpha2/invariants/witness.py (extract_witness line 35)
    - src/alpha2/corpus/store.py (append_certificate lines 86-144)
    - src/alpha2/invariants/matching.py (matching_number line 11)
    - src/alpha2/search/heuristic.py (solve line 99)
  </read_first>
  <action>
    Rewrite `run_instance` in `baseline.py` to emit schema-v1 through the store, preserving the single-RNG contract v1 (ONE `random.Random(seed)`, then `triangle_free_process(n, rng)` FIRST, then
    `solve(adj, n, chi, rng)` next — never a subseed; Pitfall 3). After `solve`, call `extract_witness(adj, n)` to get (M, U, nu2) and build the canonical H_edges expression already at line 46. Assemble the
    record with `schema.build_record` using `schema.provenance_seed("triangle_free_process_complement", n, seed, "Bohman uniform triangle-free process")`, `model_branch_sets=[list(s) for s in sets]`,
    `matching_M=M`, `tutte_berge_U=U`, `method="heuristic"`, `omega_G=None`, `verified=True`; then `store.append_certificate(rec, path=path)`. REMOVE the `assert tf` at line 33 and the ad-hoc `rec = {...}` /
    `json.dump(records)` tail — correctness now routes through the raise-based verify-at-append gate, not asserts (Pitfall 6 / the -O job). Extend the `main()` instance list to add the two showpieces
    `(301, 13), (501, 14)` after the existing 12 baseline instances, so this driver emits 14 TFP records. Drive appends against `paths.CORPUS` starting from an EMPTY corpus (delete/recreate the file first so re-runs
    do not double-append onto a non-empty corpus — the store is append-only and will refuse a reorder). Keep the file in ruff `extend-exclude`? No — baseline.py is NOT determinism-frozen at the module level;
    only tfp.py/heuristic.py/cayley.py are. Leave baseline.py lintable.
  </action>
  <verify>
    <automated>uv run --frozen python -m alpha2.repro.baseline && uv run --frozen pytest tests/test_corpus_r1.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'store.append_certificate' src/alpha2/repro/baseline.py` >= 1; `grep -c 'assert ' src/alpha2/repro/baseline.py` == 0.
    - `grep -c '(301, 13)' src/alpha2/repro/baseline.py` == 1 and `grep -c '(501, 14)' src/alpha2/repro/baseline.py` == 1.
    - After running the driver: `python -c "import json,alpha2.paths as p; r=json.load(open(p.CORPUS)); print(len(r))"` prints >= 14, and every record has `schema_version==1`.
    - `uv run --frozen pytest tests/test_corpus_r1.py -q` exits 0 (GREEN — all stored records re-verify from JSON; (31,1) model == D.2).
  </acceptance_criteria>
  <done>baseline.py emits schema-v1 via the trust root with no asserts in the verified path; the 14-record TFP slice is stored and R1 is green on it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| driver -> store | Emission-time witness/model (UNTRUSTED) crosses into the append-only corpus; the store re-checks it via the trust root before persisting. |
| stored JSON -> R1 test | Untrusted on-disk bytes are re-validated from scratch by `verify_certificate` on every commit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-01 | Tampering | data/corpus/...certificates.json | mitigate | Every write goes through `store.append_certificate` (verify-at-append, both legs, hash-chain, os.replace); R1 re-verifies every stored record from JSON alone. No hand-rolled write path in the driver. |
| T-3-03 | Elevation/Repudiation | baseline.py + test_corpus_r1.py verified path | mitigate | Remove `assert tf`; the verification decision routes only through raise-based `verify_certificate` so `python -O` (Plan 04 job) cannot turn the check into a no-op. `grep assert == 0` in baseline.py. |
| T-3-02 | Tampering (accidental drift) | single-RNG contract v1 in run_instance | mitigate | Preserve the exact `Random(seed)` -> generator -> solve order; the (31,1)==D.2 byte-equality assertion in R1 trips on any drift. |
</threat_model>

<verification>
- `uv run --frozen pytest tests/test_corpus_r1.py tests/test_fingerprint.py -q` green.
- Full suite still green: `uv run --frozen pytest -q` (no regression of the 42 Phase-1/2 tests).
- `git diff --quiet src/alpha2/corpus/verifier.py src/alpha2/corpus/schema.py src/alpha2/corpus/store.py` — trust primitives byte-unchanged (composition-only phase).
</verification>

<success_criteria>
- R1 green on the baseline+showpiece slice; every stored record re-verifies from JSON.
- (31,1) stored model byte-equal to Appendix D.2.
- baseline.py assert-free in the verified path; pytest pinned; `slow` marker registered.
</success_criteria>

<output>
Create `.planning/phases/03-corpus-reproduction-ci-first-blood/03-01-SUMMARY.md` when done.
</output>
