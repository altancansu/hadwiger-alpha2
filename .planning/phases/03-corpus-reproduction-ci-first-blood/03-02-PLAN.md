---
phase: 03-corpus-reproduction-ci-first-blood
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/alpha2/generators/cayley.py
  - src/alpha2/repro/sweep.py
  - src/alpha2/repro/seed137.py
  - src/alpha2/repro/cayley_run.py
  - src/alpha2/repro/freeze.py
  - pyproject.toml
  - data/corpus/hadwiger_alpha2_certificates.json
autonomous: true
requirements: [ENV-04]
must_haves:
  truths:
    - "generators/cayley.py reproduces the Appendix C.3 sum-free Cayley construction deterministically (rng injected, never global)"
    - "The 269 sweep instances, the 12 Cayley instances, and the seed-137 exact-study record are all stored as full self-contained schema-v1 certificates"
    - "Cayley records carry inline canonical H_edges (not just S/p) so they verify from stored JSON alone"
    - "seed-137 is stored ONCE as the Appendix D.3 16-set literal with NO solver run (Phase 3 is solver-free)"
    - "After the freeze the corpus holds exactly 296 records: 284 TFP + 12 Cayley"
  artifacts:
    - path: "src/alpha2/generators/cayley.py"
      provides: "Verbatim C.3 port: can_add / random_maximal_symmetric_sumfree / cayley_adj"
      contains: "def cayley_adj"
    - path: "src/alpha2/repro/sweep.py"
      provides: "269-instance sweep driver (seed-137 excluded), schema-v1 via store"
      contains: "append_certificate"
    - path: "src/alpha2/repro/seed137.py"
      provides: "seed-137 exact-study driver carrying the D.3 K16 literal, no solver"
      contains: "SEED137_MODEL"
    - path: "src/alpha2/repro/cayley_run.py"
      provides: "12-instance Cayley driver, inline reconstructed H_edges"
      contains: "cayley_adj"
    - path: "src/alpha2/repro/freeze.py"
      provides: "Single ordered-freeze entry point (empty corpus -> 296 records)"
      contains: "296"
    - path: "data/corpus/hadwiger_alpha2_certificates.json"
      provides: "Frozen 296-record corpus (284 TFP + 12 Cayley)"
  key_links:
    - from: "src/alpha2/repro/cayley_run.py"
      to: "alpha2.generators.cayley.cayley_adj"
      via: "adjacency reconstruction before build_record"
      pattern: "cayley_adj\\("
    - from: "src/alpha2/repro/seed137.py"
      to: "alpha2.corpus.store.append_certificate"
      via: "literal-carry emission (no solve)"
      pattern: "append_certificate"
---

<objective>
Scale the proven baseline path to the full 296. Port the one missing generator
(`generators/cayley.py`, verbatim from Appendix C.3), write the three remaining drivers
(`sweep.py`, `seed137.py`, `cayley_run.py`) plus a single ordered-freeze entry point, then run
them from an empty corpus to freeze exactly 296 self-contained schema-v1 certificates.

Purpose: Success Criterion 1 requires all 296 "re-verified from stored JSON alone" — so the 269
in-run sweep instances (reference stored only 27) must become full stored certs, the 12 Cayley
records must carry inline `H_edges` (reference stored only `S`), and seed-137 must be present as
the solver-free Appendix D.3 literal. This is a composition + freezing task: reuse the finalized
baseline emission pattern verbatim; write no new verification, hashing, or storage logic.
Output: `generators/cayley.py`, three drivers + `freeze.py`, and a frozen 296-record corpus.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-corpus-reproduction-ci-first-blood/03-RESEARCH.md
@.planning/phases/03-corpus-reproduction-ci-first-blood/03-PATTERNS.md
@.planning/phases/03-corpus-reproduction-ci-first-blood/03-01-SUMMARY.md
@.planning/reference/alpha2-program-source.md

<interfaces>
<!-- The finalized baseline emission pattern (Plan 01) is the copy source for sweep/cayley/seed137. -->

From src/alpha2/generators/tfp.py (analog for the cayley module header + RNG-injected signature):
  header determinism-warning docstring lines 1-11; def triangle_free_process(n, rng) line 33

From Appendix C.3 (reference/alpha2-program-source.md; PATTERNS lines 52-83 quote the exact bodies):
  def can_add(S, p, a)                          # sum-free admissibility test
  def random_maximal_symmetric_sumfree(p, rng)  # uses rng.shuffle(elems) — determinism-sensitive
  def cayley_adj(p, S) -> list[set]             # Cayley adjacency over Z_p
  # DO NOT port ilp_had2 (C.3 ~393-425) — Phase 3 is solver-free; all 12 Cayley resolve heuristically.

From src/alpha2/corpus/schema.py:
  provenance_seed(family, n, seed, process); provenance_params(family, n, params, seed=None); build_record(...)

From src/alpha2/repro/baseline.py (FINALIZED in Plan 01 — copy run_instance verbatim):
  witness -> canonical H_edges -> build_record(provenance_seed(...), method="heuristic") -> append_certificate

From src/alpha2/invariants/matching.py: matching_number(adj, n) line 11
From src/alpha2/invariants/witness.py: extract_witness(adj, n) -> (M, U, nu) line 35
From src/alpha2/search/heuristic.py: solve(adj, n, k, rng, time_budget=90.0) line 99

Sweep decomposition (269): n=31 s in 100..299 excluding 137 (199) + n=51 s 100..149 (50) + n=101 s 100..119 (20).
Cayley (12): p in {31,53,101,151}, seed = 5000 + 10*p + k for k in {0,1,2}  ->  31:{5310,5311,5312} 53:{5530,5531,5532} 101:{6010,6011,6012} 151:{6510,6511,6512}.
seed-137 D.3 model (K16, 9 pairs + 7 singletons):
  [[2,20],[4,7],[8,18],[9,13],[12,27],[16,22],[17,24],[19,29],[26,28],[0],[1],[3],[10],[11],[21],[23]]
  method = "exact ILP (CBC): had_2(G)=17"  (documents true had_2; family = Phase 4);  omega_G = 14; had_2 derived = 16 (interim).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port generators/cayley.py verbatim from Appendix C.3 + ruff exclude</name>
  <files>src/alpha2/generators/cayley.py, pyproject.toml</files>
  <read_first>
    - src/alpha2/generators/cayley.py (being created)
    - src/alpha2/generators/tfp.py (analog for the module header + RNG-injected signature; header lines 1-11, signature line 33)
    - .planning/reference/alpha2-program-source.md (Appendix C.3 — the exact bodies of can_add / random_maximal_symmetric_sumfree / cayley_adj)
    - .planning/phases/03-corpus-reproduction-ci-first-blood/03-PATTERNS.md (section `generators/cayley.py`, lines ~33-88 — verbatim bodies + "Do NOT port ilp_had2")
    - pyproject.toml ([tool.ruff] extend-exclude — currently lists tfp.py + heuristic.py)
  </read_first>
  <action>
    Create `src/alpha2/generators/cayley.py` containing `can_add(S, p, a)`, `random_maximal_symmetric_sumfree(p, rng)`, and `cayley_adj(p, S)` copied byte-verbatim from Appendix C.3 (bodies exactly as quoted in
    03-PATTERNS.md lines 52-83). `rng` is a parameter, never a global — `random_maximal_symmetric_sumfree(p, rng)` calls `rng.shuffle(elems)`; never seed inside the module. Prepend the tfp.py-style determinism-warning
    docstring retargeted to C.3 (byte-identical bodies; do NOT reformat / run `ruff --fix` / reorder comprehensions — byte-reproduction depends on CPython set-iteration order AND rng.shuffle order). Do NOT port
    `ilp_had2` — Phase 3 is solver-free. Add the string `src/alpha2/generators/cayley.py` to the `[tool.ruff] extend-exclude` list in pyproject.toml (Pitfall 3 — determinism-sensitive rng.shuffle).
  </action>
  <verify>
    <automated>uv run --frozen python -c "import random; from alpha2.generators.cayley import random_maximal_symmetric_sumfree, cayley_adj; S=random_maximal_symmetric_sumfree(31, random.Random(5310)); a=cayley_adj(31,S); print(len(S), sum(len(x) for x in a))"</automated>
  </verify>
  <acceptance_criteria>
    - `src/alpha2/generators/cayley.py` contains `def can_add(`, `def random_maximal_symmetric_sumfree(`, and `def cayley_adj(`; contains `rng.shuffle`; does NOT contain `ilp_had2` or `import pulp`.
    - `grep -c 'generators/cayley.py' pyproject.toml` == 1 (ruff exclude present).
    - The import+construct smoke command exits 0 and prints a nonempty symmetric S and a positive edge count.
  </acceptance_criteria>
  <done>cayley.py exists with the three verbatim C.3 functions, rng injected, no solver, and is ruff-excluded.</done>
</task>

<task type="auto">
  <name>Task 2: sweep.py (269) + seed137.py (D.3 literal, no solver)</name>
  <files>src/alpha2/repro/sweep.py, src/alpha2/repro/seed137.py</files>
  <read_first>
    - src/alpha2/repro/baseline.py (FINALIZED Plan 01 — copy run_instance emission verbatim)
    - .planning/phases/03-corpus-reproduction-ci-first-blood/03-PATTERNS.md (sections `repro/sweep.py` lines ~162-180 and `repro/seed137.py` lines ~216-243)
    - .planning/phases/03-corpus-reproduction-ci-first-blood/03-RESEARCH.md (Pattern 2 "seed-137 carried literal" lines ~199-211; Pitfall 1 decomposition lines ~269-276)
    - src/alpha2/corpus/schema.py (provenance_seed lines 104-106; build_record lines 246-310)
    - .planning/phases/02-trust-root-corpus-schema/02-02-SUMMARY.md (seed-137 interim record: had_2=16 derived, method documents 17, omega_G=14)
  </read_first>
  <action>
    Create `sweep.py` by copying the finalized baseline `run_instance` verbatim (same generator -> solve -> witness -> build_record(provenance_seed, method="heuristic") -> append_certificate path, single-RNG contract v1).
    The ONLY difference is `main()`'s instance list, encoded as data: `[(31,s) for s in range(100,300) if s != 137] + [(51,s) for s in range(100,150)] + [(101,s) for s in range(100,120)]`; assert this list has length 269 before running.
    seed-137 is EXCLUDED here (it is stored once by seed137.py as the exact study — Pitfall 1). Create `seed137.py` that regenerates H from `random.Random(137)` via `triangle_free_process(31, rng)` and carries the
    Appendix D.3 model literal `SEED137_MODEL = [[2,20],[4,7],[8,18],[9,13],[12,27],[16,22],[17,24],[19,29],[26,28],[0],[1],[3],[10],[11],[21],[23]]` — do NOT call `solve`, do NOT run CBC (Phase 3 is solver-free; Anti-Pattern
    "Re-solving seed-137"). Build the record with `provenance_seed("triangle_free_process_complement", 31, 137, "Bohman uniform triangle-free process")`, `model_branch_sets=SEED137_MODEL`, `method="exact ILP (CBC): had_2(G)=17"`,
    `omega_G=14`, extracting M/U via `extract_witness(adj, 31)`. had_2 derives to 16 (the interim); `method` containing "ILP" makes `reproduction.kind=semantic` automatically. Both drivers append to `paths.CORPUS` (or a `path` argument);
    keep asserts out of any verified-path decision. Do NOT freeze/commit the corpus here — Task 3 runs the full ordered freeze.
  </action>
  <verify>
    <automated>uv run --frozen python -c "import pathlib; s=pathlib.Path('src/alpha2/repro/sweep.py').read_text(); assert 'append_certificate' in s and 'if s != 137' in s; z=pathlib.Path('src/alpha2/repro/seed137.py').read_text(); assert 'SEED137_MODEL' in z and 'solve(' not in z; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `sweep.py` contains `append_certificate` and the `range(100, 300)` / `!= 137` exclusion; a runtime `assert len(instances) == 269` is present.
    - `seed137.py` contains the exact `SEED137_MODEL` 16-set literal, contains `triangle_free_process(31`, does NOT contain `solve(` and does NOT import pulp/ortools; method string contains `had_2(G)=17`; omega_G is 14.
    - Neither driver contains an `assert` governing a verification decision (verification routes through append_certificate).
  </acceptance_criteria>
  <done>sweep.py emits the 269 TFP sweep certs and seed137.py emits the solver-free D.3 exact-study record, both via the trust root.</done>
</task>

<task type="auto">
  <name>Task 3: cayley_run.py (12, inline H_edges) + freeze.py ordered freeze to 296</name>
  <files>src/alpha2/repro/cayley_run.py, src/alpha2/repro/freeze.py, data/corpus/hadwiger_alpha2_certificates.json</files>
  <read_first>
    - src/alpha2/repro/baseline.py (FINALIZED driver shape) + Appendix C.3 run() for Cayley accounting
    - .planning/phases/03-corpus-reproduction-ci-first-blood/03-PATTERNS.md (section `repro/cayley_run.py`, lines ~183-213 — inline H_edges reconstruction + provenance_params)
    - .planning/phases/03-corpus-reproduction-ci-first-blood/03-RESEARCH.md (Pattern 3 "Cayley H_edges reconstructed", lines ~213-225; Pitfall 2 lines ~278-281; Pitfall 5 O(N^2) note lines ~294-298)
    - src/alpha2/generators/cayley.py (random_maximal_symmetric_sumfree, cayley_adj — from Task 1)
    - src/alpha2/corpus/schema.py (provenance_params lines 109-117)
    - src/alpha2/corpus/store.py (append_certificate lines 86-144 — append-only, refuses reorder)
  </read_first>
  <action>
    Create `cayley_run.py`: for `p in (31, 53, 101, 151)` and `k in range(3)`, `seed = 5000 + 10*p + k` (12 instances). Per instance, single-RNG contract v1: `rng = random.Random(seed)`; `S = random_maximal_symmetric_sumfree(p, rng)`
    (consumes rng via shuffle) FIRST; `adj = cayley_adj(p, S)`; `nu = matching_number(adj, p)`; `chi = p - nu`; `sets, *_ = solve(adj, p, chi, rng, time_budget=60)` next. Reconstruct and store INLINE canonical
    `H_edges = sorted([min(u,v),max(u,v)] for u in range(p) for v in adj[u] if u < v)` (Pitfall 2 — the reference stored only S). Extract M/U via `extract_witness(adj, p)`. Build with
    `provenance_params("cayley_maximal_sumfree_Zp", p, {"S": sorted(S)}, seed=seed)`, `model_branch_sets=[list(s) for s in sets]`, `method="heuristic"`, `omega_G=None`; append via the store. All 12 resolve heuristically — no ilp fallback.
    Create `freeze.py` as the SINGLE ordered-freeze entry point: it deletes/recreates `paths.CORPUS` as an empty JSON array `[]`, then invokes the four drivers in order — baseline (14) -> sweep (269) -> cayley_run (12) -> seed137 (1) —
    appending all 296 onto the empty corpus (sequential `append_certificate` is O(N^2); accept for MVP per A3/Pitfall 5 — never weaken verify-at-append). After the run, `freeze.py` asserts `len(corpus) == 296` and the family counts
    (284 `triangle_free_process_complement`, 12 `cayley_maximal_sumfree_Zp`). Run `python -m alpha2.repro.freeze` to produce the committed 296-record corpus; leave it in place for Plan 03 to hash. SLOW-FREEZE ESCAPE HATCH (W5 / Pitfall 5 / A3): the full 296 freeze re-verifies every prior record on each append (O(N^2), ~44k verify calls, plus n=501 witness extraction) and can exceed the 30s Nyquist verify latency by minutes — an accepted MVP tradeoff. If the `<verify>` command risks truncation, run `freeze.py` in the background (or raise the tool timeout), and/or reduce the heuristic `time_budget` for the bulk pass; never weaken verify-at-append or add a batch path here. The committed 296-record corpus is the artifact R1/R2/R3 check.
  </action>
  <verify>
    <automated>uv run --frozen python -m alpha2.repro.freeze && uv run --frozen python -c "import json,collections,alpha2.paths as p; r=json.load(open(p.CORPUS)); c=collections.Counter(x['provenance']['family'] for x in r); assert len(r)==296, len(r); assert c['triangle_free_process_complement']==284 and c['cayley_maximal_sumfree_Zp']==12, dict(c); print('296 ok', dict(c))"</automated>
  </verify>
  <acceptance_criteria>
    - `cayley_run.py` contains `cayley_adj(`, builds inline `H_edges` (contains the `for v in adj[u] if u < v` comprehension), uses `provenance_params(`, and does NOT import pulp/ortools.
    - `freeze.py` empties the corpus before appending (contains a write of `[]` / unlink before the run) and asserts `len(... ) == 296`.
    - After `python -m alpha2.repro.freeze`: the corpus has exactly 296 records with family counts (284, 12); the verify command exits 0.
    - `uv run --frozen pytest tests/test_corpus_r1.py -q` still exits 0 against the now-296 corpus.
  </acceptance_criteria>
  <done>The Cayley driver stores 12 self-contained records and freeze.py deterministically rebuilds the full 296-record corpus (284 TFP + 12 Cayley) from empty.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cayley generator -> store | Cayley adjacency is reconstructed at emission; the record must carry inline H_edges so the verifier trusts stored bytes, not a reconstruction routine. |
| driver -> append-only corpus | 296 untrusted emission records cross into the corpus; each is re-checked by the trust root before persistence, and the whole prefix is re-verified on every append. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-01 | Spoofing/Tampering | Cayley certificates | mitigate | Store inline canonical `H_edges` (not just S/p) so `verify_certificate` re-derives adjacency from stored bytes; a Cayley record with absent/empty H_edges fails `_build_adj` (Pitfall 2). |
| T-3-02 | Tampering (accidental drift) | generators/cayley.py rng.shuffle determinism | mitigate | Verbatim C.3 bodies + ruff `extend-exclude` (no `--fix`/format); single-RNG contract v1 preserved; Plan 03 R2 hashes each Cayley H against the frozen manifest. |
| T-3-01b | Tampering (miscount) | freeze.py 296 accounting | mitigate | seed-137 stored ONCE (excluded from sweep); freeze asserts len==296 and family counts (284,12); Plan 03 R1 tightens to a hard count gate. |
| T-3-SC | Tampering (supply chain) | no new PyPI installs | accept | Phase 3 introduces no new packages (runtime stack unchanged from the Phase-1 verified lockfile); the only new external dep is the CI action, pinned to a SHA in Plan 04. |
</threat_model>

<verification>
- `uv run --frozen python -m alpha2.repro.freeze` produces a 296-record corpus (284 TFP + 12 Cayley).
- `uv run --frozen pytest tests/test_corpus_r1.py tests/test_fingerprint.py -q` green on the full corpus.
- `git diff --quiet src/alpha2/corpus/verifier.py src/alpha2/corpus/schema.py src/alpha2/corpus/store.py src/alpha2/generators/tfp.py` — no trust/generator primitive mutated.
</verification>

<success_criteria>
- Exactly 296 self-contained schema-v1 certificates stored (284 TFP + 12 Cayley), all re-verifying from JSON alone.
- Cayley records carry inline H_edges; seed-137 present once as the solver-free D.3 literal.
- freeze.py deterministically rebuilds the corpus from empty as a single command.
</success_criteria>

<output>
Create `.planning/phases/03-corpus-reproduction-ci-first-blood/03-02-SUMMARY.md` when done.
</output>
