---
quick_id: 260722-r57
slug: ruff-lint-phase-1-6
description: Resolve 6 ruff lint errors across Phase 1-6 source and tests
date: 2026-07-22
status: complete
code_commit: 30fbd73
---

# Quick Task 260722-r57 — Summary

## Outcome

`ruff check src tests` now passes clean (was 6 errors). Full test suite green.
No behavioral change; no determinism-excluded module touched.

## Changes (commit 30fbd73)

| File | Error | Fix |
|------|-------|-----|
| `src/alpha2/invariants/cliques.py:25` | E702 | semicolon → two lines |
| `src/alpha2/invariants/matching.py:13,29` | E702 ×2 | semicolon → two lines |
| `tests/test_cpsat_backend.py:24` | F401 | removed unused `itertools.combinations` |
| `tests/test_differential_panel.py:50` | F401 | removed unused `provenance_seed` |
| `tests/test_schema_roundtrip.py:87` | F841 | removed unused local `n = 5` |

## Verification

- **ruff:** `All checks passed!`
- **CHI-01 AST guard** (`test_chi_no_estimate.py`): pass — semicolon splits do not
  disturb the guard, which targets the `max_weight_matching` call site.
- **Affected modules** (35 tests incl. `test_differential_panel`, `test_schema_roundtrip`,
  `test_cpsat_backend`, `test_tutte_berge`): pass.
- **Full suite:** 245 passed under load; the 2 seed-137 optimality proofs
  (`test_seed137_dual_backend`, `test_seed137_had2_17_proved_optimal_through_trust_root`)
  re-run **in isolation → 2 passed (327 s)**. Their earlier full-suite failure was
  self-inflicted CPU contention (multiple parallel pytest runs starving the CBC
  solve past its 600 s limit), NOT a regression — lint edits cannot affect solver
  optimality.

## Note for CI / Phase 7

The seed-137 optimality tests are timing-sensitive (~149 s single-thread solve vs
600 s limit, ~4× headroom). On a loaded runner they can flake for the same reason,
independent of code. Worth knowing before the Phase 7 frontier work adds more
solver load.
